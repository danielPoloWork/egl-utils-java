# ROADMAP — egl-utils-java

The negotiated milestone roadmap. Authored by the **producer** in the `plan` phase
(`.eados-core/orchestrator/commands/plan.md`) from the approved RFCs recorded in
`orchestrator/project.yaml` → `delivery_state.refs.rfcs`. The `roadmap-covers-rfcs` gate
(`traceability.py`) reads the `## Milestone N` sections below; `plan → scaffold` is illegal until
every recorded RFC is addressed by at least one milestone.

**Every item carries a size and a route.** Size is a macroscopic T-shirt estimate
(`plan.yaml sizing_scale: XS S M L XL`) — not story points. Route is the advisory model/effort
recommendation from the `os/routing` policy (ADR-0017), stated as **tier / effort**, never a model
name — names rot, the dated `catalog:` owns them. Routes below were resolved with
`route_advice.py`, not asserted; the signals that earned each one are listed under
[Routing basis](#routing-basis). **The route is advisory: the human keeps final model authority.**

> **Note on section order.** The context sections come first and the milestones last, deliberately.
> `traceability.parse_milestones` slices each `## Milestone N` section up to the *next* one, so
> anything placed after the last milestone is absorbed into its body — an appendix that merely
> *mentions* an RFC id would register as a coverage edge from Milestone 8. Keeping the milestones
> last means the RFC → milestone edges the gate derives are exactly the real ones.

## Negotiation inputs — stated, because they shape everything below

| Input | Value | Where it comes from |
|---|---|---|
| Approved RFCs | **RFC-0001** only | `delivery_state.refs.rfcs` |
| Capacity | **one person** — the owner | `ownership.owner`/`maintainer`/`author` all = Daniel Polo; `governance` notes reviewers "stay deferred until there is a second collaborator" |
| Dates | **none, deliberately** | `governance.version_start: "pre-1.0 milestone-driven"`. This project's own release policy is milestone-driven, not calendar-driven, so this roadmap sequences work and does **not** invent dates |
| Ordering | the ADR-001 dependency DAG | core first, then capability leaves, then adapters, then release |
| Milestone vocabulary | `semver` | `domains/software.yaml milestone_vocabulary` |

**Scope is the owner's call.** The producer reconciles and sequences; cuts and additions are a human
decision (`plan.md` Boundary). Nothing below is committed scope until the owner says so.

## The design debt this roadmap makes visible

The negotiation protocol is explicit: *"Each item references an approved RFC — no roadmap item
without a design behind it."* Measured against that rule, **one milestone of eight is covered**:

| Milestone | RFC behind it | Status |
|---|---|---|
| M1 repository foundations | ADR-001 + ADR-004 (both Accepted) — no RFC | structural decisions already settled by ADR; see note below |
| **M2 core foundations** | **RFC-0001** | **covered** |
| M3 core cross-cutting | — | **needs an RFC** (item 3.0) |
| M4 json and jdbc | — | **needs an RFC** (item 4.0) |
| M5 concurrent | — | **needs an RFC** (item 5.0) |
| M6 security | ADR-003 covers the JWT library choice only | **needs an RFC** (item 6.0) |
| M7 adapters and test support | — | **needs an RFC** (item 7.0) |
| M8 release engineering | — | see 8.0 note |

So this roadmap schedules **five more RFCs** as first-class items (3.0, 4.0, 5.0, 6.0, 7.0). That is
not padding: `workflow.yaml` defines `plan → design` as a legal, non-human-gated transition precisely
so a new RFC can re-open planning without restarting the project. Each of those items is a return to
the `design` phase, and the milestone's implementation items are blocked on it.

**M1 is the deliberate exception.** Its content is structural and already decided by two Accepted
ADRs (ADR-001 module split, ADR-004 generated layout). Writing an RFC to re-decide an accepted ADR
adds ceremony, not a decision. M1 items therefore cite the ADR that governs them.

**M8 carries no RFC item.** Its work is CI and release plumbing implementing NFR-09 through NFR-12,
which the specification already states normatively. Item 8.3 is the exception — it changes a stated
NFR's enforcement model — and is flagged `decision-heavy` accordingly.

## Sequencing

No dates — the project is milestone-driven pre-1.0 (`governance.version_start`). This is the
dependency order, which is what actually constrains the work:

```
M1 ─────────────────────────────────────────► everything (ADR-004: no code before the reactor)
 └─ 1.1 blocks 1.2–1.9 and all of M2

M2 ──► M3, M4, M5              [Result/ErrorDetail is the shared error vocabulary]
 │
 ├─ M3 ──┐
 ├─ M4 ──┤
 └─ M5 ──┴──► M7               [adapters bind what the modules expose]
              M6 ─┘            [independent of M3/M4; needs only M2]

M6 + M7 ──► M8                 [japicmp needs a published surface; 8.6 audits the finished one]
```

M3, M4, M5 and M6 are mutually independent once M2 lands — with capacity of one they are sequential
in practice, and the order among them is the owner's priority call, not a technical constraint.
**8.3 is the exception to that ordering:** it can be pulled forward at any time after 1.8, and doing
so would stop the M2 perf numbers from sitting in an advisory limbo for the whole project.

## Routing basis

Routes were resolved with `route_advice.py` against the `os/routing` policy, not asserted. The
signal sets and their verdicts:

| Signals | Verdict | Applied to |
|---|---|---|
| `severity:high` + flag `decision-heavy` | **frontier-reasoning / max** | 1.1, 8.3 — a wrong foundational call is the most expensive artifact here |
| `severity:high` + flag `sets-pattern` | **frontier-reasoning / high** | 1.6, 1.7, 2.1, 2.2, 8.1 — first of its class; every follower copies the template |
| `security` + `severity:high` | **frontier-reasoning / high** | 3.3, 4.1, 4.3, 6.1–6.4, 8.4, 8.5, 8.6 — on security posture a subtle miss dwarfs the routing saving |
| `adr` | **frontier-reasoning / high** | 1.10, 3.0, 4.0, 5.0, 6.0, 7.0, 5.3 — an RFC-, ADR- or interface-defining item is decision-heavy by definition |
| `severity:high` | **standard / high** | 1.4, 1.5, 2.3, 2.4, 4.4, 4.5, 5.1, 5.2, 7.1, 7.3, 7.4 |
| `severity:medium` | **standard / medium** | 1.2, 1.3, 1.8, 1.9, 2.5, 3.1, 3.2, 4.2, 7.2, 8.2 |

Two judgment calls, stated because the tool cannot make them: **4.5 `PageRequest` was not labelled
`security`** even though its sort whitelist is an injection defence — its primary job is pagination,
and labelling every item containing one validation as `security` would route the whole roadmap to the
top tier and make the signal meaningless. Conversely **4.1 `JsonMapper` was**, despite being a
three-flag configuration change: getting default typing wrong is a known CVE class, so the
consequence — not the size — earns the route.

At run time, `route_advice.py --check --current-model <id>` compares a step's route against the
session model and prints `ROUTE-OK` / `ROUTE-MISMATCH`. It never switches the model; model authority
is the human's (ADR-0017).

## Traceability

- **RFC → milestone:** the only approved RFC is addressed by Milestone 2 (items 2.1–2.5). Verified by
  `python .eados-core/tools/traceability.py ROADMAP.md RFC-0001`.
- **Recorded refs:** `orchestrator/project.yaml` → `delivery_state.refs`.
- **Spec → item:** every item cites its FR/NFR id from the manifest's `spec.functional_reqs` /
  `spec.nonfunctional_reqs`, published as [`docs/specs/01_spec_utils.md`](docs/specs/01_spec_utils.md).
  **Corrected by item 1.12:** this line used to name `.spec/d4np-java.md` §2/§6 as the id source, and
  that document contains **no `FR-` ids at all** (0 FR, 6 NFR — the vocabulary was minted during
  intake). It is superseded provenance; the precedence ladder is
  [ADR-0010](docs/adr/0010-single-specification-authority.md), and `spec-authority` in
  `consistency_lint.py` now enforces it — including that no item here cites an id no specification
  states.
- **Gate:** `roadmap-covers-rfcs` must pass before `plan → scaffold` is legal.

## Spec Coverage Map

Tracks which spec section is fulfilled by which roadmap item(s). Every spec section has a row with at
least one fulfilling item and a status glyph. Legend: ⏳ not started · 🚧 in progress · ✅ done ·
❎ N/A. Sections follow the six-section shape of `docs/specs/01_spec_util.md`. Read with the
`check_spec_map` lint: it requires a non-empty items cell and a recognised glyph per row.

Placed **before** the milestone sections on purpose — `traceability.parse_milestones` would otherwise
absorb this table into Milestone 8's body, the same appendix-bleed that produced a false
`RFC-0001 → M8` edge in the first draft.

| Spec § | Requirement | Roadmap items | Status |
|--------|-------------|---------------|--------|
| §1 | Objective & business context | 1.1, 1.7 | ⏳ |
| §2 | Functional requirements | 2.1, 2.2, 2.3, 2.4, 2.5, 3.1, 3.2, 3.3, 4.1, 4.2, 4.3, 4.4, 4.5, 5.1, 5.2, 5.3, 6.1, 6.2, 6.3, 7.1, 7.2, 7.3, 7.4 | ⏳ |
| §3 | Non-functional requirements | 1.5, 1.7, 1.8, 2.2, 2.3, 5.1, 5.2, 6.2, 6.4, 8.1, 8.2, 8.3, 8.4, 8.5 | ⏳ |
| §4 | Logical architecture | 1.1, 1.6, 1.7 | ⏳ |
| §5 | Public interface | 2.1, 2.2, 2.3, 2.4, 2.5, 5.3, 8.1 | ⏳ |
| §6 | Verification & test strategy | 1.3, 1.5, 1.8, 2.2, 5.1, 6.1, 8.2, 8.3, 8.6 | ⏳ |

Two rows worth reading closely. **§1** maps to the reactor and the enforcer rather than to a feature:
the objective is *framework independence*, and 1.1 + 1.7 are what make it a build property instead of
a claim (NFR-08). **§3** is the widest row because the NFRs are enforcement, not code — most of them
land in M1 and M8 rather than in the milestone that implements the thing being measured.

---

## Milestone 1 — Project bootstrap & CI

**Goal:** the thinnest slice that compiles, tests and ships under the full quality bar — which for
this project means the ADR-001 Maven reactor exists first, then the build, tests, quality configs and
CI matrix stand up on top of it. **Hard prerequisite for every later milestone**: no component code
lands before the reactor exists (ADR-004).

> Items 1.2–1.5 are the factory's universal day-zero bootstrap (`templates/ROADMAP.md.tmpl`); 1.1 and
> 1.6–1.9 come from `spec.milestone1_items`. The scaffold render and the plan-phase negotiation were
> merged here rather than one replacing the other — the render carried five bootstrap items the
> negotiation lacked, and the negotiation carried the sizes and routes the render has no concept of.
> Title taken from the render so it stays congruent with the generated README milestone table.

- [x] 1.1 Maven reactor skeleton — parent POM, the nine modules of ADR-001, and the BOM; relocate the rendered tree to become the `core` module's source root, updating every generated `src_main`/`src_test`/`src_bench` reference **and** the `consistency_lint.py` path assertions in the same item (ADR-004). Blocks 1.2–1.9 and all of M2. — size: **L** · route: **frontier-reasoning / max**
- [x] 1.2 Make the reactor buildable and seed the version constant — **both deliverables were absorbed by item 1.1** and are verified, not asserted: `mvn -B clean verify` is green on all four CI platforms (Linux/Temurin 17, Linux/21, macOS/21, Windows/21) on run 30301665727, and `<version>0.0.0</version>` in the parent `pom.xml` matches the README `Status-v0.0.0` badge. `version-lockstep` genuinely compares the two now — 1.1 fixed the rendered `version_file` path and anchored `version_regex`, which had left the check silently vacuous. No separate work was manufactured for this item — size: **S** · route: **standard / medium**
- [x] 1.3 Wire JUnit 5 + AssertJ with one passing smoke test under `d4np-core/src/test/java/it/d4np/utils/` (path corrected: ADR-0003 relocated the tree into the `core` module). JUnit BOM 5.12.2 + AssertJ 3.27.3 declared test-scoped on the parent so every module inherits one stack; `maven-surefire-plugin` **pinned to 3.5.6** because an unpinned surefire can be a 2.x build that does not know the JUnit Platform and therefore **silently runs no tests while the build passes**. The smoke test asserts the build contract, not a tautology: package root, class-file major version 61 (proves `--release 17` still pins the baseline, NFR-07), and the runtime floor — size: **S** · route: **standard / medium**
- [x] 1.4 Formatter and linter configs at the reactor root — **Spotless 2.44.5 (google-java-format 1.27.0) and Checkstyle 10.26.1 via plugin 3.6.0**, inherited by every module from the parent POM. Both are declared in `<build><plugins>`, not only `pluginManagement`: Maven resolves a goal prefix from `<build><plugins>` plus the default pluginGroups, and `com.diffplug.spotless` is in neither — a management-only entry reproduces the very `No plugin found for prefix 'spotless'` failure this item exists to fix. **Division of labour is deliberate:** Spotless owns formatting; `config/checkstyle/checkstyle.xml` owns only what a formatter cannot see — naming, import legality, structure, public-API Javadoc — and encodes the spec §1.1 jakarta-only policy as an `IllegalImport` rule that still permits JDK `javax.crypto`/`javax.sql` (FR-12, FR-05 need them). `google_checks.xml` was rejected: it governs formatting Spotless already owns, so the two would fight over the same lines. **ErrorProne + NullAway were split out to 1.11.** **Closed only after the toolchain was run rather than reasoned about:** a local Maven was obtained, and it found four defects that XML-well-formedness checks had all passed over — (a) **`.mvn/jvm.config` supports no comments**, so two explanatory `#` lines made *every* Maven invocation on *every* platform fail with `Could not find or load main class #`, including `mvn -v` — this, not any linter, is what turned all nine jobs of run 30306512701 red; the rationale now lives in `.mvn/README.md`; (b) `checkstyle.xml` named **`IllegalThrow`, which is not a Checkstyle module** (the check is `IllegalThrows`), and the engine rejects the entire configuration rather than skipping an unknown module — all 41 module names were then cross-checked against the 207 `*Check` classes in the 10.26.1 jar, and this was the only invalid one; (c) `spotless:check` **cannot run on `d4np-bom`**, because that module deliberately carries no parent (ADR-001/NFR-09) and Spotless refuses a project that does not declare it — full coordinates fail too (`Spotless plugin absent from the project`), so CI and the docs now pass `-pl '!d4np-bom'`, which is also semantically right since the BOM has no `src` tree; (d) the 1.3 smoke test **was not google-java-format-clean**, since `spotless:apply` had never been runnable; (e) the same trailing-comment trap as (a) had silently disabled **three `.gitignore` patterns** — `target/`, `*.class`, `*.iml` — because `#` only opens a comment at the start of a line, so the first real local build produced nine untracked `target/` directories that the repo was one `git add -A` away from committing. Verified locally end to end: `spotless:check`, `checkstyle:check` across all ten modules, `clean verify` with `Tests run: 3`, `spotless:apply` idempotent, `consistency_lint.py` OK — size: **M** · route: **standard / high**
- [x] 1.5 Stand up the CI matrix (Linux / Windows / macOS on Temurin JDK 17 & 21) with build + test + format + lint. This is what makes every later gate actually run. **The matrix was four cells, not six** — Windows and macOS ran JDK 21 only, so **17, the *published* baseline (NFR-07), was untested on two of the three platforms** while `tier1_platforms` and `README.md` both claimed otherwise; nothing recorded that as a decision, so the profile default had simply gone unexamined. Now six cells. **Changes were made in `orchestrator/project.yaml`, the source of truth, not in the rendered `ci.yml`** — which mattered, because a re-render was proven to *revert item 1.4*: `render.py` reproduced the committed workflow byte-for-byte except for 1.4's Spotless fix, since 1.4 had edited only the rendered artifact and left `commands.format_check` and `ci.extra_jobs` carrying the broken `mvn -B spotless:check checkstyle:check`. Both are now synced, and congruence is *verified* by re-rendering and diffing rather than asserted — `consistency_lint.py` does **not** check `ci.yml` against `project.yaml`, so nothing else would have caught it. Format + lint gained a two-platform `format-lint` job (Linux + Windows; the `build` matrix template emits only build+test, and `.eados-core/` is upstream tooling this project does not edit). **That job earned itself on the first run:** `spotless:check` failed on Windows reporting **all 77 lines** of the 1.3 smoke test as violations, because Spotless reads `lineEndings` from `GIT_ATTRIBUTES`, **this repo had no `.gitattributes`**, and the fallback is the platform's native ending — CRLF on Windows — while `core.autocrlf=input` had written LF. The verdict therefore depended on each contributor's git config rather than on the commit. Fixed at the root with a declared `.gitattributes` (**ADR-0004**, which also takes number 0004 from item 1.10's sketched range — L-0005 again); `git add --renormalize .` produced no changes, so it codifies existing reality. Verified by running both toolchains, not by reasoning: Temurin **17.0.19+10** (the exact build CI resolves) obtained locally, and `clean verify` (`Tests run: 3`), `spotless:check`, `checkstyle:check` all green on **Windows + JDK 17** — the newly added cell — plus `profile_ci_lint.py` and `consistency_lint.py` — size: **M** · route: **standard / high**
- [x] 1.6 Per-module `module-info.java` (JPMS) for all nine modules, with the test module's `--add-opens` requirement documented (ADR-001, spec §1.1). **Eight descriptors, not nine:** `d4np-bom` is `packaging=pom` with no sources, so a descriptor there is impossible rather than merely unnecessary — the lint now *asserts* it has none. **A naming conflict was escalated rather than guessed:** `orchestrator/project.yaml` said modules land at `it.d4np.utils.core, .jdbc, .security` while `AGENTS.md` §5, the ADR-0003 note and the generated README hint all put core's own sources at `it/d4np/utils/`. A JPMS module name is a binary contract that appears in consumers' own descriptors, so the maintainer settled it — **`d4np-core` owns the family root `it.d4np.utils`** — and the manifest note is corrected so the records no longer disagree (**ADR-0005**, which takes number 0005 and pushes item 1.10's range to 0006–0009: L-0005 for the second PR running). **No module can export anything yet, and that is enforced by the compiler, not chosen:** the reactor holds exactly one Java file (the 1.3 smoke test), and `exports` of a package with no class is a compile error — *"package is empty or does not exist"* — because an `exports` clause is not a forward declaration. Two candidate workarounds were measured rather than reasoned about: inventing placeholder types was rejected as manufacturing published API under japicmp's gate, and **`package-info.java` alone was verified NOT to satisfy `exports`** (it emits no class file, so the package stays "empty") — the natural first fix, and it does not work. So each descriptor declares its name and its internal `requires` edges only; `exports`, `java.sql`, Jackson and Spring-as-`requires static` arrive with the code that uses them, the same no-speculation discipline item 1.1 applied to versions. **Two tool findings, both verified:** Spotless *does* parse module declarations (it reformatted all eight), but **Checkstyle 10.26.1 cannot parse one at all** — the pin is effective and a minimal `TreeWalker` + `AvoidStarImport` config on a bare `module m {}` still fails with `no viable alternative at input 'module'`, and because that aborts the whole goal rather than reporting a violation, one descriptor would take the entire lint down; `**/module-info.java` is excluded, costing nothing real since every rule in `checkstyle.xml` governs *type* declarations and a descriptor has no types and no imports. **The graph is now a gate, not a document:** `consistency_lint.py` gains `jpms-congruence`, asserting internal `requires` equals the POM's internal `<dependency>` set, names are unique and under the family root, and the BOM has no descriptor — deriving artifact→module-name from the descriptors themselves so the table is not restated a third time. Proven non-vacuous against five deliberate breakages (missing `requires`, extra `requires`, missing descriptor, out-of-family name, descriptor in the BOM). Verified on Windows + Temurin 17: `clean verify` green, and `--describe-module` on all eight built JARs reproduces spec §3 exactly, with `--add-modules ALL-MODULE-PATH` resolving the whole graph — itself shown non-vacuous, since dropping `d4np-json` yields `FindException: Module it.d4np.utils.json not found, required by it.d4np.utils.spring` — size: **M** · route: **frontier-reasoning / high**
- [x] 1.7 `maven-enforcer` wired with the ADR-001 banned-dependency and convergence rules — this is what makes NFR-08 a build property rather than a review promise. Enforcer **3.6.3**, bound to `validate` (unlike Spotless/Checkstyle, which 1.4 deliberately left unbound: a dependency gate that runs only when invoked *is* the review promise NFR-08 replaces), so all six build cells enforce it. **The policy is per-module and irreducibly so** — core may not see Jackson; `d4np-json` exists in order to see it — so the parent carries only what holds everywhere (`requireJavaVersion [17,)`, `requireMavenVersion [3.3.1,)` tied to the real `.mvn/jvm.config` floor rather than a guess at runner versions, `requireReleaseDeps`, `banDuplicatePomDependencyVersions`, `dependencyConvergence`, plus FR-25's ban on `d4np-test` outside test scope and the javax EE artifacts of NFR-07), and each module carries its own `enforce-adr-001`. **The rule's FORM follows the module's contract (ADR-0006):** default-**deny** allowlists where the contract is a statement about everything — core/jdbc/concurrent/test ("zero third-party", "JDBC API only"), plus security and json with one framework each — because a ban list naming Spring/Jackson/Redisson would have **admitted `commons-lang3` into core**; default-**allow** ban lists for the two framework carriers, since an allowlist there would have to track Spring Boot's and Redisson's transitive closure and would fail for reasons unrelated to policy. **A trap recorded rather than discovered:** Redisson itself depends on Jackson, so banning Jackson family-wide — the naive reading of "Jackson lives only in `d4np-json`" — would break `d4np-lock-redisson` the moment item 5.3 adds Redisson. **The CI command was wrong and measurement proved it:** `mvn enforcer:enforce` runs only the `default-cli` execution and therefore **silently skips every per-module rule** — with a banned `commons-lang3` in core it reported *all rules passed*, while `mvn validate` failed with `commons-lang3 <--- banned via the exclude/include list`; the job now invokes the phase. Verified by a **28-case matrix**, all passing, that injects one dependency into one module at a time and asserts banned-vs-allowed in both directions (Jackson allowed in `json`, banned in `security`; Spring Boot with its full closure allowed in `spring-adapter`, Redisson banned there; Redisson plus Netty/Jackson allowed in `lock-redisson`, Spring banned; `d4np-test` banned at compile scope, allowed at test scope; a *new* test-scope library still allowed, so the rules are not brittle to test-stack upgrades). `consistency_lint.py` gains `module-dependency-policy` so a module cannot ship with no policy while still building green. **Known gaps, stated:** the rules are dormant until M2 adds a real dependency — the matrix is what shows they will not stay dormant-green; `d4np-bom` is unpoliced because it carries no parent by design and declares only `dependencyManagement`, which enforcer does not treat as a dependency; and **this item does NOT turn the `compatibility` job green** — an earlier revision of this line implied it would, which was wrong. That job also runs `japicmp:cmp`, failing with `No plugin found for prefix 'japicmp'` because japicmp is not configured at all: **item 8.1 owns that half** — size: **M** · route: **frontier-reasoning / high**
- [x] 1.8 JMH (`bench/`) and jcstress (`jcstress/`) harness skeletons so the NFR gates have somewhere to land — **and declare the `jmh` and `jcstress` Maven profiles the CI jobs already invoke.** Discovered while closing 1.2: `ci.extra_jobs` runs `mvn -B -Pjmh verify` and `mvn -B -Pjcstress verify`, but neither profile existed, and Maven only *warns* on an unmatched profile (`The requested profile "jmh" could not be activated because it does not exist`), so both jobs reported **pass while verifying nothing** — the same vacuous-green class as item 1.3's unpinned surefire, 1.1's `version-lockstep`, and 1.7's `enforcer:enforce`. **The harness sources are TEST-scope roots, and that is what makes the item cheap (ADR-0007):** `src/bench/java` and `src/jcstress/java` are added by `build-helper` *inside* each profile with `jmh-core` 1.37 and `jcstress-core` 0.16 at `test` scope, so ADR-0006's `*:*:*:*:test` exemption already permits them in the four default-deny modules — a compile-scope harness would have meant editing core's zero-third-party allowlist to admit measurement tooling and shipping `jmh-core` in the consumer-visible graph. **The runners are launched with `exec:exec`, not `exec:java`:** both tools fork JVMs from `java.class.path`, which under `exec:java` is the plugin's isolated classloader, so the fork would start a JVM that cannot see the harness. **Opt-in is per module** (`jmh.skip`/`jcstress.skip`, defaulting to skip, flipped to `false` beside the sources in `d4np-core`) because both tools **exit non-zero on an empty test list** — verified: an opt-in without sources dies in `TestList.getTests` with an `NPE` that names nothing you wrote — so an unconditional binding would fail the eight modules that measure nothing yet. `consistency_lint.py` gains `harness-opt-in`, asserting the parent declares both profiles and that sources ⟺ opt-in in both directions; proven non-vacuous against four deliberate breakages (renamed profile, sources without opt-in, opt-in without sources, a bench file dropped into `d4np-jdbc`). **The skeletons measure real things rather than placeholders, the 1.6 discipline:** the benchmark times a volatile field read against a final field read — the two costs NFR-01's ≤ 2 ns/op budget for `Lazy.get()` is *made of* — and both land at 0.4–0.6 ns/op locally, the first evidence that budget is reachable; the jcstress test asserts that a `volatile` flag orders a plain payload (the FR-03 idiom), with "flag seen, payload not" marked `FORBIDDEN`, and **it fails the build when a forbidden state is observed** — verified by forbidding an outcome the run does produce. **Four findings that only running it could produce:** (a) plugin-level `<configuration>` cannot be used for the runners, because both profiles declare `exec-maven-plugin` and under `-Pjmh,jcstress` the blocks merge — measured: **jcstress ran twice, JMH never ran, and the build passed**; config now lives per `<execution>`; (b) **a non-ASCII character in a jcstress `desc` breaks the generated `META-INF/TestList`** (lengths counted in characters, consumed as bytes), so one em dash killed the run with `NumberFormatException: For input string: ""` — jcstress strings stay ASCII, and this repo's house style makes that trap easy to hit twice; (c) jcstress writes its `.bin.gz` result blob to the **working directory** with no option to relocate it (`-r` is only the HTML report), dropping untracked files next to `pom.xml` until the exec process was pointed at `target/`; (d) **Spotless and Checkstyle were both blind to the new roots** — Spotless's default include set is `src/main` + `src/test`, and Checkstyle sees only *active* compile roots — so the format gate would have been green over unread files and `spotless:apply` could not have formatted them; both are now explicit and both were verified to read the new files. Verified on Windows against the toolchains CI uses, Temurin **17.0.20+8** and **21.0.12+8**: `clean verify` (`Tests run: 3`), `-Pjmh verify` (two benchmarks, `target/jmh-result.json` written), `-Pjcstress verify` (28 results on 17, 14 on 21 — biased locking is gone in 21 — zero failures), `-Pjmh,jcstress verify` together, `spotless:check`, `checkstyle:check`, `validate`, `consistency_lint.py`. **Known gap, stated:** a plain `mvn verify` does not compile the harnesses, so a benchmark that stops compiling is caught by the two profile jobs (Linux/JDK 21) rather than by the six build cells — the price of profile activation, versus paying JMH's code generation in every cell. The rendered `benchmark` job also references `matrix.toolchain` while declaring no matrix, which resolves to JDK 21; it comes from the upstream template, and 21 is the toolchain the job was verified on — size: **S** · route: **standard / medium**
- [x] 1.9 `SECURITY.md` with a coordinated-disclosure policy, and `.github/CODEOWNERS` — **done by the scaffold render**: both artifacts landed with the bootstrap, and CODEOWNERS (`* @danielPoloWork`) now supersedes `ownership.owner` as the live contribution-policy fallback — size: **S** · route: **standard / medium**
- [x] 1.10 Reconcile the ADR record — **settled by indexing the imported four in place and keeping their ids (ADR-0008; the maintainer chose the route on 2026-07-29 after the three options were costed).** By the time the item ran, `docs/adr/README.md` indexed **seven** records while **eleven** decisions had been made: the four Accepted pre-governance decisions (module split, error model, JWT library, generated layout) sit in `.spec/adr/` as `d4np_java_adr_00N_*.md`, with three-digit ids, twelve days older than the docs system and still binding — `ADR-001` is what every module POM and every `module-info.java` cites for the dependency graph. **The blast radius was measured, not estimated, and it decided the route:** `git ls-files` at `4e2bf62` carries **183 references to `ADR-001`…`ADR-004` across 45 files** (124 to `ADR-001` alone; 10 POMs, 8 module descriptors, `orchestrator/project.yaml` 28, this file 22, the imported spec 16, `docs/rfc/0001` 15) plus **~53 path citations** to `.spec/adr/…`, about **30 of them inside already-Accepted ADRs** (0003 cites those paths thirteen times) — and the manifest's `linter:` string literally contains "maven-enforcer for the ADR-001 dependency rules", so renumbering is a manifest change plus a re-render of six generated files, not a find-and-replace. **The decisive argument was history, not cost:** `main` carries `a76ea4c build: enforce the ADR-001 dependency policy per module`, and renumbering would leave that pointing at a dead id that *collides with a live one* — a reader resolving "ADR-001" to `ADR-0001` lands on "Record architecture decisions" and is silently wrong. An id is a stable identifier, not an address. **A recorded plan had to be superseded, and it was already impossible:** ADR-004's closing line — accepted by the owner on 2026-07-26 — promised that "all four normalise to `docs/adr/000N-*.md` when scaffold moves them", but scaffold never moved them and the generated docs system took `0001` and `0002` for its own bootstrap records, so `ADR-001` can never become `ADR-0001`. Only the file-naming note is void; ADR-004's actual decision stands, and the note is left in place rather than edited — an Accepted record is superseded, not rewritten. **L-0005 for the fifth time:** this line previously predicted 0007–0010, then 0006–0009; ADR-0004, 0005, 0006 and 0007 each took a number it had reserved, and the item claims **0008** at the moment of landing instead of predicting anything. **What landed:** two complete indexes in `docs/adr/README.md` (governed four-digit, imported three-digit, twelve rows, with a "carried forward by" column so a reader can navigate between the schemes), a *"Two numbering schemes, and why"* section stating that the digit count is semantic, that `ADR-001 ≠ ADR-0001` and that the three-digit series is **closed**; `.spec/` documented as the **intake area** in `docs/README.md`, whose layout table had never mentioned it; the citation convention in AGENTS.md §7. **The index is now a gate, in both schemes:** `consistency_lint.py`'s `adr-index` gains the imported set's bijection, id-to-filename agreement, sequential three-digit numbering, and **status congruence against the record itself** — which nothing had ever compared, so an ADR marked `Superseded` in its header could have sat in the index as `Accepted` indefinitely. Proven non-vacuous against six deliberate breakages (imported row deleted, governed status cell falsified, imported record's status changed under the index, title id vs filename, a gap in the three-digit series, an index link to a file that does not exist). **Zero references changed** — no POM, descriptor, manifest entry, spec, RFC or Accepted ADR was edited to accommodate the decision, which is the property that justified it. **Two gaps, stated:** `docs/adr/README.md` is a *generated* file whose renderer emits only the 0001/0002 rows, so a re-render still deletes every row added since (risk R-07) — unchanged by this item except that the loss now fails the lint immediately instead of being noticed months later; and nothing mechanical prevents someone issuing `ADR-005` into the closed series, which is held by the convention section alone. **Filed rather than answered:** which *specification* document is authoritative when `.spec/d4np-java.md` and `docs/specs/01_spec_utils.md` disagree is the same two-homes shape but a different document, and is now item **1.12** — size: **M** · route: **frontier-reasoning / high**
- [x] 1.11 Wire ErrorProne + NullAway — **done: ErrorProne 2.50.0 + NullAway 0.13.8 in a profile that self-activates on `<jdk>[21,)</jdk>`, plus `failOnWarning` globally, so AGENTS.md §9/§10's warnings-as-errors promise is enforced instead of described (ADR-0009).** The three findings 1.4 left behind were re-measured, and two of them changed. **(a) A JDK-17-capable ErrorProne does exist**, so the roadmap's "JDK-21-only profile *or* a version built for 17" was a real either/or: reading the class-file majors out of the published `error_prone_core` jars gives 2.31.0 → 55, **2.36.0–2.42.0 → 61**, **2.43.0+ → 65**, so **2.42.0 is the last release that loads on JDK 17** — and 2.42.0 + NullAway 0.13.8 was verified to find the same violations on 17 *with the identical flag set* (both versions also work on JDK 25). It was rejected deliberately, not overlooked: 2.42.0 being the *last* 17-capable release means adopting it freezes static analysis at a 2025 version for as long as NFR-07 keeps 17 as the published baseline, and a two-version split across cells would make "clean" JDK-dependent — a check added after 2.42.0 fires only on the 21 cells, a false positive fixed after it only on the 17 cells. **(b) There are three required javac flags, not two,** each proven by removing it and reading the failure: `-XDcompilePolicy=simple` (*"The default compilation policy (by-todo) is not supported by Error Prone"*), `--should-stop=ifError=FLOW`, and `-XDaddTypeAnnotationsToSymbol=true` (*"required by Error Prone on JDK 21"*). **(c) The `ProvisionException` 1.4 read as a version conflict is an ambiguous wrapper** — it was reproduced here *from the compatible pair* just by omitting `-XepOpt:NullAway:AnnotatedPackages`, whose cause reads *"NullAway configuration is incorrect"*. The conflict is nonetheless real: 2.50.0 + 0.12.7 **with** the option set still dies, but the cause is `NoClassDefFoundError: com/google/errorprone/predicates/type/DescendantOf` — a class removed from ErrorProne after 0.12.7 was built. So 1.4's conclusion stands and its diagnosis was incomplete: **read the cause chain, not the wrapper**, and treat the pair as the unit of upgrade (Dependabot will offer them separately). **Two knobs earned their place by measurement:** `-XepExcludedPaths` for generated sources is **required, not prophylactic** — without it ErrorProne reports 12 `ThreadPriorityCheck` findings in item 1.8's **JMH-generated** code and, warnings being errors, `default-testCompile` fails; and `.mvn/jvm.config` is **load-bearing for ErrorProne**, which 1.4's README only anticipated — with the file moved away, compilation dies with `IllegalAccessError … module jdk.compiler does not export com.sun.tools.javac.api`. The existing eight exports plus two opens suffice, so no flag was added. **Nothing leaks into the dependency graph:** `annotationProcessorPaths` resolve outside project dependencies, so `dependency:tree` on `d4np-core` is still nothing but test-scoped JUnit and AssertJ, NullAway's own JSpecify/Guava never reach a consumer, and ADR-0006's default-deny allowlists needed no edit. **No nullability annotation artifact was added** — core has no production types yet and the core-contracts RFC states its contracts in Javadoc, so the annotation choice arrives with the first nullable member (item 2.1), the same no-speculation discipline as 1.6 and 1.8. `consistency_lint.py` gains **`static-analysis-wired`**, asserting both directions of manifest-claim ↔ POM-config, NullAway at `ERROR`, `AnnotatedPackages` present, and `failOnWarning` on — but deliberately *not* the javac flags or the generated-sources exclusion, because dropping those fails the build by name while a severity demotion turns it green silently. Proven against six deliberate breakages; **its own first draft was vacuous** and is recorded as such — it matched `AnnotatedPackages` inside the POM *comment explaining the option*, so POM text is now matched with comments stripped. Verified on Windows against Temurin **21.0.12+8** and **17.0.20+8**: probe violations (a NullAway null-pass and an ErrorProne `SelfAssignment`) **fail the build on 21 and compile on 17**, which is the gap made visible; `clean verify`, `test`, `spotless:check`, `checkstyle:check`, `validate`, `-Pjmh,jcstress verify` (both harnesses *and* both annotation processors — 1.8's `combine.children="append"` merge held) and `consistency_lint.py` all green. **Known gap, stated:** the three JDK-17 cells and any contributor on 17 run no static analysis; nothing ships unanalyzed, since the three JDK-21 cells and the `quality` job analyse the same sources on every PR — whose title (`spotless + errorprone + checkstyle`) stops being aspirational, the jacoco/pit halves remaining item 8.2 — size: **M** · route: **frontier-reasoning / high**
- [x] 1.12 Settle which **specification** document is authoritative — **settled: the manifest's `spec.*` block is the source of record, `docs/specs/01_spec_utils.md` is its authoritative published view, an RFC outranks both where it pins a contract, and `.spec/d4np-java.md` is superseded provenance (ADR-0010).** Filed by item 1.10, which fixed the ADR half of the same two-homes problem and left this half open. **Three measurements collapsed the choice this item was framed to make.** (1) **The imported draft cannot be the id source — it contains zero `FR-` ids**, and only `NFR-01`…`NFR-06` (its §6 performance table). The `FR-01`…`FR-25` / `NFR-01`…`NFR-12` vocabulary that this file, the POMs, the threat model and the compliance register all cite was **minted during Phase-5 intake**, not imported; the draft numbers its requirements as *items 1–25*, and `item 17` ≡ `FR-17` by coincidence of numbering, which is why nobody noticed. So the roadmap's own Traceability line named a document that holds none of the ids it claimed to source — **corrected in this PR**. (2) The manifest and the published spec **already agree exactly**: 25 FR + 12 NFR each, sets equal both ways, all 25 requirement texts equal character for character. (3) The “frozen contract” is a **generated file** — byte-identical to a fresh `render.py` run — so it cannot be the source: a hand-edit there is reverted by the next re-render, which is risk R-07 and already cost item 1.4's work once. **Hence the branch the item offered second (keep the draft authoritative and back-port the core-contracts RFC into it) was rejected on evidence, not taste:** back-porting would first require *minting* the FR vocabulary into an archived 2026-07-14 document and then maintaining two normative specs whose shapes differ (nine sections versus six), with the authoritative one living outside `docs/`. **What landed:** a `SUPERSEDED` banner on the draft naming its successor and stating that it holds no `FR-` ids (body untouched below it — a narrow, additive exception to ADR-0008's “do not edit the archive”, justified because a reader arriving by search has no other signal); the five `[GAP] no contract stated` requirement lines that the core-contracts RFC has in fact pinned now say so **by pointer, not by copy**, since duplicating that RFC's tables into the manifest would create the very second home this item removes; and the manifest edit was propagated by **re-rendering** the spec rather than hand-editing it, verified by diffing the regenerated file. `consistency_lint.py` gains **`spec-authority`**, asserting manifest ↔ published-spec id-set *and* text equality in both directions, that every FR/NFR id cited anywhere in this file exists in the manifest (so a typo one past the end of the set fails the build — including in this very line, which the check caught while it was being written), and that the draft keeps its banner and never regrows `FR-` ids. Proven against five deliberate breakages (id added to the generated spec, requirement text hand-edited there, a dangling id in the roadmap, the banner removed, `FR-` ids re-introduced into the archive). **Known cost, stated:** the authoritative spec is a rendered artifact, which is inherent to an EADOS-generated repository and the same trade ADR-0003 recorded for the source layout — mitigated because the render is now *verified* rather than assumed. The remaining `[GAP]` markers (FR-06 transaction semantics, FR-12 AAD, NFR-11 CVSS threshold, NFR-12 provenance) are real and stay owned by later RFCs — size: **M** · route: **frontier-reasoning / high**

## Milestone 2 — core foundations

**Goal:** the zero-dependency core — creational and structural patterns plus the ADR-002 error model.
**Covered by RFC-0001** (`docs/rfc/0001-core-contracts.md`), which pins the nullability, error and
thread-safety contract for all nine types before any of them is written.

Note on routing: RFC-0001 already made these design decisions, so no M2 item carries
`decision-heavy`. That drop in route cost is the return on contract-first ordering, and it is the
argument for items 3.0–7.0 doing the same for their milestones.

- [ ] 2.1 `Result<T>` sealed `Ok`/`Err`, `ErrorDetail(String code, String message, Throwable cause)`, unchecked `BusinessException` (RFC-0001; FR-17, FR-18, ADR-002) — the shared error vocabulary every later module depends on — size: **M** · route: **frontier-reasoning / high**
- [ ] 2.2 `Lazy<T>` with the jcstress harness proving safe publication; default failure policy **retry**, `memoizingFailures()` opt-in, re-entrant initializer throws (RFC-0001; FR-03, NFR-01) — size: **M** · route: **frontier-reasoning / high**
- [ ] 2.3 `StrategyRegistry<K,S>` with the `find`/`getOrThrow` contract (RFC-0001; FR-04, NFR-04) — size: **S** · route: **standard / high**
- [ ] 2.4 `GenericFactory<T,K>` (duplicate keys rejected, explicit `replace()`) and `FluentBuilder<T>` (template method, accumulated validation, repeatable `build()`) (RFC-0001; FR-01, FR-02) — size: **M** · route: **standard / high**
- [ ] 2.5 `StringCaseConverter` (`Locale.ROOT`, the pinned tokenizer table), `ObjectUtils` (only what `java.util.Objects` lacks), `ResourceLoaderUtils` (`Class<?>` anchor resolution) (RFC-0001; FR-22, FR-23, FR-24) — size: **M** · route: **standard / medium**

## Milestone 3 — core cross-cutting

**Goal:** validation, metrics, and an audit trail that cannot leak secrets.

- [ ] 3.0 **RFC-0002 — cross-cutting core contracts.** Must settle the FR-16 `AuditLog` redaction policy before any code: as specified, `AuditLog` faithfully records secrets and PII into a store typically retained longer and replicated wider than application logs. Needs field-level allowlisting, a `@Sensitive` opt-out, and an explicit never-capture list. Also covers the `Validator` wrapper surface and the metrics-aspect fallback contract. Blocks 3.1–3.3. — size: **M** · route: **frontier-reasoning / high**
- [ ] 3.1 `Validator` over Jakarta Bean Validation 3.x (RFC-0002; FR-14) — size: **S** · route: **standard / medium**
- [ ] 3.2 `ExecutionTimeMetricAspect` with Micrometer-or-log fallback; the AspectJ/Spring binding stays in the adapter (RFC-0002; FR-15) — size: **S** · route: **standard / medium**
- [ ] 3.3 `AuditLog` with a field-level redaction allowlist and `@Sensitive` opt-out (RFC-0002; FR-16) — size: **M** · route: **frontier-reasoning / high**

## Milestone 4 — json and jdbc

**Goal:** persistence and serialization capability modules, each owning its own third-party surface.

- [ ] 4.0 **RFC-0003 — jdbc transaction semantics and json hardening.** Must settle FR-06 (`JdbcTxRunner`: isolation levels, nesting/suspension, savepoints, and which exception types trigger rollback — all currently undefined) and FR-21 (`ObjectMapperExtensions`: operation list, null-vs-absent semantics, collection/generic handling). Blocks 4.2 and 4.4. — size: **M** · route: **frontier-reasoning / high**
- [ ] 4.1 `JsonMapper` hardened configuration — JavaTimeModule, `FAIL_ON_UNKNOWN_PROPERTIES=false`, **no default typing** (the polymorphic-deserialization CVE class configured away) (FR-20) — size: **S** · route: **frontier-reasoning / high**
- [ ] 4.2 `ObjectMapperExtensions` once RFC-0003 specifies the operation set (RFC-0003; FR-21) — size: **S** · route: **standard / medium**
- [ ] 4.3 `SimpleJdbcExecutor` — parameterized statements only, by construction; no string-concatenation overload exists (FR-05, NFR-03) — size: **M** · route: **frontier-reasoning / high**
- [ ] 4.4 `JdbcTxRunner` once RFC-0003 specifies isolation and rollback semantics (RFC-0003; FR-06) — size: **M** · route: **standard / high**
- [ ] 4.5 `PageRequest`/`PageResponse<T>` with whitelist-validated sorting (FR-07) — size: **S** · route: **standard / high**

## Milestone 5 — concurrent

**Goal:** pooling and async execution with the concurrency claims proven, not asserted. Spec §1
rule: *a thread-safety claim without a named jcstress test is not a claim.*

- [ ] 5.0 **RFC-0004 — concurrency contracts.** Pool lifecycle and rejection semantics, MDC propagation guarantees across `CompletableFuture` boundaries, and the `DistributedLock` lease/reentrancy contract every implementation must honour. Blocks 5.1–5.3. — size: **M** · route: **frontier-reasoning / high**
- [ ] 5.1 `CustomThreadPoolFactory` plus the rejection/shutdown jcstress harness; graceful shutdown drains within the configured timeout (RFC-0004; FR-08, NFR-05) — size: **M** · route: **standard / high**
- [ ] 5.2 `AsyncExecutor` with MDC propagation and the submission-overhead benchmark (RFC-0004; FR-09, NFR-02) — size: **M** · route: **standard / high**
- [ ] 5.3 `DistributedLock` — interface only in this module, lease time mandatory, no reentrancy promise (RFC-0004; FR-10, ADR-001). The interface *is* the deliverable: it constrains every future implementation — size: **S** · route: **frontier-reasoning / high**

## Milestone 6 — security

**Goal:** JWT and cryptography under the hardened profiles the ADRs pin, with negative tests as
first-class deliverables.

- [ ] 6.0 **RFC-0005 — security module contracts.** Closes the FR-12 gaps (no AAD support; no per-key message cap driving the rotation trigger) and the NFR-11 gap (no CVSS failing threshold and no suppression-file policy, so the scan is a report rather than a gate). Also decides whether `OutputEncoder` may take a compile dependency on OWASP Java Encoder — a new third-party surface in `d4np-security`, legal under ADR-001 but a decision, not a default. Blocks 6.2–6.4. — size: **M** · route: **frontier-reasoning / high**
- [ ] 6.1 `JwtTokenProvider` on Nimbus with the ADR-003 hardened profile — per-key algorithm allowlist, `alg=none` structurally impossible, mandatory `exp` with configurable skew, JWKS caching — plus negative tests (alg-confusion, alg=none, expired, wrong-audience) and RFC 7515/7519 vectors (ADR-003; FR-11) — size: **L** · route: **frontier-reasoning / high**
- [ ] 6.2 `AesEncryptor` AES-256-GCM with the rotation envelope, AAD, and a per-key message cap; IV-uniqueness property test over 10⁷ operations (RFC-0005; FR-12, NFR-06) — size: **L** · route: **frontier-reasoning / high**
- [ ] 6.3 `OutputEncoder` delegating to OWASP Java Encoder rather than reimplementing context-aware escaping (RFC-0005; FR-13) — size: **S** · route: **frontier-reasoning / high**
- [ ] 6.4 OWASP Dependency-Check CVSS threshold and suppression policy turned into a real gate (RFC-0005; NFR-11). **Note the plugin is not declared at all yet** — the `security / owasp dependency-check` CI job fails with `No plugin found for prefix 'dependency-check'`, so this item must first *introduce* the plugin and only then tighten it into a gate — size: **S** · route: **frontier-reasoning / high**

## Milestone 7 — adapters and test support

**Goal:** confine host coupling to the edge; ship the test-scoped helpers without letting production
code reach them.

- [ ] 7.0 **RFC-0006 — adapter boundary contracts.** The normative RFC 7807 exception→status mapping, the AOP/DI binding surface, the Redisson lock's reentrancy and lease behaviour against the FR-10 interface, and the test module's `--add-opens` contract. Blocks 7.1–7.4. — size: **M** · route: **frontier-reasoning / high**
- [ ] 7.1 `spring-adapter`: `GlobalExceptionHandler` with the RFC 7807 mapping table — `BusinessException`→422, validation→400, `StrategyNotFoundException`→500 + alert, fallback→500 (RFC-0006; FR-19) — size: **M** · route: **standard / high**
- [ ] 7.2 `spring-adapter`: AOP/DI binding for the metrics aspect (RFC-0006; FR-15 binding) — size: **S** · route: **standard / medium**
- [ ] 7.3 `lock-redisson`: Redisson `DistributedLock` implementation with container-based integration tests (RFC-0006; FR-10 implementation) — size: **M** · route: **standard / high**
- [ ] 7.4 `test` module: `ReflectionUtils` with the `--add-opens` contract and the CI check that fails production code importing the test module (RFC-0006; FR-25) — size: **M** · route: **standard / high**

## Milestone 8 — release engineering

**Goal:** make the compatibility and supply-chain contracts enforceable, then publish **1.0.0**.
This is the milestone where `0.x` becomes `1.0.0` and binary compatibility starts being promised.

- [ ] 8.1 `japicmp` per-module binary-compatibility gate against the previous release (NFR-09) — size: **M** · route: **frontier-reasoning / high**
- [ ] 8.2 JaCoCo line coverage ≥ 85% and PIT mutation ≥ 60% enforced on `core` and `security` (NFR-10) — **and declare the `coverage` and `mutation` Maven profiles.** The quality job already runs `mvn -B verify -Pcoverage` and `mvn -B -Pmutation test`; neither profile exists, and Maven only warns on an unmatched profile, so those two steps pass having measured nothing — the same vacuous-green class recorded against 1.8. After item 1.4 the job is green on `spotless:check` + `checkstyle:check` for real, and on the other two steps only nominally — size: **S** · route: **standard / medium**
- [ ] 8.3 Perf gates pinned to a stable runner **or** compared against a stored per-runner baseline, replacing the absolute NFR-01/NFR-06 numbers in CI. This changes how a stated NFR is enforced, so it is a design decision, not plumbing: NFR-01's 2 ns/op sits near JMH's noise floor and hosted runners vary by more than 10%. Until this lands, the Milestone 2 perf numbers are tracked on the reference machine and **advisory in CI** — size: **M** · route: **frontier-reasoning / max**
- [ ] 8.4 GPG-signed Maven Central publication via Sonatype, sources + javadoc JARs, reproducible-build plugin (NFR-12) — size: **L** · route: **frontier-reasoning / high**
- [ ] 8.5 Build provenance/attestation (SLSA or Sigstore) and pinned CI action SHAs (NFR-12 gap) — size: **M** · route: **frontier-reasoning / high**
- [ ] 8.6 Threat model under `docs/security/` via `/eados audit` (STRIDE) — size: **M** · route: **frontier-reasoning / high**
