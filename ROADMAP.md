# ROADMAP — egl-utils-java

The negotiated milestone roadmap. Authored by the **producer** in the `plan` phase
(`.eados-core/orchestrator/commands/plan.md`) from the approved RFCs recorded in
`orchestrator/project.yaml` → `delivery_state.refs.rfcs`. The `roadmap-covers-rfcs` gate
(`traceability.py`) reads the `## Milestone N` sections below; `plan → scaffold` is illegal until
every recorded RFC is addressed by at least one milestone.

**Every item carries a size and a route.** Size is a macroscopic T-shirt estimate
(`plan.yaml sizing_scale: XS S M L XL`) — not story points. Route is the advisory model/effort
recommendation from the `os/routing` policy (ADR-0017), stated as **tier / effort**, never a model
name — names rot, the dated `catalog:` owns them. Routes below were resolved with
`route_advice.py`, not asserted; the signals that earned each one are listed under
[Routing basis](#routing-basis). **The route is advisory: the human keeps final model authority.**

**Latest checkpoint:** [2026-08-30 — RFC-0005, and a mandate the roadmap did not list (item 6.0)](docs/journal/2026/08/2026-08-30-rfc-0005-security-contracts.md).
The journal index is [`docs/journal/`](docs/journal/README.md). This pointer is step 3 of the
checkpoint protocol and had no target in this file until item 2.1 created the first checkpoint.
Items 3.1 and 3.2 wrote their checkpoint files and skipped steps 2 and 3; item 3.3 back-filled both
index rows and this pointer, because an index with holes is worth less than no index.

> **Note on section order.** The context sections come first and the milestones last, deliberately.
> `traceability.parse_milestones` slices each `## Milestone N` section up to the *next* one, so
> anything placed after the last milestone is absorbed into its body — an appendix that merely
> *mentions* an RFC id would register as a coverage edge from Milestone 8. Keeping the milestones
> last means the RFC → milestone edges the gate derives are exactly the real ones.

## Negotiation inputs — stated, because they shape everything below

| Input | Value | Where it comes from |
|---|---|---|
| Approved RFCs | **RFC-0001** only | `delivery_state.refs.rfcs` |
| Capacity | **one person** — the owner | `ownership.owner`/`maintainer`/`author` all = Daniel Polo; `governance` notes reviewers "stay deferred until there is a second collaborator" |
| Dates | **none, deliberately** | `governance.version_start: "pre-1.0 milestone-driven"`. This project's own release policy is milestone-driven, not calendar-driven, so this roadmap sequences work and does **not** invent dates |
| Ordering | the ADR-001 dependency DAG | core first, then capability leaves, then adapters, then release |
| Milestone vocabulary | `semver` | `domains/software.yaml milestone_vocabulary` |

**Scope is the owner's call.** The producer reconciles and sequences; cuts and additions are a human
decision (`plan.md` Boundary). Nothing below is committed scope until the owner says so.

## The design debt this roadmap makes visible

The negotiation protocol is explicit: *"Each item references an approved RFC — no roadmap item
without a design behind it."* Measured against that rule, **one milestone of eight is covered**:

| Milestone | RFC behind it | Status |
|---|---|---|
| M1 repository foundations | ADR-001 + ADR-004 (both Accepted) — no RFC | structural decisions already settled by ADR; see note below |
| **M2 core foundations** | **RFC-0001** | **covered** |
| M3 core cross-cutting | — | **needs an RFC** (item 3.0) |
| M4 json and jdbc | — | **needs an RFC** (item 4.0) |
| M5 concurrent | — | **needs an RFC** (item 5.0) |
| M6 security | ADR-003 covers the JWT library choice only | **needs an RFC** (item 6.0) |
| M7 adapters and test support | — | **needs an RFC** (item 7.0) |
| M8 release engineering | — | see 8.0 note |

So this roadmap schedules **five more RFCs** as first-class items (3.0, 4.0, 5.0, 6.0, 7.0). That is
not padding: `workflow.yaml` defines `plan → design` as a legal, non-human-gated transition precisely
so a new RFC can re-open planning without restarting the project. Each of those items is a return to
the `design` phase, and the milestone's implementation items are blocked on it.

**M1 is the deliberate exception.** Its content is structural and already decided by two Accepted
ADRs (ADR-001 module split, ADR-004 generated layout). Writing an RFC to re-decide an accepted ADR
adds ceremony, not a decision. M1 items therefore cite the ADR that governs them.

**M8 carries no RFC item.** Its work is CI and release plumbing implementing NFR-09 through NFR-12,
which the specification already states normatively. Item 8.3 is the exception — it changes a stated
NFR's enforcement model — and is flagged `decision-heavy` accordingly.

## Sequencing

No dates — the project is milestone-driven pre-1.0 (`governance.version_start`). This is the
dependency order, which is what actually constrains the work:

```
M1 ─────────────────────────────────────────► everything (ADR-004: no code before the reactor)
 └─ 1.1 blocks 1.2–1.9 and all of M2

M2 ──► M3, M4, M5              [Result/ErrorDetail is the shared error vocabulary]
 │
 ├─ M3 ──┐
 ├─ M4 ──┤
 └─ M5 ──┴──► M7               [adapters bind what the modules expose]
              M6 ─┘            [independent of M3/M4; needs only M2]

M6 + M7 ──► M8                 [japicmp needs a published surface; 8.6 audits the finished one]
```

M3, M4, M5 and M6 are mutually independent once M2 lands — with capacity of one they are sequential
in practice, and the order among them is the owner's priority call, not a technical constraint.
**8.3 is the exception to that ordering:** it can be pulled forward at any time after 1.8, and doing
so would stop the M2 perf numbers from sitting in an advisory limbo for the whole project.

## Routing basis

Routes were resolved with `route_advice.py` against the `os/routing` policy, not asserted. The
signal sets and their verdicts:

| Signals | Verdict | Applied to |
|---|---|---|
| `severity:high` + flag `decision-heavy` | **frontier-reasoning / max** | 1.1, 8.3 — a wrong foundational call is the most expensive artifact here |
| `severity:high` + flag `sets-pattern` | **frontier-reasoning / high** | 1.6, 1.7, 2.1, 2.2, 8.1 — first of its class; every follower copies the template |
| `security` + `severity:high` | **frontier-reasoning / high** | 3.3, 4.1, 4.3, 6.1–6.4, 8.4, 8.5, 8.6 — on security posture a subtle miss dwarfs the routing saving |
| `adr` | **frontier-reasoning / high** | 1.10, 3.0, 4.0, 5.0, 6.0, 7.0, 5.3 — an RFC-, ADR- or interface-defining item is decision-heavy by definition |
| `severity:high` | **standard / high** | 1.4, 1.5, 2.3, 2.4, 4.4, 4.5, 5.1, 5.2, 7.1, 7.3, 7.4 |
| `severity:medium` | **standard / medium** | 1.2, 1.3, 1.8, 1.9, 2.5, 3.1, 3.2, 4.2, 7.2, 8.2 |

Two judgment calls, stated because the tool cannot make them: **4.5 `PageRequest` was not labelled
`security`** even though its sort whitelist is an injection defence — its primary job is pagination,
and labelling every item containing one validation as `security` would route the whole roadmap to the
top tier and make the signal meaningless. Conversely **4.1 `JsonMapper` was**, despite being a
three-flag configuration change: getting default typing wrong is a known CVE class, so the
consequence — not the size — earns the route.

At run time, `route_advice.py --check --current-model <id>` compares a step's route against the
session model and prints `ROUTE-OK` / `ROUTE-MISMATCH`. It never switches the model; model authority
is the human's (ADR-0017).

## Traceability

- **RFC → milestone:** the only approved RFC is addressed by Milestone 2 (items 2.1–2.5). Verified by
  `python .eados-core/tools/traceability.py ROADMAP.md RFC-0001`.
- **Recorded refs:** `orchestrator/project.yaml` → `delivery_state.refs`.
- **Spec → item:** every item cites its FR/NFR id from the manifest's `spec.functional_reqs` /
  `spec.nonfunctional_reqs`, published as [`docs/specs/01_spec_utils.md`](docs/specs/01_spec_utils.md).
  **Corrected by item 1.12:** this line used to name `.spec/d4np-java.md` §2/§6 as the id source, and
  that document contains **no `FR-` ids at all** (0 FR, 6 NFR — the vocabulary was minted during
  intake). It is superseded provenance; the precedence ladder is
  [ADR-0010](docs/adr/0010-single-specification-authority.md), and `spec-authority` in
  `consistency_lint.py` now enforces it — including that no item here cites an id no specification
  states.
- **Gate:** `roadmap-covers-rfcs` must pass before `plan → scaffold` is legal.

## Spec Coverage Map

Tracks which spec section is fulfilled by which roadmap item(s). Every spec section has a row with at
least one fulfilling item and a status glyph. Legend: ⏳ not started · 🚧 in progress · ✅ done ·
❎ N/A. Sections follow the six-section shape of `docs/specs/01_spec_util.md`. Read with the
`check_spec_map` lint: it requires a non-empty items cell and a recognised glyph per row.

Placed **before** the milestone sections on purpose — `traceability.parse_milestones` would otherwise
absorb this table into Milestone 8's body, the same appendix-bleed that produced a false
`RFC-0001 → M8` edge in the first draft.

| Spec § | Requirement | Roadmap items | Status |
|--------|-------------|---------------|--------|
| §1 | Objective & business context | 1.1, 1.7 | ✅ |
| §2 | Functional requirements | 2.1, 2.2, 2.3, 2.4, 2.5, 3.1, 3.2, 3.3, 4.1, 4.2, 4.3, 4.4, 4.5, 5.1, 5.2, 5.3, 6.1, 6.2, 6.3, 7.1, 7.2, 7.3, 7.4 | 🚧 |
| §3 | Non-functional requirements | 1.5, 1.7, 1.8, 2.2, 2.3, **4.3**, 5.1, 5.2, 6.2, 6.4, 8.1, 8.2, 8.3, 8.4, 8.5, **8.8** | 🚧 |
| §4 | Logical architecture | 1.1, 1.6, 1.7 | ✅ |
| §5 | Public interface | 2.1, 2.2, 2.3, 2.4, 2.5, 3.0, 4.0, **5.0**, 5.3, 8.1 | 🚧 |
| §6 | Verification & test strategy | 1.3, 1.5, 1.8, 2.1, 2.2, 5.1, 6.1, 8.2, 8.3, 8.6 | 🚧 |

Two rows worth reading closely. **§1** maps to the reactor and the enforcer rather than to a feature:
the objective is *framework independence*, and 1.1 + 1.7 are what make it a build property instead of
a claim (NFR-08). **§3** is the widest row because the NFRs are enforcement, not code — most of them
land in M1 and M8 rather than in the milestone that implements the thing being measured.

> **§3's items cell corrected in item 4.3.** The row listed fourteen items and not **4.3**, although
> 4.3 is the item that fulfils **NFR-03** — the row's own subject. The omission is the same shape the
> note below records for the glyphs: `check_spec_map` asserts a non-empty cell and a recognised glyph,
> never that the cell is *complete*, so an under-reporting row stays green forever. **8.8** joins it in
> the same edit, because it is the item that turns NFR-03 from a measured number into an enforced one —
> which is precisely what this row means by "the NFRs are enforcement, not code".
>
> **Glyphs corrected in item 2.1.** All six rows still read ⏳ after Milestone 1 shipped, although
> every item behind §1 and §4 had closed and three behind §3 and §6 had. The `check_spec_map` lint
> asserts a *recognised* glyph and a non-empty items cell, not that the glyph agrees with the
> checkboxes — so an under-reporting row stays green. §2 and §5 move to 🚧 with this item, and 2.1
> joins §6, whose rule ("a thread-safety claim without a named jcstress test is not a claim") it
> satisfies with `ImmutablePublicationStress`.

> **§5's items cell corrected in item 4.0.** It listed only implementation items, although §5 is the
> *public interface* section and its own `[GAP]` line — "only 5 of ~25 public types carry a
> nullability/error/thread-safety contract row" — is what the RFC items exist to close. **3.0 and 4.0
> are added together**: RFC-0002 pinned three types' contract rows and RFC-0003 pins six, so listing
> one without the other would have been a fresh hole rather than a fix. Same class as the glyph
> correction above, and same reason it survived: `check_spec_map` requires a non-empty items cell, not
> a complete one.

---

## Milestone 1 — Project bootstrap & CI

> **Released as v0.1.0** (2026-07-29) — release notes
> [`docs/releases/v0.1.0.md`](docs/releases/v0.1.0.md), entries
> [`docs/changelog/v0/v0.1.0.md`](docs/changelog/v0/v0.1.0.md). All twelve items closed.

**Goal:** the thinnest slice that compiles, tests and ships under the full quality bar — which for
this project means the ADR-001 Maven reactor exists first, then the build, tests, quality configs and
CI matrix stand up on top of it. **Hard prerequisite for every later milestone**: no component code
lands before the reactor exists (ADR-004).

> Items 1.2–1.5 are the factory's universal day-zero bootstrap (`templates/ROADMAP.md.tmpl`); 1.1 and
> 1.6–1.9 come from `spec.milestone1_items`. The scaffold render and the plan-phase negotiation were
> merged here rather than one replacing the other — the render carried five bootstrap items the
> negotiation lacked, and the negotiation carried the sizes and routes the render has no concept of.
> Title taken from the render so it stays congruent with the generated README milestone table.

- [x] 1.1 Maven reactor skeleton — parent POM, the nine modules of ADR-001, and the BOM; relocate the rendered tree to become the `core` module's source root, updating every generated `src_main`/`src_test`/`src_bench` reference **and** the `consistency_lint.py` path assertions in the same item (ADR-004). Blocks 1.2–1.9 and all of M2. — size: **L** · route: **frontier-reasoning / max**
- [x] 1.2 Make the reactor buildable and seed the version constant — **both deliverables were absorbed by item 1.1** and are verified, not asserted: `mvn -B clean verify` is green on all four CI platforms (Linux/Temurin 17, Linux/21, macOS/21, Windows/21) on run 30301665727, and `<version>0.0.0</version>` in the parent `pom.xml` matched the README `Status-v0.0.0` badge (both moved to `0.1.0` when M1 shipped; the check compares them, not a fixed value). `version-lockstep` genuinely compares the two now — 1.1 fixed the rendered `version_file` path and anchored `version_regex`, which had left the check silently vacuous. No separate work was manufactured for this item — size: **S** · route: **standard / medium**
- [x] 1.3 Wire JUnit 5 + AssertJ with one passing smoke test under `d4np-core/src/test/java/it/d4np/utils/` (path corrected: ADR-0003 relocated the tree into the `core` module). JUnit BOM 5.12.2 + AssertJ 3.27.3 declared test-scoped on the parent so every module inherits one stack; `maven-surefire-plugin` **pinned to 3.5.6** because an unpinned surefire can be a 2.x build that does not know the JUnit Platform and therefore **silently runs no tests while the build passes**. The smoke test asserts the build contract, not a tautology: package root, class-file major version 61 (proves `--release 17` still pins the baseline, NFR-07), and the runtime floor — size: **S** · route: **standard / medium**
- [x] 1.4 Formatter and linter configs at the reactor root — **Spotless 2.44.5 (google-java-format 1.27.0) and Checkstyle 10.26.1 via plugin 3.6.0**, inherited by every module from the parent POM. Both are declared in `<build><plugins>`, not only `pluginManagement`: Maven resolves a goal prefix from `<build><plugins>` plus the default pluginGroups, and `com.diffplug.spotless` is in neither — a management-only entry reproduces the very `No plugin found for prefix 'spotless'` failure this item exists to fix. **Division of labour is deliberate:** Spotless owns formatting; `config/checkstyle/checkstyle.xml` owns only what a formatter cannot see — naming, import legality, structure, public-API Javadoc — and encodes the spec §1.1 jakarta-only policy as an `IllegalImport` rule that still permits JDK `javax.crypto`/`javax.sql` (FR-12, FR-05 need them). `google_checks.xml` was rejected: it governs formatting Spotless already owns, so the two would fight over the same lines. **ErrorProne + NullAway were split out to 1.11.** **Closed only after the toolchain was run rather than reasoned about:** a local Maven was obtained, and it found four defects that XML-well-formedness checks had all passed over — (a) **`.mvn/jvm.config` supports no comments**, so two explanatory `#` lines made *every* Maven invocation on *every* platform fail with `Could not find or load main class #`, including `mvn -v` — this, not any linter, is what turned all nine jobs of run 30306512701 red; the rationale now lives in `.mvn/README.md`; (b) `checkstyle.xml` named **`IllegalThrow`, which is not a Checkstyle module** (the check is `IllegalThrows`), and the engine rejects the entire configuration rather than skipping an unknown module — all 41 module names were then cross-checked against the 207 `*Check` classes in the 10.26.1 jar, and this was the only invalid one; (c) `spotless:check` **cannot run on `d4np-bom`**, because that module deliberately carries no parent (ADR-001/NFR-09) and Spotless refuses a project that does not declare it — full coordinates fail too (`Spotless plugin absent from the project`), so CI and the docs now pass `-pl '!d4np-bom'`, which is also semantically right since the BOM has no `src` tree; (d) the 1.3 smoke test **was not google-java-format-clean**, since `spotless:apply` had never been runnable; (e) the same trailing-comment trap as (a) had silently disabled **three `.gitignore` patterns** — `target/`, `*.class`, `*.iml` — because `#` only opens a comment at the start of a line, so the first real local build produced nine untracked `target/` directories that the repo was one `git add -A` away from committing. Verified locally end to end: `spotless:check`, `checkstyle:check` across all ten modules, `clean verify` with `Tests run: 3`, `spotless:apply` idempotent, `consistency_lint.py` OK — size: **M** · route: **standard / high**
- [x] 1.5 Stand up the CI matrix (Linux / Windows / macOS on Temurin JDK 17 & 21) with build + test + format + lint. This is what makes every later gate actually run. **The matrix was four cells, not six** — Windows and macOS ran JDK 21 only, so **17, the *published* baseline (NFR-07), was untested on two of the three platforms** while `tier1_platforms` and `README.md` both claimed otherwise; nothing recorded that as a decision, so the profile default had simply gone unexamined. Now six cells. **Changes were made in `orchestrator/project.yaml`, the source of truth, not in the rendered `ci.yml`** — which mattered, because a re-render was proven to *revert item 1.4*: `render.py` reproduced the committed workflow byte-for-byte except for 1.4's Spotless fix, since 1.4 had edited only the rendered artifact and left `commands.format_check` and `ci.extra_jobs` carrying the broken `mvn -B spotless:check checkstyle:check`. Both are now synced, and congruence is *verified* by re-rendering and diffing rather than asserted — `consistency_lint.py` does **not** check `ci.yml` against `project.yaml`, so nothing else would have caught it. Format + lint gained a two-platform `format-lint` job (Linux + Windows; the `build` matrix template emits only build+test, and `.eados-core/` is upstream tooling this project does not edit). **That job earned itself on the first run:** `spotless:check` failed on Windows reporting **all 77 lines** of the 1.3 smoke test as violations, because Spotless reads `lineEndings` from `GIT_ATTRIBUTES`, **this repo had no `.gitattributes`**, and the fallback is the platform's native ending — CRLF on Windows — while `core.autocrlf=input` had written LF. The verdict therefore depended on each contributor's git config rather than on the commit. Fixed at the root with a declared `.gitattributes` (**ADR-0004**, which also takes number 0004 from item 1.10's sketched range — L-0005 again); `git add --renormalize .` produced no changes, so it codifies existing reality. Verified by running both toolchains, not by reasoning: Temurin **17.0.19+10** (the exact build CI resolves) obtained locally, and `clean verify` (`Tests run: 3`), `spotless:check`, `checkstyle:check` all green on **Windows + JDK 17** — the newly added cell — plus `profile_ci_lint.py` and `consistency_lint.py` — size: **M** · route: **standard / high**
- [x] 1.6 Per-module `module-info.java` (JPMS) for all nine modules, with the test module's `--add-opens` requirement documented (ADR-001, spec §1.1). **Eight descriptors, not nine:** `d4np-bom` is `packaging=pom` with no sources, so a descriptor there is impossible rather than merely unnecessary — the lint now *asserts* it has none. **A naming conflict was escalated rather than guessed:** `orchestrator/project.yaml` said modules land at `it.d4np.utils.core, .jdbc, .security` while `AGENTS.md` §5, the ADR-0003 note and the generated README hint all put core's own sources at `it/d4np/utils/`. A JPMS module name is a binary contract that appears in consumers' own descriptors, so the maintainer settled it — **`d4np-core` owns the family root `it.d4np.utils`** — and the manifest note is corrected so the records no longer disagree (**ADR-0005**, which takes number 0005 and pushes item 1.10's range to 0006–0009: L-0005 for the second PR running). **No module can export anything yet, and that is enforced by the compiler, not chosen:** the reactor holds exactly one Java file (the 1.3 smoke test), and `exports` of a package with no class is a compile error — *"package is empty or does not exist"* — because an `exports` clause is not a forward declaration. Two candidate workarounds were measured rather than reasoned about: inventing placeholder types was rejected as manufacturing published API under japicmp's gate, and **`package-info.java` alone was verified NOT to satisfy `exports`** (it emits no class file, so the package stays "empty") — the natural first fix, and it does not work. So each descriptor declares its name and its internal `requires` edges only; `exports`, `java.sql`, Jackson and Spring-as-`requires static` arrive with the code that uses them, the same no-speculation discipline item 1.1 applied to versions. **Two tool findings, both verified:** Spotless *does* parse module declarations (it reformatted all eight), but **Checkstyle 10.26.1 cannot parse one at all** — the pin is effective and a minimal `TreeWalker` + `AvoidStarImport` config on a bare `module m {}` still fails with `no viable alternative at input 'module'`, and because that aborts the whole goal rather than reporting a violation, one descriptor would take the entire lint down; `**/module-info.java` is excluded, costing nothing real since every rule in `checkstyle.xml` governs *type* declarations and a descriptor has no types and no imports. **The graph is now a gate, not a document:** `consistency_lint.py` gains `jpms-congruence`, asserting internal `requires` equals the POM's internal `<dependency>` set, names are unique and under the family root, and the BOM has no descriptor — deriving artifact→module-name from the descriptors themselves so the table is not restated a third time. Proven non-vacuous against five deliberate breakages (missing `requires`, extra `requires`, missing descriptor, out-of-family name, descriptor in the BOM). Verified on Windows + Temurin 17: `clean verify` green, and `--describe-module` on all eight built JARs reproduces spec §3 exactly, with `--add-modules ALL-MODULE-PATH` resolving the whole graph — itself shown non-vacuous, since dropping `d4np-json` yields `FindException: Module it.d4np.utils.json not found, required by it.d4np.utils.spring` — size: **M** · route: **frontier-reasoning / high**
- [x] 1.7 `maven-enforcer` wired with the ADR-001 banned-dependency and convergence rules — this is what makes NFR-08 a build property rather than a review promise. Enforcer **3.6.3**, bound to `validate` (unlike Spotless/Checkstyle, which 1.4 deliberately left unbound: a dependency gate that runs only when invoked *is* the review promise NFR-08 replaces), so all six build cells enforce it. **The policy is per-module and irreducibly so** — core may not see Jackson; `d4np-json` exists in order to see it — so the parent carries only what holds everywhere (`requireJavaVersion [17,)`, `requireMavenVersion [3.3.1,)` tied to the real `.mvn/jvm.config` floor rather than a guess at runner versions, `requireReleaseDeps`, `banDuplicatePomDependencyVersions`, `dependencyConvergence`, plus FR-25's ban on `d4np-test` outside test scope and the javax EE artifacts of NFR-07), and each module carries its own `enforce-adr-001`. **The rule's FORM follows the module's contract (ADR-0006):** default-**deny** allowlists where the contract is a statement about everything — core/jdbc/concurrent/test ("zero third-party", "JDBC API only"), plus security and json with one framework each — because a ban list naming Spring/Jackson/Redisson would have **admitted `commons-lang3` into core**; default-**allow** ban lists for the two framework carriers, since an allowlist there would have to track Spring Boot's and Redisson's transitive closure and would fail for reasons unrelated to policy. **A trap recorded rather than discovered:** Redisson itself depends on Jackson, so banning Jackson family-wide — the naive reading of "Jackson lives only in `d4np-json`" — would break `d4np-lock-redisson` the moment item 5.3 adds Redisson. **The CI command was wrong and measurement proved it:** `mvn enforcer:enforce` runs only the `default-cli` execution and therefore **silently skips every per-module rule** — with a banned `commons-lang3` in core it reported *all rules passed*, while `mvn validate` failed with `commons-lang3 <--- banned via the exclude/include list`; the job now invokes the phase. Verified by a **28-case matrix**, all passing, that injects one dependency into one module at a time and asserts banned-vs-allowed in both directions (Jackson allowed in `json`, banned in `security`; Spring Boot with its full closure allowed in `spring-adapter`, Redisson banned there; Redisson plus Netty/Jackson allowed in `lock-redisson`, Spring banned; `d4np-test` banned at compile scope, allowed at test scope; a *new* test-scope library still allowed, so the rules are not brittle to test-stack upgrades). `consistency_lint.py` gains `module-dependency-policy` so a module cannot ship with no policy while still building green. **Known gaps, stated:** the rules are dormant until M2 adds a real dependency — the matrix is what shows they will not stay dormant-green; `d4np-bom` is unpoliced because it carries no parent by design and declares only `dependencyManagement`, which enforcer does not treat as a dependency; and **this item does NOT turn the `compatibility` job green** — an earlier revision of this line implied it would, which was wrong. That job also runs `japicmp:cmp`, failing with `No plugin found for prefix 'japicmp'` because japicmp is not configured at all: **item 8.1 owns that half** — size: **M** · route: **frontier-reasoning / high**
- [x] 1.8 JMH (`bench/`) and jcstress (`jcstress/`) harness skeletons so the NFR gates have somewhere to land — **and declare the `jmh` and `jcstress` Maven profiles the CI jobs already invoke.** Discovered while closing 1.2: `ci.extra_jobs` runs `mvn -B -Pjmh verify` and `mvn -B -Pjcstress verify`, but neither profile existed, and Maven only *warns* on an unmatched profile (`The requested profile "jmh" could not be activated because it does not exist`), so both jobs reported **pass while verifying nothing** — the same vacuous-green class as item 1.3's unpinned surefire, 1.1's `version-lockstep`, and 1.7's `enforcer:enforce`. **The harness sources are TEST-scope roots, and that is what makes the item cheap (ADR-0007):** `src/bench/java` and `src/jcstress/java` are added by `build-helper` *inside* each profile with `jmh-core` 1.37 and `jcstress-core` 0.16 at `test` scope, so ADR-0006's `*:*:*:*:test` exemption already permits them in the four default-deny modules — a compile-scope harness would have meant editing core's zero-third-party allowlist to admit measurement tooling and shipping `jmh-core` in the consumer-visible graph. **The runners are launched with `exec:exec`, not `exec:java`:** both tools fork JVMs from `java.class.path`, which under `exec:java` is the plugin's isolated classloader, so the fork would start a JVM that cannot see the harness. **Opt-in is per module** (`jmh.skip`/`jcstress.skip`, defaulting to skip, flipped to `false` beside the sources in `d4np-core`) because both tools **exit non-zero on an empty test list** — verified: an opt-in without sources dies in `TestList.getTests` with an `NPE` that names nothing you wrote — so an unconditional binding would fail the eight modules that measure nothing yet. `consistency_lint.py` gains `harness-opt-in`, asserting the parent declares both profiles and that sources ⟺ opt-in in both directions; proven non-vacuous against four deliberate breakages (renamed profile, sources without opt-in, opt-in without sources, a bench file dropped into `d4np-jdbc`). **The skeletons measure real things rather than placeholders, the 1.6 discipline:** the benchmark times a volatile field read against a final field read — the two costs NFR-01's ≤ 2 ns/op budget for `Lazy.get()` is *made of* — and both land at 0.4–0.6 ns/op locally, the first evidence that budget is reachable; the jcstress test asserts that a `volatile` flag orders a plain payload (the FR-03 idiom), with "flag seen, payload not" marked `FORBIDDEN`, and **it fails the build when a forbidden state is observed** — verified by forbidding an outcome the run does produce. **Four findings that only running it could produce:** (a) plugin-level `<configuration>` cannot be used for the runners, because both profiles declare `exec-maven-plugin` and under `-Pjmh,jcstress` the blocks merge — measured: **jcstress ran twice, JMH never ran, and the build passed**; config now lives per `<execution>`; (b) **a non-ASCII character in a jcstress `desc` breaks the generated `META-INF/TestList`** (lengths counted in characters, consumed as bytes), so one em dash killed the run with `NumberFormatException: For input string: ""` — jcstress strings stay ASCII, and this repo's house style makes that trap easy to hit twice; (c) jcstress writes its `.bin.gz` result blob to the **working directory** with no option to relocate it (`-r` is only the HTML report), dropping untracked files next to `pom.xml` until the exec process was pointed at `target/`; (d) **Spotless and Checkstyle were both blind to the new roots** — Spotless's default include set is `src/main` + `src/test`, and Checkstyle sees only *active* compile roots — so the format gate would have been green over unread files and `spotless:apply` could not have formatted them; both are now explicit and both were verified to read the new files. Verified on Windows against the toolchains CI uses, Temurin **17.0.20+8** and **21.0.12+8**: `clean verify` (`Tests run: 3`), `-Pjmh verify` (two benchmarks, `target/jmh-result.json` written), `-Pjcstress verify` (28 results on 17, 14 on 21 — biased locking is gone in 21 — zero failures), `-Pjmh,jcstress verify` together, `spotless:check`, `checkstyle:check`, `validate`, `consistency_lint.py`. **Known gap, stated:** a plain `mvn verify` does not compile the harnesses, so a benchmark that stops compiling is caught by the two profile jobs (Linux/JDK 21) rather than by the six build cells — the price of profile activation, versus paying JMH's code generation in every cell. The rendered `benchmark` job also references `matrix.toolchain` while declaring no matrix, which resolves to JDK 21; it comes from the upstream template, and 21 is the toolchain the job was verified on — size: **S** · route: **standard / medium**
- [x] 1.9 `SECURITY.md` with a coordinated-disclosure policy, and `.github/CODEOWNERS` — **done by the scaffold render**: both artifacts landed with the bootstrap, and CODEOWNERS (`* @danielPoloWork`) now supersedes `ownership.owner` as the live contribution-policy fallback — size: **S** · route: **standard / medium**
- [x] 1.10 Reconcile the ADR record — **settled by indexing the imported four in place and keeping their ids (ADR-0008; the maintainer chose the route on 2026-07-29 after the three options were costed).** By the time the item ran, `docs/adr/README.md` indexed **seven** records while **eleven** decisions had been made: the four Accepted pre-governance decisions (module split, error model, JWT library, generated layout) sit in `.spec/adr/` as `d4np_java_adr_00N_*.md`, with three-digit ids, twelve days older than the docs system and still binding — `ADR-001` is what every module POM and every `module-info.java` cites for the dependency graph. **The blast radius was measured, not estimated, and it decided the route:** `git ls-files` at `4e2bf62` carries **183 references to `ADR-001`…`ADR-004` across 45 files** (124 to `ADR-001` alone; 10 POMs, 8 module descriptors, `orchestrator/project.yaml` 28, this file 22, the imported spec 16, `docs/rfc/0001` 15) plus **~53 path citations** to `.spec/adr/…`, about **30 of them inside already-Accepted ADRs** (0003 cites those paths thirteen times) — and the manifest's `linter:` string literally contains "maven-enforcer for the ADR-001 dependency rules", so renumbering is a manifest change plus a re-render of six generated files, not a find-and-replace. **The decisive argument was history, not cost:** `main` carries `a76ea4c build: enforce the ADR-001 dependency policy per module`, and renumbering would leave that pointing at a dead id that *collides with a live one* — a reader resolving "ADR-001" to `ADR-0001` lands on "Record architecture decisions" and is silently wrong. An id is a stable identifier, not an address. **A recorded plan had to be superseded, and it was already impossible:** ADR-004's closing line — accepted by the owner on 2026-07-26 — promised that "all four normalise to `docs/adr/000N-*.md` when scaffold moves them", but scaffold never moved them and the generated docs system took `0001` and `0002` for its own bootstrap records, so `ADR-001` can never become `ADR-0001`. Only the file-naming note is void; ADR-004's actual decision stands, and the note is left in place rather than edited — an Accepted record is superseded, not rewritten. **L-0005 for the fifth time:** this line previously predicted 0007–0010, then 0006–0009; ADR-0004, 0005, 0006 and 0007 each took a number it had reserved, and the item claims **0008** at the moment of landing instead of predicting anything. **What landed:** two complete indexes in `docs/adr/README.md` (governed four-digit, imported three-digit, twelve rows, with a "carried forward by" column so a reader can navigate between the schemes), a *"Two numbering schemes, and why"* section stating that the digit count is semantic, that `ADR-001 ≠ ADR-0001` and that the three-digit series is **closed**; `.spec/` documented as the **intake area** in `docs/README.md`, whose layout table had never mentioned it; the citation convention in AGENTS.md §7. **The index is now a gate, in both schemes:** `consistency_lint.py`'s `adr-index` gains the imported set's bijection, id-to-filename agreement, sequential three-digit numbering, and **status congruence against the record itself** — which nothing had ever compared, so an ADR marked `Superseded` in its header could have sat in the index as `Accepted` indefinitely. Proven non-vacuous against six deliberate breakages (imported row deleted, governed status cell falsified, imported record's status changed under the index, title id vs filename, a gap in the three-digit series, an index link to a file that does not exist). **Zero references changed** — no POM, descriptor, manifest entry, spec, RFC or Accepted ADR was edited to accommodate the decision, which is the property that justified it. **Two gaps, stated:** `docs/adr/README.md` is a *generated* file whose renderer emits only the 0001/0002 rows, so a re-render still deletes every row added since (risk R-07) — unchanged by this item except that the loss now fails the lint immediately instead of being noticed months later; and nothing mechanical prevents someone issuing `ADR-005` into the closed series, which is held by the convention section alone. **Filed rather than answered:** which *specification* document is authoritative when `.spec/d4np-java.md` and `docs/specs/01_spec_utils.md` disagree is the same two-homes shape but a different document, and is now item **1.12** — size: **M** · route: **frontier-reasoning / high**
- [x] 1.11 Wire ErrorProne + NullAway — **done: ErrorProne 2.50.0 + NullAway 0.13.8 in a profile that self-activates on `<jdk>[21,)</jdk>`, plus `failOnWarning` globally, so AGENTS.md §9/§10's warnings-as-errors promise is enforced instead of described (ADR-0009).** The three findings 1.4 left behind were re-measured, and two of them changed. **(a) A JDK-17-capable ErrorProne does exist**, so the roadmap's "JDK-21-only profile *or* a version built for 17" was a real either/or: reading the class-file majors out of the published `error_prone_core` jars gives 2.31.0 → 55, **2.36.0–2.42.0 → 61**, **2.43.0+ → 65**, so **2.42.0 is the last release that loads on JDK 17** — and 2.42.0 + NullAway 0.13.8 was verified to find the same violations on 17 *with the identical flag set* (both versions also work on JDK 25). It was rejected deliberately, not overlooked: 2.42.0 being the *last* 17-capable release means adopting it freezes static analysis at a 2025 version for as long as NFR-07 keeps 17 as the published baseline, and a two-version split across cells would make "clean" JDK-dependent — a check added after 2.42.0 fires only on the 21 cells, a false positive fixed after it only on the 17 cells. **(b) There are three required javac flags, not two,** each proven by removing it and reading the failure: `-XDcompilePolicy=simple` (*"The default compilation policy (by-todo) is not supported by Error Prone"*), `--should-stop=ifError=FLOW`, and `-XDaddTypeAnnotationsToSymbol=true` (*"required by Error Prone on JDK 21"*). **(c) The `ProvisionException` 1.4 read as a version conflict is an ambiguous wrapper** — it was reproduced here *from the compatible pair* just by omitting `-XepOpt:NullAway:AnnotatedPackages`, whose cause reads *"NullAway configuration is incorrect"*. The conflict is nonetheless real: 2.50.0 + 0.12.7 **with** the option set still dies, but the cause is `NoClassDefFoundError: com/google/errorprone/predicates/type/DescendantOf` — a class removed from ErrorProne after 0.12.7 was built. So 1.4's conclusion stands and its diagnosis was incomplete: **read the cause chain, not the wrapper**, and treat the pair as the unit of upgrade (Dependabot will offer them separately). **Two knobs earned their place by measurement:** `-XepExcludedPaths` for generated sources is **required, not prophylactic** — without it ErrorProne reports 12 `ThreadPriorityCheck` findings in item 1.8's **JMH-generated** code and, warnings being errors, `default-testCompile` fails; and `.mvn/jvm.config` is **load-bearing for ErrorProne**, which 1.4's README only anticipated — with the file moved away, compilation dies with `IllegalAccessError … module jdk.compiler does not export com.sun.tools.javac.api`. The existing eight exports plus two opens suffice, so no flag was added. **Nothing leaks into the dependency graph:** `annotationProcessorPaths` resolve outside project dependencies, so `dependency:tree` on `d4np-core` is still nothing but test-scoped JUnit and AssertJ, NullAway's own JSpecify/Guava never reach a consumer, and ADR-0006's default-deny allowlists needed no edit. **No nullability annotation artifact was added** — core has no production types yet and the core-contracts RFC states its contracts in Javadoc, so the annotation choice arrives with the first nullable member (item 2.1), the same no-speculation discipline as 1.6 and 1.8. `consistency_lint.py` gains **`static-analysis-wired`**, asserting both directions of manifest-claim ↔ POM-config, NullAway at `ERROR`, `AnnotatedPackages` present, and `failOnWarning` on — but deliberately *not* the javac flags or the generated-sources exclusion, because dropping those fails the build by name while a severity demotion turns it green silently. Proven against six deliberate breakages; **its own first draft was vacuous** and is recorded as such — it matched `AnnotatedPackages` inside the POM *comment explaining the option*, so POM text is now matched with comments stripped. Verified on Windows against Temurin **21.0.12+8** and **17.0.20+8**: probe violations (a NullAway null-pass and an ErrorProne `SelfAssignment`) **fail the build on 21 and compile on 17**, which is the gap made visible; `clean verify`, `test`, `spotless:check`, `checkstyle:check`, `validate`, `-Pjmh,jcstress verify` (both harnesses *and* both annotation processors — 1.8's `combine.children="append"` merge held) and `consistency_lint.py` all green. **Known gap, stated:** the three JDK-17 cells and any contributor on 17 run no static analysis; nothing ships unanalyzed, since the three JDK-21 cells and the `quality` job analyse the same sources on every PR — whose title (`spotless + errorprone + checkstyle`) stops being aspirational, the jacoco/pit halves remaining item 8.2 — size: **M** · route: **frontier-reasoning / high**
- [x] 1.12 Settle which **specification** document is authoritative — **settled: the manifest's `spec.*` block is the source of record, `docs/specs/01_spec_utils.md` is its authoritative published view, an RFC outranks both where it pins a contract, and `.spec/d4np-java.md` is superseded provenance (ADR-0010).** Filed by item 1.10, which fixed the ADR half of the same two-homes problem and left this half open. **Three measurements collapsed the choice this item was framed to make.** (1) **The imported draft cannot be the id source — it contains zero `FR-` ids**, and only `NFR-01`…`NFR-06` (its §6 performance table). The `FR-01`…`FR-25` / `NFR-01`…`NFR-12` vocabulary that this file, the POMs, the threat model and the compliance register all cite was **minted during Phase-5 intake**, not imported; the draft numbers its requirements as *items 1–25*, and `item 17` ≡ `FR-17` by coincidence of numbering, which is why nobody noticed. So the roadmap's own Traceability line named a document that holds none of the ids it claimed to source — **corrected in this PR**. (2) The manifest and the published spec **already agree exactly**: 25 FR + 12 NFR each, sets equal both ways, all 25 requirement texts equal character for character. (3) The “frozen contract” is a **generated file** — byte-identical to a fresh `render.py` run — so it cannot be the source: a hand-edit there is reverted by the next re-render, which is risk R-07 and already cost item 1.4's work once. **Hence the branch the item offered second (keep the draft authoritative and back-port the core-contracts RFC into it) was rejected on evidence, not taste:** back-porting would first require *minting* the FR vocabulary into an archived 2026-07-14 document and then maintaining two normative specs whose shapes differ (nine sections versus six), with the authoritative one living outside `docs/`. **What landed:** a `SUPERSEDED` banner on the draft naming its successor and stating that it holds no `FR-` ids (body untouched below it — a narrow, additive exception to ADR-0008's “do not edit the archive”, justified because a reader arriving by search has no other signal); the five `[GAP] no contract stated` requirement lines that the core-contracts RFC has in fact pinned now say so **by pointer, not by copy**, since duplicating that RFC's tables into the manifest would create the very second home this item removes; and the manifest edit was propagated by **re-rendering** the spec rather than hand-editing it, verified by diffing the regenerated file. `consistency_lint.py` gains **`spec-authority`**, asserting manifest ↔ published-spec id-set *and* text equality in both directions, that every FR/NFR id cited anywhere in this file exists in the manifest (so a typo one past the end of the set fails the build — including in this very line, which the check caught while it was being written), and that the draft keeps its banner and never regrows `FR-` ids. Proven against five deliberate breakages (id added to the generated spec, requirement text hand-edited there, a dangling id in the roadmap, the banner removed, `FR-` ids re-introduced into the archive). **Known cost, stated:** the authoritative spec is a rendered artifact, which is inherent to an EADOS-generated repository and the same trade ADR-0003 recorded for the source layout — mitigated because the render is now *verified* rather than assumed. The remaining `[GAP]` markers (FR-06 transaction semantics, FR-12 AAD, NFR-11 CVSS threshold, NFR-12 provenance) are real and stay owned by later RFCs — size: **M** · route: **frontier-reasoning / high**

## Milestone 2 — core foundations

**Goal:** the zero-dependency core — creational and structural patterns plus the ADR-002 error model.
**Covered by RFC-0001** (`docs/rfc/0001-core-contracts.md`), which pins the nullability, error and
thread-safety contract for all nine types before any of them is written.

Note on routing: RFC-0001 already made these design decisions, so no M2 item carries
`decision-heavy`. That drop in route cost is the return on contract-first ordering, and it is the
argument for items 3.0–7.0 doing the same for their milestones.

- [x] 2.1 `Result<T>` sealed `Ok`/`Err`, `ErrorDetail(String code, String message, Throwable cause)`, unchecked `BusinessException` (RFC-0001; FR-17, FR-18, ADR-002) — the shared error vocabulary every later module depends on. **The first production code in the repository**, so it also closes the two things earlier items deferred to "the first types": `module-info.java` gains `exports it.d4np.utils;` (item 1.6 left it out because `exports` of an empty package is a compile error, not a forward declaration), and the nullability annotation arrives (item 1.11 deferred it to "the first nullable member", which is `ErrorDetail.cause`). **Core declares its own `@Nullable` rather than depending on one (ADR-0011)** — ADR-001 fixes core at zero third-party dependencies, and an annotation is the one dependency kind that appears in the *published signatures*, so JSpecify at compile scope would put an artifact in every consumer's graph for three lines of metadata; at `provided` scope it is worse, because the marker survives into our class files with nothing on a consumer's classpath to resolve it. NullAway matches any annotation whose FQN ends in `.Nullable`, so this is the mechanism rather than a workaround — **proven in both directions:** deleting the marker fails compilation with `[NullAway] passing @Nullable parameter 'null' where @NonNull is required` at the two-argument constructor, restoring it goes green, and `NullableTest` asserts the marker actually propagates to the accessor, the field *and* the canonical constructor parameter (a target set missing one site would leave it invisible exactly where NullAway reads it, while the build stayed green because nothing else in core is nullable). **One sentence of the specification turned out to be unimplementable and is recorded rather than quietly dropped (ADR-0012):** FR-17's "`Ok(null)` is forbidden — use `Result<Void>`" cannot be honoured in both halves, because `Void` is uninhabited (its only value *is* `null`) and a record's canonical constructor cannot be bypassed — so if `new Ok<>(null)` throws, no code, not even a factory in this library, can build the void success. The three real options were costed: permit null and mark the payload `@Nullable` (rejected — it makes `Ok.value()` conditionally null **public API**, so every consumer inherits a null check on the happy path for a case that cannot apply to them), mint a `Unit` type (rejected as premature — it contradicts a sentence three governing documents state, on the strength of zero call sites), or enforce the rejection and say what is missing. The last was chosen because it is the **reversible** direction: adding a type or factory later is MINOR under RFC-0001 §Versioning, making a non-null return nullable later is MAJOR. Item 3.0's scope now carries the question, since RFC-0002 owns the first `Result`-returning surfaces. **`ErrorDetail` is `Serializable` and that is not decoration:** `BusinessException` inherits `Serializable` from `Throwable` and holds an `ErrorDetail`, so a non-serialisable payload would make every `BusinessException` fail to serialise — silently, and only in the hosts that do it — and a round-trip test asserts it instead. **A jcstress regression was caused, measured and fixed inside this item, and it is the most useful thing here:** jcstress probes the target VM for `@Contended` support and adds `-XX:-RestrictContended` to the forked test VMs only if the probe passes; that probe calls `Reflections.getClasses("Result")`, which derives class names from `java.class.path` by replacing path separators with dots — correct for a JAR entry (`JarEntry.getName()`), broken for a **directory** entry, where it passes `File.toString()` through unchanged. So `target/classes/it/d4np/utils/Result$Err.class` became `E:.repository.….Result$Err`, `Class.forName` threw, and the probe went from `[OK]` at v0.1.0 to `[N/A]` — meaning `@Contended` padding stopped taking effect and **every jcstress test in this repo silently became less likely to observe the interleaving it exists to forbid**. Any `*Result*` name is enough: verified that `ResultTest` in `target/test-classes` matches too, which is what rules out packaging core as a JAR as the fix. Restored with `-jvmArgsPrepend` (not `-jvmArgs`, whose own help text says it "disables JVM flags auto-detection and runs only the single JVM mode" — that would collapse the 14-configuration matrix and cost more sensitivity than it recovered). **The loss was invisible in the log**, which is why it is worth the paragraph: jcstress echoes only each configuration's *variable* flags, so a v0.1.0 run and a broken run print the identical two brackets and the only signal is the `[N/A]` line. **Verified by running the toolchains, not by reading configuration** — Temurin **17.0.20+8** and **21.0.12+8**: `clean verify` (`Tests run: 42`) on both, `spotless:check`, `checkstyle:check` across all ten modules, `validate`, `-Pjmh,jcstress verify` on both (28 jcstress results on 21, 56 on 17 — biased locking exists there — zero failures, and the new harness proven non-vacuous by forbidding an outcome the run does produce, which turns the build red), and `consistency_lint.py`. **Three findings recorded because only running produced them:** an XML comment may not contain two consecutive dashes, so jcstress's own log prefix cannot be quoted in the POM comment explaining it — the third config format in this repo that will not carry its own explanation, after `.mvn/jvm.config` and `.gitignore` in item 1.4; `consistency_lint.py`'s `patterns` check could not be satisfied by any honest row, because it accepted only four-digit ADR links (this pattern was decided by the imported **ADR-002**, which ADR-0008 deliberately kept at three digits) and only a repo-root-relative `src/...` path (ADR-0003 moved production code to `<module>/src/main/java/...`) — both widened and both still resolved on disk, proven against six deliberate breakages; and **JDK 17's doclint reports three spurious "no comment" warnings for the components of a `Serializable` record** — reproduced by swapping `Serializable` for `Cloneable`, which silences them, absent on JDK 21, and not suppressible with `@serial`. Javadoc is otherwise clean at `-Xdoclint:all -Xwerror` on both toolchains after writing out the compact constructors' `@param` tags, which JDK 18+ infers and 17 does not; there is no Javadoc gate in the build yet and **item 8.4 should build the Javadoc JAR on 21** — size: **M** · route: **frontier-reasoning / high**
- [x] 2.2 `Lazy<T>` with the jcstress harness proving safe publication; default failure policy **retry**, `memoizingFailures()` opt-in, re-entrant initializer throws (RFC-0001; FR-03, NFR-01) — **done: double-checked `volatile` behind a private monitor, with `null` itself as the uninitialized marker ([ADR-0013](docs/adr/0013-lazy-initialization-by-double-checked-volatile.md)).** RFC-0001 had pinned four behaviours and named the fifth — *"double-checked `volatile`"* — in a table cell, so this item's decisions were the ones a mechanism name leaves open, and they are coupled: `null` can be the marker **only** because a `null` result is rejected as a defect, so the two rules hold each other up and neither can be relaxed alone. The monitor is a private object rather than `this`, because a type that synchronizes on itself publishes its lock; re-entrancy is a plain `boolean` guarded by that same monitor rather than a `ThreadLocal`, because **the lock already establishes thread identity** — only the holder can observe the flag, and Java monitors are reentrant — and the check runs *before* the flag is set, so a rejected re-entrant call never reaches the `finally` that would clear the outer invocation's flag (invert those two lines and one bad call wedges the instance forever; `reEntrancyLeavesNoStuckState` is the regression). **NFR-01 is the first budget in this project closed by a measurement instead of an assertion:** `Lazy.get()` measures **0.945 ns/op** against a **2 ns/op** budget on Temurin 21, with item 1.8's bare `volatileRead` floor at 0.552 ns/op in the same JMH invocation — so the wrapper costs ~0.4 ns over the field access it wraps, which is the number that says `get()` is inlining. **The most useful finding is a gap, and it was produced by breaking the code rather than by reasoning about it:** `volatile` was deleted from the value field and the whole gated suite re-run — **the build stayed green**, 56/56 jcstress results passing and the compiler silent. Both halves were then diagnosed separately. **(a)** ErrorProne's `DoubleCheckedLocking` check *is* enabled and *does* work — a probe class with the classic single-method idiom compiled in this reactor produces `Double-checked locking on non-volatile fields is unsafe`, which item 1.11's `failOnWarning` turns into a build failure — but it never fires on `Lazy`, because it matches an if/synchronized/if nest **inside one method body** and this design deliberately splits the two checks across `get()` and `initialize()`. **The optimisation that makes NFR-01 reachable is the same edit that removes the static-analysis net for the defect it enables.** **(b)** jcstress cannot surface it here either: x86-64 is a TSO machine and does not reorder the stores whose reordering is what makes the pre-JSR-133 idiom unsafe — the defect is genuinely unobservable on this hardware and needs a weakly-ordered CPU to appear. So that one keyword is currently held **by review, not by a gate**, which is the argument for an **aarch64 jcstress cell** when the matrix next changes; filed against item 8.3, which already owns runner topology for NFR gates. **What *is* mechanically enforced was proven by breaking it:** both new harnesses had their single `ACCEPTABLE` outcome flipped to `FORBIDDEN`, and the run reported `[FAILED]` for each with Maven exiting 1. **Item 1.8's `VolatilePublicationStress` was kept, not replaced** — its own note predicted otherwise and is corrected in place: it exercises the bare `volatile` pair with no lock, which makes it the **control** (both fail → suspect the platform; only the `Lazy` ones fail → the fault is ours), the role `PublicationBaselineBenchmark` keeps on the JMH side. **One limitation measured rather than assumed:** a *sneaky-thrown* checked exception is not memoized — `Supplier.get()` declares none, so the catch is `RuntimeException | Error`, and a probe shows three `get()` calls producing **three** initializer invocations with the original `IOException` propagating unchanged each time; nothing wedges, `memoizingFailures` simply degrades to retry for that one exotic case, and widening the catch to `Throwable` was rejected rather than overlooked. Verified on Temurin **17.0.20+8** and **21.0.12+8**: `clean verify` (`Tests run: 57`, up from 42), `-Pjmh,jcstress verify` on both, `spotless:check`, `checkstyle:check`, `validate`, and `consistency_lint.py` — whose `patterns` check caught the catalogue row citing ADR-0013 before the record existed, which is the gate item 2.1 widened doing its job on the next item — size: **M** · route: **frontier-reasoning / high**
- [x] 2.3 `StrategyRegistry<K,S>` with the `find`/`getOrThrow` contract (RFC-0001; FR-04, NFR-04) — **done: a final `ConcurrentHashMap`-backed registry with exactly the three methods FR-04 names, plus `StrategyNotFoundException` ([ADR-0015](docs/adr/0015-strategy-registry-last-write-wins.md)) and the library's first logging ([ADR-0014](docs/adr/0014-log-through-the-jdk-system-logger.md)).** RFC-0001 carried FR-04 over "unchanged", and implementing it surfaced three questions the contract does not answer, each affecting the published surface. **(a) `StrategyNotFoundException` must not extend `BusinessException`** — FR-19's own table maps a business rule violation to **422** and a missing strategy to **500 plus an alert**, so as a subclass a handler whose `catch` clauses ran in the wrong order would silently report an operations failure as a client error; a test asserts the *negative*, because this is a decision a later refactor can reverse by accident. **(b) The known keys are captured as text, not as `Set<K>`** — every `Throwable` is `Serializable`, so a field of the consumer's key type would make the exception serialisable *only when their key type happens to be*, failing silently and only in the hosts that serialise. Item 2.1 hit the same shape with `ErrorDetail` and could fix it by making the payload serialisable; here the payload is consumer-supplied, so it is rendered at construction instead. **(c) The message is truncated at 20 keys** while `knownKeys()` stays complete: NFR-04 sizes this registry at **1000** strategies, so a full list is not a diagnostic but an incident, and the truncation states its count rather than the list silently stopping. **NFR-04 is met on both toolchains — `find` at 12.8 ns/op on JDK 21 and 17.8 on JDK 17, against a 50 ns/op budget**, measured at the budgeted shape rather than a convenient one (1000 entries, `@Threads(8)`, keys rotating per thread so the number includes cache misses across the table). **The most useful finding is a documented claim the benchmark refuted — and then a second measurement that bounded how far the refutation reaches.** `getOrThrow` reads the map directly and allocates no `Optional`, so it was written up as the cheaper call; on JDK 21 it measures **~2 ns/op slower** (15.0 vs 12.8), reproduced across three independent 3-fork runs with non-overlapping intervals and an ordering that never inverts. The obvious explanation — that constructing the exception inline pushes the method past the JIT's inlining size threshold — was **tested by moving the throw into a private method, and changed nothing** (14.57 before, 14.57 after), so the borrowed fast/slow split from `Lazy` (ADR-0013) was **reverted rather than kept as ceremony**. **On JDK 17 the two are indistinguishable** (17.8 ± 4.3 against 18.0 ± 0.7 — overlapping intervals, and a spread too wide for that toolchain to resolve a 2 ns difference at all), so the portable claim is the weak one: `getOrThrow` is *never faster*, and on one supported toolchain it is slower. Enough to retract the original claim; not enough to support a new one about why. The cause is left open at ~2 ns inside a 50 ns budget, and the Javadoc now states the measurement instead of the theory, telling callers not to choose between the two lookups on speed. **Logging was a decision, not a line of code:** FR-04 requires a warning on re-registration, ADR-001 forbids a logging dependency, and core's descriptor requires nothing but `java.base` — so `System.Logger` is the only mechanism that satisfies all three, and it routes through whatever backend the consumer already installed. **Two ways of testing it were built and both failed, which is the transferable part:** a test `System.LoggerFinder` registered through `META-INF/services` **can never win under surefire**, because the JDK resolves the finder once per VM on the first `System.getLogger` call and caches it, and the fork has already triggered platform logging by then — the identical service file works in a plain `java` launch, which is what makes it confusing; and attaching a `java.util.logging.Handler` is blocked by the module graph, since these tests compile *inside* `it.d4np.utils` and `java.util.logging` lives in `java.logging`, so it would mean either advertising a dependency core does not have or threading `--add-reads` through two build phases. Hence a package-private constructor taking a `System.Logger` — a testability seam, stated as a cost rather than hidden — which **earned its keep twice**: the jcstress registration harness uses it to silence a warning that would otherwise be emitted millions of times and measure the console instead of the registry. Verified on Temurin **17.0.20+8** and **21.0.12+8**: `clean verify` (`Tests run: 81`, up from 57), `-Pjmh,jcstress verify` on both (84 jcstress results on 21, 168 on 17, zero failures), `spotless:check -pl '!d4np-bom'`, `checkstyle:check`, `validate`, and `consistency_lint.py`. Both new harnesses proven non-vacuous by flipping an accepted outcome to `FORBIDDEN`. **Route matched for the first time since 1.9** — `standard / high` against this host's `standard` tier, so no ROUTE-MISMATCH to record — size: **S** · route: **standard / high**
- [x] 2.4 `GenericFactory<T,K>` (duplicate keys rejected, explicit `replace()`) and `FluentBuilder<T>` (template method, accumulated validation, repeatable `build()`) (RFC-0001; FR-01, FR-02) — **done: two creational patterns, two new exceptions, and two contract gaps closed ([ADR-0016](docs/adr/0016-generic-factory-atomic-duplicate-rejection.md), [ADR-0017](docs/adr/0017-fluent-builder-accumulated-validation.md)).** **FR-01 had no thread-safety row** — FR-03 and FR-04 have one, this does not, and the spec's own §5 `[GAP]` line flags exactly that ("only 5 of ~25 public types carry a nullability/error/thread-safety contract row"). It is not cosmetic here, because **duplicate rejection is a check-then-act operation**: implemented as `containsKey`-then-`put`, two threads registering the same key can both observe it absent, both write, and both believe they won — so the duplicate the method exists to reject is silently accepted, exactly during the parallel module initialisation where it matters. That implementation passes **every sequential test**, which is why `GenericFactoryRegistrationStress` forbids `2, 0` (both accepted) and `0, 2` (both rejected, key unbound) by name and only `1, 1` is acceptable. The factory is declared thread-safe and `register` is a single `putIfAbsent`. **FR-02's member list could not deliver FR-02's own promise.** The RFC pins "`build()` collects **every** violation", but the only accumulator it sketches is `require(Object, String)` — a null check — so every cross-field invariant (*"end date must follow start date"*, *"at least one line"*) could only throw, dropping the caller back to one violation per round trip for exactly the rules that are hardest to get right. **`reject(String)` is added**, recorded as an addition rather than slipped in, with the veto cost stated: removing a `protected` method later is MAJOR, so it is the decision worth objecting to now. Two overload alternatives were rejected — `require(boolean, String)` because a builder validating a `Boolean` field would silently bind the wrong overload, and `require(null, "...")`-as-a-hack because it reads as a null check that is not one. **The defensive-copy rule is the one clause the base class cannot enforce, so it is demonstrated instead of asserted:** a deliberately leaky subclass is built, the builder mutated afterwards, and the already-"built" object asserted to have changed — which also means the rule would fail loudly if `build()` ever started resetting the builder. **Item 2.3's exception was refactored rather than copied:** FR-01 needs a `FactoryKeyNotFoundException` whose message lists the known keys, which is character-for-character what `StrategyNotFoundException` already does, so the four non-obvious behaviours (render to text, sort, truncate at a cap, snapshot into a serialisable set) moved into a package-private `KeyDiagnostics`. Copying would have failed **silently** — the two would drift and both exceptions' tests would still pass, each testing its own copy. The two exceptions stay **siblings, not a hierarchy**: a shared public supertype would put a permanent coupling in the published hierarchy to save four lines of accessor, and a consumer catching one would begin catching the other. One shipped message changed as a result (*"the registry is empty"* → *"nothing is registered"*), recorded rather than left in a diff. **`BuilderValidationException` also stays outside `BusinessException`, and this one is the easy mistake:** FR-19 maps "validation" to **400**, but that is FR-14's `Validator` checking data from a *client*, whereas a builder violation means this application's own code forgot to set a field — reporting it as a bad request would misattribute the fault. **One finding from the toolchain:** NullAway rejects a `construct()` returning `null` at compile time, so the runtime check in `build()` is unreachable from an *annotated* subclass and the test asserting it needs a suppression to exist at all; the check still earns its place because ADR-0009 runs NullAway only on this build's JDK 21+ cells and no consumer's build runs it by default. **No benchmark, deliberately** — no NFR names either type, and inventing a budget the specification does not have would be worse than leaving it unmeasured. Verified on Temurin **17.0.20+8** and **21.0.12+8**: `clean verify` (`Tests run: 124`, up from 81), `-Pjmh,jcstress verify` on both (98 jcstress results on 21, 196 on 17, zero failures), `spotless:check -pl '!d4np-bom'`, `checkstyle:check`, `validate`, and `consistency_lint.py`. The new harness proven non-vacuous by flipping its accepted outcome to `FORBIDDEN` — size: **M** · route: **standard / high**
- [x] 2.5 `StringCaseConverter` (`Locale.ROOT`, the pinned tokenizer table), `ObjectUtils` (only what `java.util.Objects` lacks), `ResourceLoaderUtils` (`Class<?>` anchor resolution) (RFC-0001; FR-22, FR-23, FR-24) — **done, and it closes Milestone 2 ([ADR-0018](docs/adr/0018-tokenizer-word-threshold-and-utf8-default.md)).** **RFC-0001 pins FR-22 three times over — a table, a prose rule and pseudocode — and the three do not agree.** Seven rows are consistent; `URLs` is not. Its uppercase run is `URL`, length 3, followed by the lowercase letter `s`, so both the prose (*"an uppercase run of length ≥ 2 followed by a lowercase letter splits before the final uppercase"*) and the pseudocode (`if buf is upper-run and next c is lower: flush`) produce `UR`+`Ls`, rendering **`urLs`** — while the table pins `URLs` as one token rendering **`urls`**, with its tokens column and all three rendering columns agreeing with each other and disagreeing with the rule stated below them. **The table wins, because it is the testable artifact** — what a test transcribes, what a reviewer checks, what a consumer reads to predict output — and the unique rule reproducing all eight rows is that **an uppercase run splits only when at least two lowercase characters follow**: one character is a suffix, not a word. A one-entry special case for `URLs` was rejected as an acronym dictionary with one entry, which RFC-0001 explicitly refuses. **The threshold's cost is pinned by a test rather than left to be discovered:** `HTTPId` renders `httpid`, because `d` is a single lowercase character exactly as `s` is, while `HTTPIdentity` renders `http_identity`. **RFC-0001 is deliberately not edited here** — under ADR-0010 an RFC outranks the spec, but nothing yet says which half of an RFC outranks the other, and amending an approved RFC to match one's own implementation is the wrong direction for a governance system that exists to prevent exactly that; item 3.0 owns the one-line amendment. **`Locale.ROOT` is tested by changing the default locale, not by reading the source:** the test sets `tr-TR` and asserts `IDToken` still converts to `id_token`, where the default-locale overload would give `ıd_token` with a dotless ı and the identifier would stop matching the key it came from — silently, and only on some hosts. That, and `ResourceLoaderUtils` rejecting `..` on all three entry points, become compliance controls **C-03** and **C-04**. **FR-23 was fixed by a rule rather than a wishlist** — "only what `java.util.Objects` does not have" — so a test asserts *the rule*, comparing the two classes' declared method names reflectively; without it nothing stops the class growing a second spelling of `requireNonNull`. A second test asserts there is no `isEmpty(Object)` overload, since an `Object` parameter would silently answer `false` for any type nobody considered. **The UTF-8 default is written out rather than `Charset.defaultCharset()`**, which at the JDK 17 baseline is still platform-dependent (JEP 400 lands in 18); a fixture holding byte `0xE9` — valid ISO-8859-1, invalid UTF-8 — makes a regression to the platform default fail rather than pass on whichever machine ran it. **One test expectation was wrong and the implementation was right**, recorded because it is the useful direction: `toKebab("HTTPId")` was expected to give `http-id`, which contradicts the very threshold that makes `URLs` work. Verified on Temurin **17.0.20+8** and **21.0.12+8**: `clean verify` (`Tests run: 190`, up from 124), `-Pjmh,jcstress verify`, `spotless:check -pl '!d4np-bom'`, `checkstyle:check`, `validate`, and `consistency_lint.py`. No new jcstress harness and no benchmark, both correct rather than omissions: all three types are stateless statics making no thread-safety claim for a harness to prove, and no NFR names any of them — size: **M** · route: **standard / medium**

## Milestone 3 — core cross-cutting

**Goal:** validation, metrics, and an audit trail that cannot leak secrets.

- [x] 3.0 **RFC-0002 — cross-cutting core contracts.** **Done and approved:** [RFC-0002](docs/rfc/0002-cross-cutting-contracts.md) was drafted `Proposed` with an empty `approved-by:` — no RFC self-approves (AGENTS.md §6) — and **the owner approved it in session on 2026-08-02**, flipped in a separate commit so the two acts are visible as two acts. The peer-review round still did not run: this acceptance rests on the owner's direct authority, not on review, and that is recorded rather than implied. **FR-16 is why this RFC precedes any M3 code, and the risk inverts the usual calculus:** an application log is noisy, short-retention operational data, while an audit store is *deliberately* long-retention, widely replicated and exported for compliance review — the same leak is worse there, and "redact it later" fails because the records are already written and already copied. **The central rule is that redaction happens at capture, not at write:** the `AuditEvent` leaving capture is already redacted and exposes no raw value, because an event is an ordinary object that can be logged by an interceptor, serialised by a queue, caught in a heap dump or printed by a `toString()` on its way to the sink — **a sink cannot leak what it never receives.** **The spec's three asks are three different defaults, not alternatives** — an allowlist denies by default, a `@Sensitive` opt-out permits by default — so the contract is the *precedence*: never-capture > `@Sensitive` > `@Audited` (component) > `@Audited` (type) > omitted. `@Sensitive` is not redundant against deny-by-default, because the ergonomic form is one type-level `@Audited` and per-component `@Sensitive` is what makes that safe; the never-capture list then covers what both miss — a component added to an already-`@Audited` type months later. **Redacted and omitted are deliberately different outcomes:** a blocked-but-audited component records `name → [REDACTED]` plus whether it *changed*, because "the password was changed at 14:02 by alice" is exactly what an audit trail is for, and one bit that a secret rotated is not a secret. **Name matching is whole-token, not substring, forced by counterexample:** `pin` ⊂ `shipping`, `auth` ⊂ `author` — while a too-generic single token fails the other way, `key` swallowing `primaryKey`, which is why the list carries the pairs `api_key`/`access_key`/`private_key`. **This makes FR-22's `Locale.ROOT` rule security-load-bearing:** normalisation runs `StringCaseConverter`, so on a Turkish-locale JVM the default-locale overload would lower-case `API_KEY` to `apı_key`, **fail to match, and write the key to the audit store in clear** — control C-03 stops being a lonely correctness rule. **A fourth mechanism was forced by a trap the three asks walk into:** rendering a composite with `String.valueOf` prints a record's generated `toString()`, which includes *every* component — so `record Credentials(String user, @Sensitive String password)` behind an `@Audited` field would emit the password with the marker present, correct and completely bypassed. Only simple values are captured directly; anything else must itself be `@Audited` (recursion bounded at depth 3, cycles by identity) or it is a **misconfiguration that fails loudly at first capture**, surfacing in the first test rather than in production. **Two non-goals, stated so they are not requested later:** no content inspection (declaration-based only — scanning has false positives that corrupt records and false negatives that manufacture confidence), and **no deep reflection** — values are read through public accessors only, so a consuming module needs no `opens`, and asking for deep reflective access over a consumer's package to build a *redaction engine* is the wrong privilege to request. **`record` throws rather than returning a `Result`** — the one place loudness beats composability, since an ignored return value is silent and an audit trail that silently stops writing is a hole found at the next review. **FR-14** pins that a violation carries the property path and template **only**: Jakarta exposes `getInvalidValue()`, and *"'hunter2' is not a valid password"* becomes an RFC 7807 body reaching the HTTP client under FR-19 (control C-01); `ValidationException` sits outside `BusinessException` because FR-19 maps validation to **400** and `BusinessException` to **422**. **FR-15** pins three things the implementation must not decide: the sink resolves **once at construction** (a per-invocation lookup lets behaviour change mid-run, so two measurements of one method stop being comparable), **a recorder that throws must never propagate** (a business method failing *because* the metrics backend is down is an outage the instrumentation caused), and failed invocations are timed too, since recording only successes biases every latency number in the direction that hides the problem. **The inherited `Result<Void>` question is settled by minting `Unit` ([ADR-0019](docs/adr/0019-mint-unit-for-the-void-success.md)) — and the honest part is that the call sites did not decide it:** `Validator.validate` returns `Result<T>` and `AuditLog.record` throws, so neither M3 type forces it. It was decided on the error model, which is otherwise asymmetric — an expected failure can be a value only when the operation *also* has a payload, pushing every no-payload operation into the exception channel, the outcome ADR-002 adopted `Result` to avoid. It is an **enum with one constant**, because a final class with a public constant would need `readResolve` to survive the deserialisation `BusinessException` inherits from `Throwable`. **Scope widened by ~40 lines of production code, deliberately:** amending FR-17 to recommend `Result<Unit>` while `Unit` did not exist would recreate exactly the spec-outruns-code gap ADR-0012 was written to record. **Two amendments to RFC-0001 land by pointer, not by rewrite** — its FR-22 rule (which contradicts its own table, ADR-0018) and its `Result<Void>` sentence — with a header note on RFC-0001 so a reader arriving at the approved body is not misled, and the body untouched, because item 2.5 declined to edit an approved RFC so it would match the implementation the same agent had just written. **A gap is filed rather than fixed:** the audit sink is an outbound flow the threat model has no boundary for — B1–B6 do not cover "library → host-supplied audit store", now the widest-retention sink in the system — routed to **item 8.6**, which owns the next STRIDE pass, because the security-auditor owns that document. Manifest FR-16 gains a `[RESOLVED by RFC-0002]` pointer and FR-17's text is amended, with the spec **re-rendered** rather than hand-edited (ADR-0010) — applied **file-by-file**, because a wholesale copy would have overwritten 16 hand-maintained files with pristine generated versions, which is risk R-07 exactly as ADR-0008 predicted. Verified on Temurin **17.0.20+8** and **21.0.12+8**: `clean verify` (`Tests run: 196`, up from 190), `-Pjmh,jcstress verify`, `spotless:check -pl '!d4np-bom'`, `checkstyle:check`, `validate`, and `consistency_lint.py`. **3.1–3.3 are unblocked** — size: **M** · route: **frontier-reasoning / high**
- [x] 3.1 `Validator` over Jakarta Bean Validation 3.x (RFC-0002; FR-14) — **done, and it is the first third-party dependency edge core has ever had.** `jakarta.validation-api` arrives at `provided` scope with `requires static jakarta.validation` in the descriptor — the one artifact spec §3 permits here, and the allowlist entry item 1.7 wrote has been holding the slot open since. **`requires static` is the load-bearing half:** a plain `requires` would make the module system refuse to start every consumer that does not ship a Bean Validation API, including the ones that never validate, turning an opt-in capability into a mandatory dependency. **RFC-0002 pinned the rule — property path and message template only — and left the implementation one decision that turns out to be the whole item: WHICH Jakarta accessor renders it.** `getMessage()` is the readable one, the one every tutorial uses, and the leaking one: it is interpolated, interpolation resolves `${validatedValue}`, and quoting the rejected value is the most common reason anyone writes a custom constraint message at all — so `@Pattern(message = "${validatedValue} is not a valid password")` renders `hunter2 is not a valid password`, which FR-19 turns into an RFC 7807 body and hands to the HTTP client, past every proxy on the way. `getMessageTemplate()` is source text and cannot contain a runtime value ([ADR-0020](docs/adr/0020-render-violations-from-the-message-template.md)). **The cost is recorded rather than glossed:** a caller sees `name: {jakarta.validation.constraints.NotBlank.message}`, not prose, and a custom message keeps its literal `{max}` — resolving the template against a `ValidationMessages` bundle is the consumer's localisation step and FR-19's handler (item 7.1) is where it belongs. **Three alternatives were costed and all three fail in the same direction:** documenting *"do not quote the value in constraint messages"* is a review promise about code we never see; stripping the invalid value out of the interpolated message afterwards is content scanning, which RFC-0002 already rejected for FR-16 — a value that appears trimmed, truncated or partially escaped survives, and the result *looks* sanitised; and configuring a non-interpolating `MessageInterpolator` fixes only the factory we build, while `Validator.using(..)` accepts a host-configured validator whose interpolator is the host's business — **a guarantee that holds for one of two constructors is not a guarantee**, so the rule lives in the rendering. **The claim is proven, not asserted:** `ValidatorTest.neverCarriesTheRejectedValue` validates an object whose constraint message asks for the value by name and asserts `hunter2` appears in none of the three exits — the `ErrorDetail` of `validate`, the list from `violations`, and the `ValidationException` from `requireValid` — which is what moves compliance control **C-01** from "contract published" to one enforced call site. **Tested against Hibernate Validator, not a stub** (test scope, with Expressly for EL): the claim is about what a real provider produces from a real constraint, and a stub would assert the test's own idea of a violation. **`requires static` makes a missing provider a runtime condition**, so `create()` catches `RuntimeException` *and* `LinkageError` and refuses at construction naming both artifacts — the alternative is a `NoClassDefFoundError` from deep inside the first validated call. Both shapes are reachable in test through the package-private `fromProvider` seam, because the rest of the suite needs a provider on the classpath and the failure cannot be reproduced by removing it. **Two smaller contracts beyond the RFC's table:** the violation list is **sorted**, because the provider hands back an unordered `Set` and an unordered report makes a message assertion flaky and a log line undiffable; and a bean-level constraint's empty property path renders `<bean>`, asserted through a stub delegate rather than a custom class-level constraint, since the provider instantiates a `ConstraintValidator` reflectively and doing that from a fixture would mean opening `it.d4np.utils` to an automatic module for one token. `ValidationException` stays outside `BusinessException` (FR-19: **400** vs **422**) and carries `String` violations, for the reason ADR-0015 records — a payload typed on the consumer's object would make the exception serialisable only when that object happened to be. **The version is pinned in the module POM, not the parent** (the parent's own rule), and at **3.0.2 rather than 3.1.x**: at `provided` scope the host's copy wins at run time, so ours must be the floor of the compatibility matrix — Spring Boot 3.2+, whose managed Bean Validation is 3.0.2 — not the newest release. `Validator` is catalogued as an **Adapter**, distinct from the framework-edge Adapter the spec seeded for `spring-adapter`: this one adapts a *provider API* from inside core. Verified on Temurin **17.0.20+8** and **21.0.12+8**: `clean verify` (`Tests run: 216`, up from 196), `-Pjmh verify`, `-Pjcstress verify`, `spotless:check -pl '!d4np-bom'`, `checkstyle:check`, `validate`, and `consistency_lint.py`. **Two CI commands still cannot be reproduced locally, and neither is new here:** `japicmp:cmp` resolves no plugin (item 8.1 owns it) and `-Pcoverage` matches no profile, so Maven warns and runs a plain `verify` (item 8.2 owns JaCoCo/PIT) — recorded because a green `-Pcoverage` today is a vacuous green, the same class items 1.2, 1.7 and 1.8 each caught once. No benchmark and no jcstress harness: no NFR names FR-14, and the type is immutable over a delegate the Bean Validation specification already requires to be thread-safe — a harness would be measuring Hibernate — size: **S** · route: **standard / medium**
- [x] 3.2 `ExecutionTimeMetricAspect` with Micrometer-or-log fallback; the AspectJ/Spring binding stays in the adapter (RFC-0002; FR-15) — **done, and the first thing to settle was that the type the spec names after an aspect cannot be one.** Core may not see `aspectjrt` or Micrometer (ADR-001; spec §3 puts both in `spring-adapter`'s `provided` set), so either the name changes or the shape does. **The name stays and the shape becomes an advice body** ([ADR-0021](docs/adr/0021-time-through-an-advice-body-core-can-own.md)): core owns the measurement, the sink selection and the failure policy — all testable with no weaving at all — and the adapter contributes the `@Aspect`, the pointcut and the annotation, its advice one line. Renaming to `ExecutionTimer` was the honest alternative and was rejected on traceability: the spec is the frozen contract (ADR-0010), so it would mean a spec change to gain a word while every future reader tracing FR-15 by name lands on nothing. The Javadoc opens by saying it is not an AspectJ aspect, so nobody has to infer that from the imports. **The sink is FR-15's SPI verbatim — `ExecutionTimeRecorder(String, Duration, boolean)` — with `LoggingExecutionTimeRecorder` as core's dependency-free implementation** over `System.Logger` (ADR-0014), which is also the precedent this sets for FR-12's `KeyProvider`: a functional SPI in core, one zero-dependency implementation here, the third-party one in the module allowed to name it. **`Invocation.proceed()` throws `Throwable` because `ProceedingJoinPoint.proceed()` does**, and anything narrower would force the adapter to wrap the measured method's exception — changing what its caller catches, which is the one thing instrumentation must not do. `Callable<T>` fails exactly there: it throws `Exception`, so an `Error` from the measured method would have to be wrapped. **`call(Supplier)` and `run(Runnable)` exist so ordinary code never catches `Throwable` to time a lambda, and they repeat the eight-line measurement rather than delegating** — laundering a `Throwable` back to unchecked form needs a `catch (Throwable) { throw new AssertionError(..); }` branch **no test can reach**, and permanently uncovered code that only satisfies the compiler is a worse cost than a duplicated `finally` under an 85% coverage gate. **The RFC's never-propagate rule is bounded rather than taken literally:** `Throwable` includes `OutOfMemoryError`, so swallowing everything would absorb the news that the VM is dying and leave the measured method looking fine. The catch is `RuntimeException | LinkageError` — the same pair `Validator.fromProvider` catches for the same reason, since a `provided`-scope backend absent from the runtime image fails with `NoClassDefFoundError` and a missing metrics jar must not end a business call. **"At most once per recorder" is a claim about a race, so it is proven as one:** the flag is an `AtomicBoolean` set with `compareAndSet`, and `ExecutionTimeRecorderFailureStress` runs two threads through a recorder that fails on every call and **forbids the two-warning outcome by name** — a plain `boolean` would let both threads read `false`, both warn, and ship the first step of the log flood the rule exists to prevent. Its silent half is recorded rather than hidden: a sink that starts failing after the one warning was spent fails quietly, and a host that needs to know needs a health check, not a log line. **The fallback logs at `DEBUG`, and that is a decision with a stated cost** — one record per invocation at `INFO` would be on by default in exactly the hosts that configured nothing, which is how instrumentation becomes the dominant cost of the method it measures; the price is that timings are invisible until a host enables the level, and the Javadoc says so rather than leaving it to be discovered. **A locale trap sits in the fallback and is closed by test:** `System.Logger` substitutes with `MessageFormat`, which renders a `Long` through the *default* locale, so a raw duration prints `1.234.567` on an Italian JVM and `1,234,567` on a US one — the same measurement in two log lines no expression can parse — so every parameter reaches the logger pre-rendered, and `rendersTheDurationIdenticallyOnEveryLocale` sets both locales and asserts one string, the shape C-03 uses for identifiers. Timing is on `System.nanoTime()`: a wall clock can step backwards under NTP and a negative latency is not a value any backend can interpret. Failures are timed too, in a `finally`, with an `Error` counted as a completion rather than dropped. A blank `name` is rejected at the call site, because an unattributable metric is far cheaper to refuse here than to find in a dashboard later. **The compliance register is untouched on purpose:** nothing on this path carries an argument value — `name` comes from a method signature — so C-01 gains no call site and inventing a row for a property the API cannot violate would dilute the register. **No benchmark, because no NFR names FR-15**; the only performance statements made are structural and negative (nothing on the measured path synchronises; the fallback tests the level before rendering). **One build-config change was forced and is worth flagging, because `clean verify` does not catch it:** `throws Throwable` trips Checkstyle's `IllegalThrows`, and `checkstyle:check` is a *separate* CI goal — so the ruleset gains `SuppressWarningsFilter` + `SuppressWarningsHolder`, which is what makes the `@SuppressWarnings("CheckName")` mechanism AGENTS.md §9 already prescribes actually work; two methods carry it with the reason inline. A path-keyed suppressions file was the alternative and silences a rule for whole files including code written later. **Item 7.x would have hit the same wall**: an AspectJ `@Around` advice must declare `throws Throwable`. Verified on Temurin **17.0.20+8** and **21.0.12+8**: `clean verify` (`Tests run: 239`, up from 216), `-Pjmh,jcstress verify` (**112 results, 0 failed, 0 interesting** — the new harness observing only `1, both ran`), `spotless:check -pl '!d4np-bom'`, `checkstyle:check`, `validate`, and `consistency_lint.py`. The two CI commands that still cannot be reproduced locally are unchanged from item 3.1 — `japicmp:cmp` resolves no plugin (item 8.1) and `-Pcoverage` matches no profile (item 8.2) — size: **S** · route: **standard / medium**
- [x] 3.3 `AuditLog` with a field-level redaction allowlist and `@Sensitive` opt-out (RFC-0002; FR-16) — **done, and it closes Milestone 3 ([ADR-0022](docs/adr/0022-redact-at-capture-behind-a-typed-event.md)).** RFC-0002 arrived with more contract than any other item in this project — four layers, their precedence, whole-token matching, simple values only, depth 3, `record` throws, public accessors only — so the work was the **shape**, and five of those decisions turned out to be load-bearing. **The event is a final class rather than a record, and that is the one structural decision the redaction rule depends on:** a public record's canonical constructor is public, so `AuditEvent` as a record would be a documented way to hand `AuditLog.record` an "audit event" full of plaintext — the guarantee would hold for our capture path rather than for the type. The constructor is package-private and `capture` is the only way to obtain one; the nested `Change` **is** a record, and the split paid for itself as documentation: a record's deserialisation runs its canonical constructor, so `Change` validates for free, while the class needs a `readResolve` to re-run the constructor a stream would otherwise skip (without it, a crafted stream decides whether `changes` is modifiable). **The trap RFC-0002 was written around is reproduced rather than asserted:** with the composite guard removed, the event carries `Credentials[user=alice, password=hunter2]` and `neverRendersACompositeAsText` turns red — the `@Sensitive` marker present, correct, and completely bypassed by one `String.valueOf`. Removing layer 1 instead turns four tests red, including the API-key one. **Markers are read from every declaration site and the type is read from the value**, and the first half is a safety property rather than convenience: a `@Sensitive` written on the field of a JavaBean while the engine looked only at the getter would be present and bypassed, which is the worst available outcome because it looks right in review. The second half closes the `Object`-declared hole — a component whose declared type is `Object` and whose runtime value is a record still gets all four layers. **`@Audited` is deliberately not `@Inherited`:** an inherited permit-everything marker means a subclass that adds a component leaks it by default, which is the exact failure the RFC rejected the denylist for, moved across a class boundary where it is less visible. The consequence is stated rather than discovered — **a proxied entity is not auditable**, because a CGLIB or Hibernate proxy is a generated subclass, and it fails loudly instead of producing an empty record. **RFC-0002's own illustration collides with RFC-0002's own list, and the list wins:** the recursion rule is shown with `@Audited Credentials credentials`, `credentials` is a base-list entry, and layer 1 overrides everything — so that exact field is one `[REDACTED]` row and the non-sensitive `user` inside it disappears too. Found by a failing test written from the RFC's example, pinned by `layerOneBlocksAWholeCompositeRatherThanWalkingIntoIt`, and it is the *correct* over-redaction: blocking the subtree beats publishing the leaves of something the list already named untouchable. **Two loud refusals are additions this item makes rather than the RFC:** a marker on a field no accessor exposes (`@Audited private String userName` beside a `getUser()` is attached to nothing, so the component is missing from every record and the omission surfaces during an audit), and two accessors mapping to one component (`getActive()` beside `isActive()`, where picking one silently decides whether a value is published). **The sink follows item 3.2's SPI precedent with the opposite failure policy** — a failing metrics recorder is swallowed, a failing audit sink is wrapped in `AuditWriteException` and thrown, because a missing timing is a lost number and a missing audit record is a compliance hole found at the next review; the catch is the same narrow `RuntimeException | LinkageError`, so an `OutOfMemoryError` still propagates. `LoggingAuditSink` logs at **`INFO`** where the timing fallback logs at `DEBUG`, and says in its own Javadoc that an application log is a fallback and not a compliance store. **A control the register said was unenforceable turned out to be enforced, and only running proved it:** C-03's row read *"no gate forbids the default-locale overload; an ErrorProne pattern would be the way to close that"* — reintroducing `name.toLowerCase()` in `AuditPolicy` produces ErrorProne's **`StringCaseLocaleUsage`**, which `failOnWarning` turns into a build failure. C-03 moves to ✅ with the gate's scope stated (that overload pair, on the JDK 21+ cells ADR-0009 binds ErrorProne to), and **C-05** is created for FR-16 itself. The threat model's two `AuditLog` rows move from ▢/⚠ to ✅; **no boundary row is added**, because RFC-0002 routed "library → host-supplied audit store" to item 8.6, whose owner is the security-auditor. **Visitor is the first entry in the patterns catalogue's Rejected table** — classic Visitor needs every audited type to implement `accept`, which a library cannot ask of a consumer's domain objects, so the reflective walk reaches the same intent without the contract. **No benchmark and no plan cache, both deliberate:** no NFR names FR-16, so a `ClassValue` cache would be either an unbacked performance claim or an invented budget (item 2.4's precedent), and it stays purely additive if a host ever measures a need. **Collections are refused and that is a contract gap rather than a bug** — the four layers are defined over components and have no vocabulary for element-level policy, while `String.valueOf` on a `List<Credentials>` is the composite trap in bulk; the workaround is a rendered `String` component and the way in is an RFC-0002 amendment. Verified on Temurin **17.0.20+8** and **21.0.12+8**: `clean verify` (`Tests run: 333`, up from 239), `-Pjmh,jcstress verify` on both (**126 jcstress results on 21 and 252 on 17, zero failed, zero interesting**), `spotless:check -pl '!d4np-bom'`, `checkstyle:check`, `validate`, and `consistency_lint.py`. The new harness — two threads capturing different objects through one `AuditLog`, which fails if any of the recursion's per-call state is ever hoisted onto the instance — was proven non-vacuous by flipping its accepted outcome to `FORBIDDEN` and watching `[FAILED] AuditCaptureIsolationStress` take Maven to exit 1. **Two findings from running the Javadoc gate item 8.4 will own:** `StrategyNotFoundException` has carried a dangling `{@value #MAX_KEYS_IN_MESSAGE}` since item 2.4 moved that constant into `KeyDiagnostics` — fixed here in one line, because leaving a knowingly broken reference for 8.4 to trip over is worse than a two-line diff — and **item 2.1's JDK-17 doclint finding recurs on a second type**: `-Xdoclint:all -Xwerror` is clean on 21 and reports 8 spurious "no comment" warnings on 17, all of them components of a `Serializable` record (`AuditEvent.Change` and the pre-existing `ErrorDetail`). The plain-class half of the same warning *is* fixable and was fixed — `AuditEvent`'s five serialisable fields now carry `@serial` — which is what isolates the residue to the record-component case. The two CI commands that still cannot be reproduced locally are unchanged from item 3.1: `japicmp:cmp` resolves no plugin (item 8.1) and `-Pcoverage` matches no profile (item 8.2) — size: **M** · route: **frontier-reasoning / high**

## Milestone 4 — json and jdbc

**Goal:** persistence and serialization capability modules, each owning its own third-party surface.

- [x] 4.0 **RFC-0003 — jdbc transaction semantics and json hardening.** **Done and approved:** [RFC-0003](docs/rfc/0003-jdbc-and-json-contracts.md) was drafted `Proposed` with an empty `approved-by:` — no RFC self-approves (AGENTS.md §6) — and **the owner approved it in session on 2026-08-06**, flipped in a separate commit so the two acts are visible as two acts. The approval **preceded the pull request** rather than following a merge, so nothing about opening it performs the acceptance. The peer-review round still did not run: this acceptance rests on the owner's direct authority, not on review, and that is recorded rather than implied. **4.1–4.5 are unblocked.** **Scope was widened by the owner from the two mandated requirements to all five, and the reason is that neither mandated gap can be closed on its own.** **FR-05 and FR-06 contradict each other, silently:** FR-05 promises a "try-with-resources lifecycle", so the executor takes its *own* `Connection` from the `DataSource` — inside an FR-06 transaction that is a **second** connection, and the work commits outside the transaction the caller opened. Nothing throws, nothing logs, and **every single-statement test passes**, because with one statement there is nothing to observe. **FR-20 defeats FR-21's headline feature:** `FAIL_ON_UNKNOWN_PROPERTIES=false` is right for reading a document you do not own, and over that same mapper "partial mapping" cannot distinguish an omitted field from a **misspelled** one — so a PATCH client believes it changed something it did not. **The transport decision is the load-bearing one: the transactional `Connection` is passed explicitly and there is no ambient current connection.** An ambient `ThreadLocal` is the ergonomic option and was rejected on its failure mode — a `DataSource`-backed executor would change its transactional semantics depending on the calling thread's state, and a hand-off to `CompletableFuture` or FR-09's own `AsyncExecutor` would silently revert to auto-commit. **The residual risk is stated rather than claimed away:** a captured `DataSource`-backed executor used inside the block is legal Java that does the wrong thing, and what makes that acceptable is that it is *visible in the lambda's capture list* rather than hidden in ambient state. **Ambient state is then adopted in exactly one place, and the asymmetry is principled rather than convenient:** nesting is refused via a thread-scoped depth counter, because as *transport* an ambient value fails **open into the wrong behaviour**, while as a *detector* it fails **open into the documented behaviour** — and refusing matters because a nested call would otherwise take a second connection and wait on locks the outer transaction holds, on one thread, so a modest pool hangs with no error to read. **`Result.Err` returned by the callback commits**, which is the decision most likely to be questioned: interpreting it as a rollback signal would give a core type a second meaning in one method and is ambiguous for a nested `Result`, so the rule is *the exception channel demarcates the transaction, the value channel does not* — and FR-18 already names the type for a rule violation that aborts a use case. **ADR-0012's prediction did not hold, and it is recorded rather than quietly falsified:** that ADR named item 4.4 as a plausible first `Result<Unit>` consumer, but a transaction runner's failures — no connection, commit refused, a deadlock the database detected — are infrastructure faults that RFC-0001's own table assigns to the unchecked shape, so `inTransaction` returns `T` and throws. The cost of the wrong guess is zero, which is exactly the property ADR-0019 was buying when it declined to wait for this call site. **Three name collisions were caught before the code by turning ADR-001's naming-consequence rule into a test — rename where a wrong choice *compiles and diverges*, keep where it cannot compile:** `DataAccessException` becomes **`JdbcAccessException`** (Spring ships one for the same job, so a host's `catch` silently fails to match and the exception escapes to the 500 fallback — a divergence that compiles), `Sort` becomes `PageSort` (the weak case, taken because a new type has no users to break), and `RowMapper` **keeps its name** (a wrong import cannot compile, and the two are behaviourally identical one-method interfaces). **FR-21's null-vs-absent question is answered beside the value rather than inside it:** `{"a":null}` and `{}` are different documents that produce the same POJO, and every obvious encoding collides with a rule already held — ADR-0011 calls an `Optional` field an anti-pattern and deliberately left `@Nullable` declaration-only, and ADR-0012 forbids `null` in a `Result` — so `PartialUpdate<T>` carries the set of property names the document actually contained. Nothing is widened, no field type changes, and it composes with records, which is what both rejected encodings could not do. **The same object resolves the FR-20 collision:** `readPartial` **rejects** an unknown property while the mapper stays lenient, because leniency tolerates an unknown *addition* and strictness must refuse an unknown *instruction* — two jobs, one mapper, and a per-operation check instead of a mapper-wide flag. **FR-20 gains a second setting the requirement never named, and it is security-relevant:** `INCLUDE_SOURCE_IN_LOCATION` is disabled, because Jackson embeds a snippet of the source document in a parse error and FR-19 turns that into an RFC 7807 body — control **C-01** — set explicitly rather than trusted as a default, for the reason RFC-0001 wrote UTF-8 out instead of calling `Charset.defaultCharset()`. **`JsonMapper` does not expose its `ObjectMapper`**, since one `activateDefaultTyping` call re-opens the CVE class the requirement exists to close (ADR-0022's rule that a guarantee a consumer can switch off is advisory) — and the thread-safety claim and the missing getter **hold each other up**, because "thread-safe once configured and never reconfigured" is only true while nothing can reconfigure it. **Two findings came from reading FR-07 closely rather than from designing it.** Its "violations throw `IllegalArgumentException` **at construction**" attaches to the page/size bounds and *cannot* attach to the whitelist: `PageRequest` is built at the HTTP edge and the set of sortable columns is the repository's knowledge, so the whitelist is supplied at validation time. And a whitelist violation throws **`ValidationException`, not `IllegalArgumentException`** — FR-19 maps validation to **400** and has no row for `IllegalArgumentException`, so the latter would fall to the **500** fallback and report client-supplied input as a server fault, the same misattribution ADR-0015 recorded. **The whitelist compares exactly and case-sensitively**, because SQL identifier folding is vendor-specific (PostgreSQL lower, Oracle upper, MySQL filesystem-dependent) and normalising would pick a vendor — so **C-03 gains no consumer, deliberately**, where RFC-0002 made it security-load-bearing. **`PageResponse<T>` is not `Serializable`** for ADR-0015's reason (it would be serialisable only when `T` happened to be), `totalPages()`/`hasNext()` are derived rather than stored so they cannot disagree with their inputs, and `totalElements` is a `long` because a row count is the quantity that grows past 2.1 billion. **The surface is stated as a number now rather than discovered at item 8.1: 15 new public types**, and the two descriptors answer one question in opposite directions — `d4np-jdbc` takes **`requires transitive java.sql`** because `Connection` and `ResultSet` appear in interfaces the *consumer implements*, while `d4np-json` keeps Jackson **non-transitive**, which is only consistent because FR-21 mints its own `JsonTypeToken` instead of exposing `TypeReference`; that mint is what keeps the descriptor honest rather than aspirational, and it stops a Jackson major version that moved the type from becoming our MAJOR bump. **No new threat-model boundary is needed** — B3 already covers `jdbc → DataSource`, and both the handed-over `Connection` and the untrusted document sit inside B1 — which is stated explicitly because RFC-0002 routed a missing boundary to item 8.6 and a reader will look for the same here. Three ▢ rows become implementable; three obligations are filed on **item 7.1** (two FR-19 rows, plus the rule that the fallback handler must not render a cause chain's `getMessage()`, since that is where the driver's SQL and Jackson's snippet live). **[ADR-0023](docs/adr/0023-the-owner-approves-this-projects-rfcs.md) closes a governance deviation that had drifted in both directions unrecorded:** `rfc_check.py` requires `approved-by: tech-lead`, RFC-0001 wrote that and passes, RFC-0002 wrote `owner @danielPoloWork` and **fails the gate today** — and the gate's own expectation contradicts AGENTS.md §6, because `tech-lead` is the *authoring* role, so satisfying it literally is self-approval. The owner is the approver, the FAIL is expected output, and at the third RFC "nobody noticed" stops being plausible. **Three back-fills, each a one-token fix in a line this item was already editing:** `refs.rfcs` gains **RFC-0002** (design step 6 was skipped for item 3.0, although the manifest's own comment promised the list would grow), `manifest_rev` moves 4 → 5 (it is the optimistic-concurrency counter for the *file*, per `render.py`, so a content write moves it even with no transition), and the ADR index count read **24** when it should have read 26 — ADR-0021 and ADR-0022 added their rows without incrementing it, because `adr-index` asserts the bijection and every status but **not** that number. **The spec re-render was verified rather than asserted:** a fresh `render.py --out` of the amended manifest is **byte-identical** to the committed `docs/specs/01_spec_utils.md`, which is the ADR-0010 property that item 1.5 learned the hard way. **`authority_check.py` was run per role rather than once, and the answer is more useful than an override:** the item legitimately spans two roles — `tech-lead` owns `docs/rfc/**` and is DENIED the other six paths, while **`enterprise-architect` covers all eight** — and one human holds both. Verified on Temurin **17.0.20+8** and **21.0.12+8**: `clean verify` (`Tests run: 333`, unchanged — this item ships no production code), `-Pjmh,jcstress verify` on both, `spotless:check -pl '!d4np-bom'`, `checkstyle:check`, `validate`, `consistency_lint.py`, and `traceability.py ROADMAP.md RFC-0001 RFC-0002 RFC-0003` → OK. **No `CHANGELOG.md` entry, deliberately:** nothing user-visible changed, and item 3.0 added one only because it shipped `Unit`. The two CI commands that still cannot be reproduced locally are unchanged from item 3.1 — `japicmp:cmp` resolves no plugin (item 8.1) and `-Pcoverage` matches no profile (item 8.2). **ROUTE-MISMATCH recorded:** routed `frontier-reasoning / high`, run on this host's `standard` tier — size: **M** · route: **frontier-reasoning / high**
- [x] 4.1 `JsonMapper` hardened configuration — JavaTimeModule, `FAIL_ON_UNKNOWN_PROPERTIES=false`, **no default typing** (the polymorphic-deserialization CVE class configured away) (FR-20) — **done, and it is the first production code this project has placed outside `d4np-core` and the first compile-scope third-party dependency in the repository.** Core's one edge is `provided` and `requires static`, so nothing before this item shipped a dependency a consumer actually resolves; ADR-001 puts Jackson here precisely so that everyone else keeps not resolving it. **RFC-0003 arrived with two sentences that cannot both be literally true, and the code is where one had to be picked:** §FR-20 pays for the absent getter by letting construction take a list of Jackson `Module`s, while §Consequences justifies `d4np-json`'s **non-transitive** `requires com.fasterxml.jackson.databind` as *"only consistent because no Jackson type appears in a signature"* — and `com.fasterxml.jackson.databind.Module` is a Jackson type in a published signature. The RFC's own defence ("the type token is what keeps that descriptor honest") answers the FR-21 question it was written for and does not reach this one. **[ADR-0024](docs/adr/0024-take-a-jackson-type-in-one-signature.md) keeps both the signature and the non-transitive edge, and replaces the claim with a measurement rather than a better argument:** a consumer module was compiled *and run* against the built artifacts — `create()`/`readValue`/`writeValueAsString` need no `requires` of their own, `withModules(List.of())` needs none either, and `withModules(List.of(new SimpleModule()))` fails with *"package com.fasterxml.jackson.databind.module is not visible"* until the edge is declared. **The intuition it corrects is the one worth keeping:** javac demands the read edge where the *consumer's own source names* the type, not where an invoked signature mentions one — confirmed with a second, synthetic two-module probe — so the separate method name is an API choice and buys nothing structurally, and the consumer that builds a `Module` was always going to name one. `requires transitive` would have saved that consumer a single line while making a Jackson major version our MAJOR bump, the cost that already killed exposing `TypeReference`. **`findAndRegisterModules()` is recorded as the one alternative that keeps the RFC's sentence literally true, and it loses on the property this type exists for:** service loading registers whatever is on the classpath, which is implicit configuration inside the one class whose value is that its configuration is explicit. **A fifth setting was added beyond FR-20's three and the RFC's fourth, so it gets its own record ([ADR-0025](docs/adr/0025-render-java-time-as-iso-8601.md)) instead of being folded in as obvious:** registering the `JavaTimeModule` makes `java.time` serialisable but does not choose a representation, and at Jackson's default an `Instant` is an epoch number where every host in the compatibility matrix emits ISO-8601 — one service, two wire formats on one field, with nothing to read in the code. Amending the approved RFC was rejected on item 2.5's precedent: a document that changes to match the code it produced stops being a check on that code. **The explicit-restatement rule stopped being inherited and became measured:** `INCLUDE_SOURCE_IN_LOCATION` defaults to **enabled in Jackson 2.15.3 and disabled in 2.22.1** — read out of both releases' sources — and both are inside the supported matrix, because a Spring Boot 3.2 host's dependency management downgrades this library to its own managed Jackson. The explicit `disable` is the only reason those two hosts behave the same way, which is RFC-0001's UTF-8 argument with a counter-example attached. **The version pin inverts item 3.1's reasoning, and the inversion is the point:** at `provided` scope the host's copy wins, so Bean Validation was pinned to the matrix *floor*; at `compile` scope ours is what a plain host resolves, so Jackson is pinned to the **latest stable, 2.22.1** — an old pin would ship an unpatched Jackson to exactly the hosts with no dependency management. The floor is then paid by the *code*, and that is a claim, so it is run: `mvn -pl d4np-json -am -Djackson.version=2.15.3 clean verify` is green with all 21 tests, and the command is in the module POM so item 4.2 inherits the obligation rather than the folklore. **It earned its keep on the first run**, catching an assertion coupled to a Jackson version: 2.22 renders the suppressed source location as ``REDACTED (`StreamReadFeature.INCLUDE_SOURCE_IN_LOCATION` disabled)`` and 2.15.3 as `UNKNOWN`, so the test now asserts the absence of the snippet rather than the presence of a marker. **The BOM is imported instead of three pinned versions, and that too was verified rather than assumed:** `jackson-annotations` is at **2.22** while `databind` is at **2.22.1** — Jackson dropped the patch digit from annotations in 2.20, as its own BOM comment says — so a hand-pinned `${jackson.version}` across the set does not merely risk `dependencyConvergence`, it fails to resolve. **The security claim is asserted through behaviour and every dangerous setting is paired with a companion test that runs the same input the other way**, so a payload cannot go inert and leave a green assertion behind: a gadget-shaped document is read as data by `JsonMapper` and **does** instantiate the class it names under a mapper with default typing on; the same truncated credential document leaks `hunter2` through a mapper with `INCLUDE_SOURCE_IN_LOCATION` enabled; and `java.time` fails outright without the module. **The structural half of FR-20 is tested structurally** — `publishesNoHandleToTheConfiguredMapper` walks the reflected surface and fails on any public constructor or any method mentioning an `ObjectMapper`, because a getter added later would compile and pass every other test in the file. **Running C-01 narrowed one of RFC-0003's two defences, and it is recorded rather than smoothed over:** disabling `INCLUDE_SOURCE_IN_LOCATION` keeps the source *snippet* out of the cause — proven — but `InvalidFormatException` quotes the rejected value in the body of its own message, which no Jackson setting governs. So the exception this library throws is clean and **its cause is not**, which turns the RFC's advice to item 7.1 (*the fallback handler must not render a cause chain's `getMessage()`*) into a requirement with a named test behind it. C-01 gains its second enforced call site; the threat model's polymorphic-deserialization row moves ▢ → ✅ and **no row is added**, per RFC-0003 §Consequences. **Three smaller contracts beyond the RFC's text:** the literal `null` document is refused rather than returned, because it is valid JSON that Jackson answers with a `null` reference and C-02 says this library never hands one back; a document-supplied property name — which only a `Map` target produces — is bounded at 64 characters **and stripped of ISO control characters**, since RFC-0003's length bound does nothing about a key containing a newline, which is a log-injection primitive rather than a formatting problem; and `JsonDiagnostics` duplicates core's `KeyDiagnostics` truncate-and-cap shape rather than sharing it, because that type is package-private in `it.d4np.utils` and exporting an implementation detail across a module boundary to save fifteen lines is the worse trade. **The name collides with Jackson's own `com.fasterxml.jackson.databind.json.JsonMapper` and is kept**, under the same test item 4.0 applied to three names: the wrong import cannot diverge silently because that type has no `create()`, so the mistake fails to compile — and the cost is paid inside our file, which names Jackson's builder by its fully qualified name exactly as `Validator` names `jakarta.validation.Validator`. **Scope is bounded in the reversible direction:** `String` in, `String` out, `Class<T>` target — a generic target needs FR-21's type token and deep conversion is a second operation, both item 4.2 — and `byte[]`/stream overloads are omitted because adding one later is MINOR while removing one is MAJOR. **A consumer obligation is documented and demonstrated rather than asserted:** Jackson reflects over a consumer's own types, so a modular consumer must export or open the package holding them, and the probe reproduces the failure with a message naming precisely what to add; this library opens nothing on a consumer's behalf, the line RFC-0002 drew for FR-16. Verified on Temurin **17.0.20+8** and **21.0.12+8**: `clean verify` (`Tests run: 333` in core, unchanged, plus **21 new** in `d4np-json`), `-Pjmh,jcstress verify` on both (**126 jcstress results on 21 and 252 on 17, zero failed, zero interesting**), `spotless:check -pl '!d4np-bom'`, `checkstyle:check`, `validate`, `consistency_lint.py`, and `-Xdoclint:all -Xwerror` **clean on both toolchains** — item 3.3's JDK-17 record-component noise does not recur, because nothing published here is a `Serializable` record. **No benchmark and no jcstress harness, and for once the RFC says so directly:** no NFR names FR-20, and the thread-safety claim reduces to Jackson's own guarantee over an object this class never mutates, so a harness would be measuring Jackson (item 3.1's reasoning for `Validator`). The two CI commands that still cannot be reproduced locally are unchanged from item 3.1 — `japicmp:cmp` resolves no plugin (item 8.1) and `-Pcoverage` matches no profile (item 8.2). **ROUTE-MISMATCH recorded:** routed `frontier-reasoning / high`, run on this host's `standard` tier — size: **S** · route: **frontier-reasoning / high**
- [x] 4.2 `ObjectMapperExtensions` once RFC-0003 specifies the operation set (RFC-0003; FR-21) — **done: `convert` in two overloads, `readPartial`, plus `PartialUpdate<T>` and `JsonTypeToken<T>` — four new public types over the mapper item 4.1 built, and no new module edge or dependency.** **The item's real finding is a leak channel RFC-0003's own wrapping rule does not reach, and it is the kind that ships:** the RFC phrases the rule against `JsonProcessingException`, *"which is checked"* — and FR-21's conversion path never raises one. `ObjectMapper.convertValue` catches Jackson's `IOException` internally and rethrows an **unchecked** `IllegalArgumentException` carrying `e.getMessage()` verbatim, which quotes the rejected value: `Cannot deserialize value of type int from String "hunter2"`. A rule phrased against the checked exception is a rule the compiler enforces; nothing in the language, the build or any gate here would have flagged a `convert` that let the unchecked one through — it would have compiled, passed every test not looking for it, and shipped a **C-01 violation on the newest public method**, landing on FR-19's **500** fallback rather than 400 into the bargain (ADR-0015's misattribution, arriving by another route). **[ADR-0026](docs/adr/0026-rewrite-jacksons-unchecked-conversion-failure.md) catches and rewrites it, and restates the rule in the form that actually holds** — *no exception leaves this module carrying text this library did not write*, whatever its checked-ness — which is an obligation **item 4.4 inherits**, because a driver's `SQLException` message carries the SQL it was given. The narrower alternative (rethrow only when the cause is a `JsonProcessingException`) was rejected on what it optimises for: it preserves a host serializer's exception type at the cost of a conditional whose false branch *is* the leak. **The second decision is the one a record would have made for us, silently.** RFC-0003 sketched `PartialUpdate<T>` as a final class and a record implements that shape exactly — except that a record's generated `toString()` prints its components, so `PartialUpdate[value=Credentials[user=ada, password=hunter2], …]` is one `log.debug("patch {}", update)` away, written by an author not thinking about disclosure at all. **[ADR-0027](docs/adr/0027-a-partial-update-renders-names-not-values.md) makes it a class with a hand-written rendering that names the value's *type* and lists its property names**, and keeps construction package-private, which a record's canonical constructor could not. `equals` still reads the value — comparing is not disclosing — and that asymmetry is stated rather than left to be noticed. **The names it renders are bounded, for a case easy to miss:** with a `Map` target every document key is a *known* property, so the present-name set is client input rather than the target's vocabulary, and a key holding `\r\n` folds one log line in two. They are bounded in `toString()` and deliberately **not** in `presentProperties()`, where a truncated name would answer `isPresent` with a wrong `false` — trading a log concern for a correctness bug. **A test caught a confident wrong answer before review did.** `readPartial` reports *every* unknown property rather than the first Jackson trips on, computed by subtracting the exception's known-property ids from the document's names — and `getKnownPropertyIds()` belongs to the type Jackson was populating, which for a **nested** offender is the nested type. A document with one bad field inside `lines` reported both top-level properties of `Basket` as unknown. The subtraction now runs only when the failure's path is at the top level; below it Jackson has already named the single offender. **Two refinements of RFC-0003 are recorded here rather than given their own ADR, because they narrow the RFC rather than diverge from it:** the unknown-property refusal applies at **every depth** while `presentProperties()` reports the **top level only** — the RFC bounds the *set*, not the *check*, and a safety property with a shallow end is not one — and `JsonTypeToken` **refuses a token over a type variable at construction**, since `new JsonTypeToken<T>() {}` inside a generic method captures what erasure already discarded and Jackson would resolve it to the bound, handing back a `LinkedHashMap` that fails at a cast somewhere else. Jackson's own `TypeReference` tolerates it; refusing is the same reasoning that refuses the literal `null` document. **FR-21's strictness and FR-20's leniency coexist per operation, and that is asserted rather than described:** `leavesEveryOtherReadLenient` refuses the partial update and then reads the *same* document through `readValue` successfully, which is the property flipping the mapper-wide flag would have destroyed. **The type token earns its keep by measurement, in the companion-test discipline item 4.1 established:** `convertsIntoAGenericTarget` gets real `Sku`s, and `theRawTargetLosesTheElementTypeAndSaysNothing` shows the untyped call **succeeding** with a list of `LinkedHashMap`s — the failure the token exists to move from the caller's next method call to compile time. C-01's pairing is the same shape: our message is clean, and `theSameConversionLeaksThroughRawJackson` proves raw Jackson leaks `hunter2` on that exact input, so the assertion cannot go vacuous behind a conversion that stopped failing. **`readPartial` reads the document twice** — once as a tree for the names, once as the target — and the reason is the compatibility matrix, not laziness: the one-pass form needs a Jackson API whose shape has moved across 2.15.3 → 2.22.1, and **no NFR names FR-21** (RFC-0003 §Scalability budgets), so the stable form wins over the fast one on a request payload. **One ErrorProne suppression, scoped and argued:** `TypeParameterUnusedInFormals` on the private helper both `convert` overloads share — the check is about a *published* method whose caller infers `T` from nothing, and here every caller binds `T` from its own target parameter; the helper exists so the ADR-0026 rewrite happens in one place rather than twice. **The pattern catalogue gains a rejection rather than an adoption:** Message Translator (EIP) matches `convert`'s intent almost exactly and is refused on vocabulary — it presumes a message, a channel and an endpoint, and naming a two-argument static method after it sends the next reader looking for a pipeline (AGENTS.md §8.1 and §8.3). The super-type-token idiom is not in the canonical taxonomy and is therefore not catalogued under an invented name. C-01 gains its **third** enforced call site and stops being only about exceptions; the threat model's information-disclosure row gains the unchecked channel and **no row is added**, per RFC-0003 §Consequences. Verified on Temurin **17.0.20+8** and **21.0.12+8**: `clean verify` (`Tests run: 333` in core, unchanged, and **52** in `d4np-json` — **31 new**), `-Pjmh,jcstress verify` on both (**126 jcstress results on 21 and 252 on 17, zero failed, zero interesting**), `spotless:check -pl '!d4np-bom'`, `checkstyle:check`, `validate`, `consistency_lint.py`, and `-Xdoclint:all -Xwerror` clean on both toolchains. **The floor build item 4.1 wrote into the module POM was inherited rather than assumed:** `mvn -pl d4np-json -am -Djackson.version=2.15.3 clean verify` is green with all 52 tests, which is what says `getPath()`, `getKnownPropertyIds()` and `readerFor(..).with(..)` mean the same thing at both ends of the supported matrix. The two CI commands that still cannot be reproduced locally are unchanged from item 3.1 — `japicmp:cmp` resolves no plugin (item 8.1) and `-Pcoverage` matches no profile (item 8.2); `javadoc:javadoc` additionally has to be scoped to `-pl d4np-core,d4np-json`, because `d4np-jdbc` has no sources yet and javadoc fails on an empty module. **No ROUTE-MISMATCH, for the first time in this milestone:** the item routes `standard / medium` and this host's tier *is* `standard`, so the mismatch 4.0 and 4.1 each had to record does not arise — the routing legend and the host agree where the item is not `frontier-reasoning`. **`authority_check.py` was run per role and the answer is the one item 4.0 recorded:** the item legitimately spans two roles, and further apart than 4.0's did — of the **17** paths it touches, `tech-lead` may write exactly **one** (`docs/patterns/README.md`) and is DENIED the other sixteen, including the production sources and both new ADRs, while **`enterprise-architect` covers all 17**. One human holds both, so it ships as one PR and the split is recorded rather than worked around. — size: **S** · route: **standard / medium**
- [x] 4.3 `SimpleJdbcExecutor` — parameterized statements only, by construction; no string-concatenation overload exists (FR-05, NFR-03) — **done: `SimpleJdbcExecutor`, `RowMapper<T>` and `JdbcAccessException` open `d4np-jdbc`, and it is the first module here to publish an API with no third-party dependency at any scope a consumer resolves** — core comes close but has one `provided` edge behind `requires static`, whereas the JDBC API ships inside the JDK, so a consumer takes exactly one JAR. **The headline decision was RFC-0003's to make and this item's to test, and testing it reversed the outcome.** The RFC singled NFR-03 out as *"the one performance gate in this project that can be a real CI gate today"*, and the reasoning is right and survives: it is a **relative** comparison between two arms in one JMH invocation, so a slow runner slows both and the machine cancels — which is exactly the problem item 8.3 exists to solve for NFR-01's 2 ns/op and NFR-06's 400 MB/s, and exactly the problem this budget does not have. What the RFC could not know is the **sample size**, because the benchmark did not exist yet. Three consecutive repetitions at CI's own settings (1 fork, 1 warmup, 1 measurement iteration) on one idle machine put the ratio at **0.56, 0.66 and 1.27** — a **2.3x spread across a 1.10 threshold**, so a gate wired today would fail roughly a third of builds that had regressed nothing, which is the specific way a project teaches its team that red CI is normal. So the gate is **filed as new item 8.8 rather than shipped**, and the filing carries the three numbers instead of an intuition; 8.8 needs fork and iteration counts, **not** item 8.3's stable runner, and merging the two would bury the distinction the RFC drew. **The budget itself is met at report grade, and "with room" would be the wrong way to say it** (5 forks x 10 iterations, both toolchains): the ratio is **1.071 on JDK 21 and 1.100 on JDK 17** against the ≤ 1.10 ceiling on the raw mean, and **0.952 / 1.051** on the median of per-fork means — the first budget in this project whose verdict depends on which statistic is used, since NFR-03 says "≤ 10% overhead" without saying of what — executor 0.791 vs hand-written 0.739 ms/op on 21, and 0.788 vs 0.716 on 17, and the ~5% steady-state gap is precisely the *one virtual call per row* RFC-0003 predicted when it chose a caller-supplied mapper over reflection ([report](docs/benchmarks/2026-08-08-jdbc-row-mapping.md)). The executor is measured doing *strictly more* work than the arm it is compared against, which makes the result conservative rather than flattering: it returns `List.copyOf(..)` where the hand-written loop returns the mutable `ArrayList` it filled. **A third arm is measured and deliberately excluded from the budget**, because leaving it out would invite the reader to assume the measured arm includes pool acquisition: a `DataSource`-backed executor, which opens and closes a real connection per call, runs about **2.3x on JDK 21 and 2.6x on JDK 17 the borrowed-connection arm**. That number is not a regression and NFR-03 does not ask about it — it is the number that explains why FR-06 hands its callback a `Connection` rather than a `DataSource`, and why the `@apiNote` RFC-0003 asked this item to write matters at all. **[ADR-0028](docs/adr/0028-the-fr-05-operation-set-and-what-it-refuses.md) settles the operation set the RFC explicitly delegated** — `query` to an unmodifiable `List<T>`, `queryOne` to `Optional<T>`, `update` to the affected count, no batch — **and the decision inside it that a reviewer would not have questioned is `queryOne` refusing a second row.** Returning the first is what every convenience API does and it is wrong here in the quiet way: a duplicate in a column the schema was supposed to keep unique becomes an application reading one of two records at random, forever, with nothing in a log. **The refusal then forced a second decision, and the tempting answer was rejected:** `JdbcAccessException` was specified to carry a `SQLState` and a vendor code, and no driver raised anything here. SQLState **21000** is the SQL standard's own *cardinality violation* — PostgreSQL raises exactly it for this condition — and fabricating it would have made every instance carry a code. It is refused because the field means *what the driver said*, so writing our own conclusion into it leaves a consumer unable to tell the two apart; the cause-less form reports empty state, zero vendor code and a `null` cause, **three signals that agree**, on `JsonConversionException`'s precedent from item 4.1. **[ADR-0029](docs/adr/0029-annotate-the-varargs-so-a-null-parameter-compiles.md) records a published annotation that has to say the wrong thing, and it is the finding worth carrying past this milestone.** A `null` bind parameter is a *value* — SQL `NULL` — so `update(sql, id, null)` has to be writable, and under NullAway it does not compile: measured, five findings on the first compile of the suite, and the same would hit **every consumer** whose own code NullAway checks. Annotating the varargs fixes the call site and creates a smaller permanent problem, because `it.d4np.utils.Nullable` is a **declaration** annotation by ADR-0011's deliberate choice and a declaration annotation on a varargs parameter attaches to *the array*. So `@Nullable Object... params` tells NullAway the array may be null, which the contract denies — the array is rejected at run time — and is the only spelling that makes the element case compile. **This is the same expressiveness gap RFC-0003 met from the other side one item ago**, where `Map<String, @Nullable Object>` was inexpressible for FR-21 and `PartialUpdate` had to carry the distinction beside the value. One decision surfacing twice, in two modules, within a week: ADR-0029 keeps ADR-0011 standing and states that **a third call site should reopen the `TYPE_USE` question on its own terms** rather than have it settled in passing. **Both structural guarantees are asserted by running rather than by reading, because in both cases the readable version is the one that goes stale.** *"The connection is closed"* is not a property of a `finally` existing — a real pool hides both halves, handing back connections it already had and closing nothing on `close()` — so the tests use a hand-written `DataSource` that opens a genuinely new connection per call and asks **the driver** whether each one is closed. *"No plain `Statement` is ever created"* is not a property of the source not containing `createStatement`: every operation runs through a `java.lang.reflect.Proxy` over `Connection` that fails the test the moment it is called, so a concatenating overload added later cannot pass wherever it was written. Beside them, `treatsAnInjectionPayloadAsData` binds `'; drop table orders; --` and finds it stored as a string with the table intact. The threat model's *SQL altered via string concatenation* row moves ▢ → ✅ **with the residual stated**: a caller that concatenates the `sql` string *before* passing it in is beyond what any Java API can stop, and a column name cannot be a bind parameter in any database, which is item 4.5's row rather than this one's. **C-01 gains a fourth call site and, more usefully, a second independent provider.** H2's own `SQLException` for a unique-constraint violation carries **both the bound parameter and the entire `insert` statement** in its message — asserted, not assumed, by `theDriversOwnMessageStillCarriesBoth` — while `JdbcAccessException`'s message is a fixed operation label plus the SQLState and vendor code. Jackson demonstrated the same thing in item 4.1 through a completely different mechanism, which is what turns item 7.1's *"never render a cause chain's `getMessage()`"* from a Jackson workaround into a property of the boundary handler. **Three smaller contracts, each with a reason rather than a preference:** a `RowMapper` returning `null` raises `IllegalStateException` and **not** `JdbcAccessException`, because nothing about the database failed — the shape `Lazy` gives an initializer that returns `null`; a `DataSource` that answers with `null` is refused with a named message rather than dereferenced into an opaque NPE (control C-02); and a `null` parameter binds through `setNull(.., Types.NULL)` rather than `setObject(.., null)`, on JDBC's own documented advice that not every database accepts an untyped NULL. **No jcstress harness, and spec §6 is why that had to be said out loud** rather than left as an absence: the section makes a thread-safety claim without a named harness not a claim, and here there is no shared mutable state to stress — a harness over `on(DataSource)` measures the host's pool and one over `on(Connection)` asserts a property the class explicitly disclaims. Items 3.1 and 4.1 reached the same conclusion over Bean Validation and Jackson; **item 4.4 does owe one**, for FR-06's thread-scoped nesting detector, and that contrast is what makes its obligation clear. **Nothing in this module names a driver type at any scope — production, test or benchmark.** H2 is test scope and is reached exclusively through `DriverManager` and a `jdbc:h2:mem:` URL, with a hand-written `DataSource` in place of H2's own, which is what makes spec §3's *"JDBC API only, no drivers"* checkable instead of aspirational. `requires transitive java.sql` is the repository's only transitive edge, and the asymmetry with `d4np-json`'s non-transitive Jackson is principled: ADR-0024 measured that a consumer needs a read edge where **its own source names the type**, and a consumer *implements* `RowMapper`, whose one method takes a `ResultSet` and throws an `SQLException`. **One back-fill, in a row this item was already editing:** the Spec Coverage Map's **§3** cell listed fourteen items and not **4.3**, although 4.3 is the item that fulfils NFR-03 — the row's own subject. `check_spec_map` asserts a non-empty cell and a recognised glyph, never that the cell is complete, so an under-reporting row stays green forever; 8.8 joins it in the same edit. Verified on Temurin **17.0.20+8** and **21.0.12+8**: `clean verify` (`Tests run: 333` in core, unchanged, plus **26 new** in `d4np-jdbc`), `-Pjmh,jcstress verify` on both, `spotless:check -pl '!d4np-bom'`, `checkstyle:check`, `validate`, `consistency_lint.py`, and `-Xdoclint:all -Xwerror` clean on both toolchains at `-pl d4np-core,d4np-json,d4np-jdbc`. **The scoping caveat item 4.2 recorded shrinks by one module and does not close, which is the accurate version of a sentence this entry first got wrong:** javadoc fails an empty module with *"No public or protected classes found to document"*, `d4np-jdbc` stops being one today, and **five still are** — `d4np-concurrent`, `d4np-security`, `d4np-spring-adapter`, `d4np-lock-redisson` and `d4np-test`, each holding a `module-info.java` and nothing else until its own milestone lands. The unscoped run is item 8.7's to make pass, and this is one module less for it. **Two JDK documentation tags had to be declared before this item's own Javadoc would build**, which is a build change and is therefore stated rather than slipped in: `@apiNote` and `@implNote` are the JDK's own tags and the standard doclet rejects both with `error: unknown tag` unless told they exist. RFC-0003 §FR-06 asks item 4.3 for an `@apiNote` on `on(DataSource)` by name, so the tag is used and `maven-javadoc-plugin` is pinned and configured with the three-tag block in `pluginManagement` — measured both ways: two errors on both toolchains without it, clean with it. It **configures** the tool and deliberately does not **gate** it; wiring the CI job and repairing the `{@value}` reference stay item 8.7's, whose own text warns that either half alone is half a control. The two CI commands that still cannot be reproduced locally are unchanged from item 3.1 — `japicmp:cmp` resolves no plugin (item 8.1) and `-Pcoverage` matches no profile (item 8.2). **No ROUTE-MISMATCH is claimed and the routing is worth one line:** the item routes `frontier-reasoning / high`, this host's catalogue tier is `standard`, so the mismatch items 4.0 and 4.1 recorded applies here too — recorded once, and the session was run at maximum effort to compensate. **`authority_check.py` per role:** of the **24** paths this item touches, `tech-lead` may write **three** — `docs/patterns/README.md` and both benchmark documents, which is a wider slice than item 4.2's one, because this is the first item since 2.3 to produce a benchmark report — and is DENIED the other twenty-one, including every production source and both ADRs; **`enterprise-architect` covers all 24**, and one human holds both — size: **M** · route: **frontier-reasoning / high**
- [x] 4.4 `JdbcTxRunner` once RFC-0003 specifies isolation and rollback semantics (RFC-0003; FR-06) — **done: `JdbcTxRunner`, `TxIsolation`, `TxCallback<T>` and `TxVoidCallback`, plus the first jcstress harness this project has owed outside core.** RFC-0003 pinned this contract harder than any other in the milestone — connection ownership, isolation restoration, the nesting refusal, a four-row failure-while-failing table — so the item's findings are the three places the *specified* design does not survive contact with Java. **The first is a surface that does not compile, and it was measured before it was decided.** RFC-0003's §FR-06 table gives both run methods the same name, `inTransaction`, one returning `T` and one `void`. Two overloads over two functional interfaces make an implicit lambda whose body is a **statement expression with a value** applicable to both, and the most-specific rules do not break the tie. Six call shapes were compiled against the pair: five resolve, and the one that does not is `runner.inTransaction(c -> executor.update(sql, id))` — the most idiomatic single-statement transaction there is, answered with *reference to inTransaction is ambiguous*, which names neither the missing brace nor anything useful. **[ADR-0032](docs/adr/0032-name-the-void-transaction-form-differently.md) renames the void form to `inTransactionWithoutResult`**, which is Spring's own fix — `TransactionTemplate` gained `executeWithoutResult` in 5.2 for exactly this, in the very type FR-06 points Spring users at. The six shapes are kept as a compiled fixture rather than deleted, so a future overload cannot bring the ambiguity back unnoticed. **The rename immediately caught a second defect, which argues for the tests rather than for the rename:** the runtime message for a null return still read *use inTransaction(TxVoidCallback)*, naming a method that no longer existed, and a test failed on the string. A message pointing at a method nobody can call is rot no compiler reports. **The second is two approved documents that cannot both be read literally.** RFC-0003 says the runner *rethrows the original unchanged* — it says so while arguing about `Error`, where the sentence is unarguable — while RFC-0001 forbids any published method from declaring a checked exception, and `TxCallback.run` declares `throws Exception` because JDBC's own methods do. A body may therefore throw something `inTransaction` cannot declare. **The literal option is sneaky-throw, and it was rejected on a measurement rather than on taste:** with nothing declared, `catch (SQLException e)` around the call is a *compile error*, so the exception a transaction body is most likely to throw becomes the one the caller cannot name. **[ADR-0030](docs/adr/0030-the-two-channels-out-of-a-transaction-body.md) translates in four cases and publishes the table on the method:** a `RuntimeException` and an `Error` propagate as the same instance (asserted with `isSameAs`, because a wrapper of the right class would pass a weaker check), a `SQLException` becomes `JdbcAccessException` — the translation every other operation in this module already makes, so a body doing raw JDBC fails in the same shape as one going through the executor — and anything else checked travels in `UndeclaredThrowableException`, the JDK's own name for this situation, adding **no public surface**. The same record refuses a **`null` return**, rolling the transaction back before it does: the return is implicitly `@NonNull` in an annotated package, so allowing it would be lying to the analyser, and committing work whose result is then rejected is the reading with nothing to recommend it. **`Unit` finds its first use in this library here, and not where ADR-0012 predicted:** that ADR named this call site as a plausible first `Result<Unit>` and RFC-0003 recorded the guess as wrong, because a transaction runner's failures are infrastructure faults. The type turns out to be right one layer down — `inTransactionWithoutResult` needs something non-null to hand the shared machinery. Wrong about the signature, right about the need. **The third is a question the RFC's sentence does not answer: is the nesting detector per runner, or one for the JVM?** They differ in exactly one case, and it is not exotic — two runners over two databases, nested. The deadlock argument that motivates the refusal does *not* apply there, since the inner call borrows from a different pool. **[ADR-0031](docs/adr/0031-one-nesting-detector-for-the-whole-jvm.md) refuses it anyway, and the reason is what the permissive reading would be offering:** two nested transactions with no coordination give **no atomicity** — if the outer rolls back after the inner commits, the inner's work stands — so the shape looks like one atomic unit and is precisely not one. The sequential form makes that visible in the code; the nested form hides it. The detector is a **flag rather than the counter the RFC worded**, a narrowing recorded rather than slipped in: nesting is refused, so depth is only ever 0 or 1, and `ThreadLocal.remove()` clears the entry outright where a decrement would leave a pooled thread permanently marked after one unbalanced increment. **This is the first type outside `d4np-core` to owe a jcstress harness, and the contrast is what makes the obligation legible.** Items 3.1, 4.1 and 4.3 each recorded that a *stateless* type owes none — a harness over `Validator`, `JsonMapper` or `SimpleJdbcExecutor` would be measuring somebody else's library. `JdbcTxRunner` has real per-thread state, so spec §6's rule finally bites: `JdbcTxNestingIsolationStress` runs two threads through one runner and both must complete. **It was then shown to be falsifiable rather than assumed to be:** replacing the `ThreadLocal` with a plain `static boolean[]` — a change *no* sequential test in this repository detects, the nesting tests included, all of which still pass — turns the harness red with exactly the outcomes it names, `refused, ok` and `ok, refused` at 25% each. A harness that cannot fail proves nothing, so it was made to fail once on purpose and then restored. The detector's only failure direction is a **false refusal**, which is why it needed the harness at all: an `IllegalStateException` reading *this thread is already inside a transaction* looks exactly like a caller's bug, and an on-call engineer would spend the outage reading the wrong code. **RFC-0003's four-row failure-while-failing table is implemented row by row and tested row by row, and one row is where doing the obvious thing would be wrong.** A failed rollback is `addSuppressed` onto the original and never replaces it; a refused commit becomes `JdbcAccessException` **after an attempted rollback**, so nothing is left open; and a restore or close that fails after a successful commit is **logged and swallowed** — which is why the connection is deliberately *not* held in a try-with-resources, since the language would otherwise let a failing `close` replace the value the transaction just committed. The work is committed, and reporting a failure invites a retry that applies it twice. Those claims are only observable while they happen, so `ScriptedConnection` — a recording proxy over a *real* H2 connection that can be told to refuse one named call, including only its *second* `setAutoCommit` — is what makes them assertable. Ordering is asserted as a subsequence: `setAutoCommit(false)` → `setTransactionIsolation` → `commit` → `setTransactionIsolation(previous)` → `setAutoCommit(true)` → `close`. **C-01 reaches a log line for the first time.** FR-06's two lines — a rollback at `DEBUG`, a rollback that itself failed at `WARNING`, and commits never logged at all — carry the failure's **type name** and an SQLState, never its *message*, which is where a driver puts the failing statement. Asserted against a body whose exception message carries a credential, and the same assertion catches an unrendered `MessageFormat` placeholder, which is ADR-0014's trap that item 3.2 measured. This is the weaker of C-01's two boundaries precisely because nobody reviews a log line for disclosure. **The obligation RFC-0003 asked to be demonstrated rather than asserted is demonstrated:** `aRetainedConnectionIsDead` captures the transactional connection out of the body and shows it closed and unusable afterwards — item 2.4's leaky-subclass treatment, applied to a caller obligation the library cannot enforce. Verified on Temurin **17.0.20+8** and **21.0.12+8**: `clean verify` (`Tests run: 333` in core, unchanged, and **51** in `d4np-jdbc` — **25 new**), `-Pjmh,jcstress verify` on both (core's **126 on 21 / 252 on 17** unchanged, plus `d4np-jdbc`'s new **14 on 21 / 28 on 17** — the same per-toolchain doubling core has always shown, since jcstress generates more permutations on 17 — zero failed and zero interesting throughout), `spotless:check -pl '!d4np-bom'`, `checkstyle:check`, `validate`, `consistency_lint.py`, and `-Xdoclint:all -Xwerror` clean over `d4np-core,d4np-json,d4np-jdbc`. The two CI commands that still cannot be reproduced locally are unchanged from item 3.1 — `japicmp:cmp` resolves no plugin (item 8.1) and `-Pcoverage` matches no profile (item 8.2) — and the javadoc scoping caveat item 4.3 left is unchanged at five empty modules. **No ROUTE-MISMATCH:** the item routes `standard / high`, this host's catalogue tier *is* `standard`, and the session ran at high effort — tier and effort both match, which has now happened twice in this milestone (4.2 and 4.4) and not at all in the three `frontier-reasoning` items. **`authority_check.py` per role:** of the **21** paths this item touches, `tech-lead` may write exactly **one** (`docs/patterns/README.md`) and is DENIED the other twenty — every production source, all three ADRs and the jcstress harness — while **`enterprise-architect` covers all 21**; one human holds both, so it ships as one PR with the split recorded — size: **M** · route: **standard / high**
- [x] 4.5 `PageRequest`/`PageResponse<T>` with whitelist-validated sorting (FR-07) — **done: `PageRequest`, `PageSort` (with `PageSort.Order` and `PageSort.Direction`) and `PageResponse<T>` close Milestone 4, and the item's headline decision is one RFC-0003 left open rather than one it made.** **The specified surface could not be built as specified, and the reason was in `d4np-core` rather than in FR-07.** §FR-07 says a whitelist violation throws `ValidationException` — with the FR-19 mapping table as its evidence, since validation maps to **400** and `IllegalArgumentException` has no row and would fall to the **500** fallback — and closes the point with *"`d4np-jdbc → d4np-core` makes the type available"*. It does not: `ValidationException`'s constructor is **package-private**, deliberately, and the class documented why — *"`Validator` is the only thing that may decide an object is invalid"*. That sentence was true while a Bean Validation provider was the only route to the verdict, and FR-07 is the first place in this project that reaches it without one, because the allowed set is supplied per call by the repository and is not expressible as an annotation on the type being validated. **[ADR-0034](docs/adr/0034-mint-a-validation-failure-from-outside-core.md) opens a second door and makes the two symmetric rather than parallel:** `ValidationException.of(validated, violations)` is public, `fromProvider(..)` stays package-private so *"a provider evaluated the annotations and they failed"* remains a sentence only `Validator` can say, and **both funnel through one private constructor that bounds every string** — control characters stripped, 200 characters per violation, at most 20 listed in the message — because bounding at the type rather than at each thrower is ADR-0022's rule applied to a mint. That bounding now also covers `Validator`'s own path, which is asserted rather than assumed: `producesTheSameExceptionTheProviderDoorDoes` compares both doors on the same input and core's other 339 assertions are unchanged, so the no-op is measured. **This is the second time in two milestones that a monopoly claim written by the first implementation has failed to survive the second module** — ADR-0024 corrected `d4np-json`'s *"no Jackson type appears in a signature"* the same way — and the pattern is worth naming: a claim of that shape is a prediction, not a contract. **[ADR-0033](docs/adr/0033-publish-no-accessor-for-the-unvalidated-sort.md) is the decision the RFC's three-row table left open, and it is what makes the threat-model row a ✅ rather than a ▢:** `PageRequest` publishes **no accessor that returns the sort it carries**, so `validatedAgainst(Set<String>)` — which takes the allowlist as its argument — is the only route to the client's property strings at all. With a `sort()` accessor the control is *advice*, and the mistake it permits is invisible in review, because the missing call is an **absence** with nothing marking the line where it belonged; without one, a repository that has not named its sortable columns has nothing to interpolate. This is FR-20's shape one milestone later — `JsonMapper` hardens Jackson by never handing out the `ObjectMapper`, so there is nothing to call `activateDefaultTyping` on — and it is asserted structurally, not editorially: `publishesNoAccessorThatReturnsTheSort` walks the reflected public surface and fails on any method returning a `PageSort` that does not take a `Set`, so a convenience accessor added later cannot pass wherever it is written. **`toString()` is where the type would otherwise contradict itself**, so `PageRequest` renders the sort as a **count** and never the names, while `PageSort`'s own rendering does show them, bounded — the asymmetry is principled: whoever holds a `PageSort` either built it or received it validated, where `PageRequest` is the type that crosses the layer boundary carrying the guarantee. **Both demonstrations run against a real database, and the loud one very nearly went into the tree as a false negative.** Concatenating `sku; drop table orders --` into the clause **drops the table** — through `SimpleJdbcExecutor.query` and a `PreparedStatement`, because a statement string holding a semicolon is still one string — and the first attempt at that test ran it through `update`, where H2 refuses a query and reports *"SQL statement: select id from orders order by sku"*, naming only the first statement. Read at face value that message says the semicolon was discarded, and this entry briefly said so; the assertion is what corrected it. It is the same lesson C-01 keeps producing from the other direction — **a driver's text is evidence about the driver, not about what happened.** The quieter demonstration is the one that matters more: `theSamePropertyLeaksAHiddenColumnWhenNobodyValidatedIt` orders by `note`, a column the allowlist omits and no query here selects, and the rows come back in that column's order — so a client who can name a column can read one, at one comparison per request, leaving a perfectly ordinary query in the log. **The exact-match rule gains a second reason, and RFC-0003 only had the first.** It refused normalisation because identifier folding is vendor-specific and normalising picks a vendor (PostgreSQL lower, Oracle upper, MySQL filesystem-dependent). Building this surfaced the security half: because the comparison is exact, the string that survives validation is `equals` to the one the repository listed, so interpolating the client's is indistinguishable from interpolating our own — under case folding `"ORDERDATE"` would pass a check against `"orderDate"` and then be the text that reaches the SQL. Two arguments, one direction, which is worth knowing if anyone is ever tempted to fold. **One judge for everything about the sort, and that resolves a contradiction the requirement would otherwise ship.** FR-07's *"violations throw `IllegalArgumentException` at construction"* is fine for a programmatic caller and wrong for `?sort=,asc` from a browser, so **nothing about a property is judged at construction** — `PageSort` accepts any non-`null` string including a blank one, including a 10 000-character one — and `validatedAgainst` is the only thing that refuses, always as a 400. `null` is still refused, and the asymmetry is deliberate: a parser handing us a `null` property is a defect in the code that built the request, not something a client can send. **The same reasoning bounds two inputs the requirement never named**, both at validation time so both answer 400: at most **8** sort properties (the allowlist bounds *which* columns and never *how many*, so a repository with fifty sortable columns would otherwise accept a fifty-column `ORDER BY`) and no property named twice (`ORDER BY sku ASC, sku DESC` is legal SQL whose second clause is dead, and looking like it does something is the problem). Every rejection is reported rather than the first, on item 4.2's precedent for FR-21's unknown properties. **The message names what was rejected and never what was allowed, which is the exact inverse of `StrategyNotFoundException` and for the same reason.** That exception lists every key it holds, safely, because FR-19 maps it to a **500 with no body** — a standing coupling ADR-0015 recorded; this one's message travels inside a **400 that has one**, and an allowlist of column names is internal schema. `namesWhatWasRejectedAndNeverWhatWasAllowed` asserts every allowlist entry is absent. C-01 gains its **sixth** enforced call site and its first that deliberately quotes client input back, and the register states why the two rules agree: a value the client did not send is a disclosure, a value it did send is an acknowledgement. **The asymmetry FR-07 leaves is filed rather than fixed:** the page/size bounds *do* raise `IllegalArgumentException`, as spec §2 states normatively, so `?page=-1` arriving unmediated lands on the 500 fallback — the identical misattribution the whitelist argument avoids, applied to the other half of the same requirement, which RFC-0003 caught on one side and not the other. Deviating would put an implementation quietly at odds with both the specification and an approved RFC, so it is an `@apiNote` on the factory and a **fifth obligation on item 7.1**, which owns the mapping table. **Two overflow bugs are removed by writing the arithmetic rather than leaving it to the caller, and both are silent rather than loud.** `offset()` returns `(long) page * size` because `page * size` in `int` returns a **negative** offset at roughly ten million pages of 200, which a database rejects with a syntax error naming neither cause; `totalPages()` is `(total - 1) / size + 1` rather than the familiar `(total + size - 1) / size`, which overflows to a negative page count near `Long.MAX_VALUE`. `Math.ceilDiv(long, long)` would say it plainly and arrived in **Java 18**, which the `--release 17` baseline puts out of reach — and ErrorProne's `InvalidLink` check proved that by failing the build on a `{@link}` to it, resolving references against the release classpath rather than the compiling JDK's. `totalPages()` returns `long` for the same reason `totalElements` is one: three billion rows paged at one do not have an `int` number of pages. **`offset()` is an addition to RFC-0003's table, taken in the reversible direction** — adding a method later is MINOR, removing one MAJOR — and it earns the widening precisely because the caller's version of it has a bug in it. **`totalElements` is documented as a snapshot rather than validated into a fiction:** it comes from a second query, so a concurrent write makes it disagree with the content, and `hasNext()` inherits that; only the contradictions no race can produce are refused (a negative count, a page holding more rows than its own `size`). **`PageResponse` is a class rather than a record for ADR-0027's reason multiplied:** a record's generated `toString()` would print `content`, which is up to `size` rows of whatever the caller's query returned, and nobody would ever have decided that it should. `PageSort.Order` **is** a record, and the difference is the test — there both components must be printable for the type to be debuggable, so only `toString()` is replaced, with the bounding that makes printing a client-supplied name safe. **The patterns catalogue gains a rejection rather than an adoption:** Specification is a near miss for `validatedAgainst` and is refused on shape — its value is that predicates *compose*, which is what row 8's `AuditPolicy` genuinely does, where this is a single `Set.contains` with nothing to compose it with; adopting the name would make the catalogue's one real Specification harder to recognise. **`PageDiagnostics` is the third copy of truncate-and-strip in this repository and is re-decided rather than drifted into:** the caps and the destinations differ (20 rendered keys behind a 500 with no body for `KeyDiagnostics`, 64 characters of a JSON property name, 64 of a sort property arriving in a query string and leaving in a 400 that has a body), so a shared helper would have to be parameterised on all of it and exported from core forever. Recorded on ADR-0029's disposal of the same shape: a **fourth** call site should reopen the extraction on its own terms. **No jcstress harness and no benchmark, and that is the fourth time this reasoning is recorded, which is itself the point:** all three types are immutable with no shared mutable state, and no NFR names FR-07 (RFC-0003 §Scalability budgets says so directly) — items 3.1, 4.1 and 4.3 reached the same conclusion for `Validator`, `JsonMapper` and `SimpleJdbcExecutor`, and item 4.4 remains the only type outside core that owes one. **One build finding, avoided rather than configured around:** `@apiNote` is legal on a *method* here and rejected on a *type* — Checkstyle's `JavadocType` reports `Unknown tag 'apiNote'` where `JavadocMethod` accepts it, so item 4.3's declaration of the two JDK tags for `maven-javadoc-plugin` covers half the places the JDK allows them. The note moved onto the factory, which is where it belonged; loosening the ruleset for a documentation preference would have been a gate change bought with nothing. **The surface RFC-0003 stated as a number is now countable, and it is wrong by exactly two:** the RFC promised **15 new public types** for this milestone and the shipped total is **17** — five in `d4np-json`, ten top-level in `d4np-jdbc`, plus `PageSort.Order` and `PageSort.Direction`, both of which the RFC names in prose and neither of which it counted. Nested types are types to `japicmp`, so this is item 8.1's number rather than a quibble, and it is recorded here rather than discovered there. `d4np-core` gains **no** new type and one new public method, which is MINOR. Verified on Temurin **17.0.20.1+1** and **21.0.12.1+1** — the builds Adoptium serves as GA today, and a different build number from the `+8` pair items 4.0–4.4 used, which is stated rather than copied forward: `clean verify` (**340** in core — **+7**, all in `ValidationExceptionTest`, the first change to core's count since item 3.3 — **52** in `d4np-json` unchanged, and **123** in `d4np-jdbc`, **+72**), `-Pjmh,jcstress verify` on both (core's **126 on 21 / 252 on 17** and `d4np-jdbc`'s **14 on 21 / 28 on 17** both unchanged, zero failed and zero interesting, which is what "adds no state" looks like when it is measured), `spotless:check -pl '!d4np-bom'`, `checkstyle:check`, `validate`, `consistency_lint.py`, and `traceability.py ROADMAP.md RFC-0001 RFC-0002 RFC-0003` → OK. **The `-Xdoclint:all -Xwerror` line is the one claim this item could not carry forward, and it is corrected rather than repeated:** items 4.1–4.4 each recorded it clean on both toolchains, and on this host it is clean on **21** and **fails on 17** with **eight** `warning: no comment` findings on the record components of `AuditEvent` and `ErrorDetail` — item 3.3's JDK-17 record noise, which item 4.1 recorded as not recurring. It was not accepted as inherited: `origin/main` was checked out into a separate worktree and produces the **identical eight**, so this item introduces none, and the two files are ones it does not touch. What changed is unresolved and is stated as two candidates rather than guessed between — this host runs Temurin **17.0.20.1+1** where those items ran **17.0.20+8**, so either the patch release changed doclint's treatment of an annotated record component or the earlier claim was made against a build that did not report it. Either way it is **item 8.7's**, which already owns wiring the Javadoc gate into CI, and repairing another item's shipped file inside an unrelated PR is exactly the quiet exception item 3.1 declined to make. The two CI commands that still cannot be reproduced locally are unchanged from item 3.1 — `japicmp:cmp` resolves no plugin (item 8.1) and `-Pcoverage` matches no profile (item 8.2) — and the javadoc scoping caveat is unchanged at five empty modules. **No ROUTE-MISMATCH:** the item routes `standard / high`, this host's catalogue tier *is* `standard`, and the session ran at high effort — the third time in this milestone that tier and effort both match, against three `frontier-reasoning` items where neither did. **`authority_check.py` per role:** of the **21** paths this item touches, `tech-lead` may write exactly **one** (`docs/patterns/README.md`) and is DENIED the other twenty — every production source, both ADRs, and for the first time in this milestone three files in `d4np-core` — while **`enterprise-architect` covers all 21**; one human holds both, so it ships as one PR with the split recorded — size: **S** · route: **standard / high**

## Milestone 5 — concurrent

**Goal:** pooling and async execution with the concurrency claims proven, not asserted. Spec §1
rule: *a thread-safety claim without a named jcstress test is not a claim.*

> **All four items closed.** `d4np-concurrent` is feature-complete at **11 public types** — nine
> top-level and two nested — across FR-08, FR-09 and FR-10, with **zero third-party dependencies at any
> scope** and no `requires` edge beyond `d4np-core`. Spec §1's rule was applied four times and answered
> "no harness" three of them, each for a stated reason: FR-08 owed one and got it (NFR-05 names it), while
> FR-09's leak is a sequential property and FR-10 ships no implementation to stress.

- [x] 5.0 **RFC-0004 — concurrency contracts.** Pool lifecycle and rejection semantics, MDC propagation guarantees across `CompletableFuture` boundaries, and the `DistributedLock` lease/reentrancy contract every implementation must honour. Blocks 5.1–5.3. — **done and approved:** [RFC-0004](docs/rfc/0004-concurrency-contracts.md) was drafted `Proposed` with an empty `approved-by:` — no RFC self-approves (AGENTS.md §6) — and **the owner approved it in session on 2026-08-30**, flipped in a separate commit so the two acts are visible as two acts. **The sequence differs from every RFC before it and the difference is recorded rather than smoothed over:** RFC-0001, RFC-0002 and RFC-0003 were each approved *before* their pull request, which is what let RFC-0003 say that *"merging publishes an already-accepted document rather than performing the acceptance"*. That sentence is not available here — this RFC merged as PR #49 while still `Proposed`, and the approval followed. For the window between those two events `main` carried an unapproved RFC, which is visible in the history rather than hidden by it; **nothing was implemented against it in that window**, since item 5.1 is its first consumer and had not started. The peer-review round still did not run: this acceptance rests on the owner's direct authority, not on review, and that is recorded rather than implied. **5.1–5.3 are unblocked.** **The item's method is the change, and it is item 4.4's lesson turned into procedure: RFC-0003 pinned an FR-06 surface that did not compile, so every surface claim here was compiled before it was written down.** Three probes ran first, and two of them changed the answer. **The headline is that FR-09 names a type this module may not import, and the ADR that made that true predicted the moment.** `d4np-concurrent`'s `enforce-adr-001` allowlist is default-deny with exactly **two** entries — `it.d4np:*` and test scope — where `d4np-core` has a **third** for `jakarta.validation-api` at `provided`; there is no provided-scope escape here, so SLF4J's `MDC` fails `mvn validate` rather than review. Sharper still, this library logs through `System.Logger` ([ADR-0014](docs/adr/0014-log-through-the-jdk-system-logger.md)), which has **no MDC at all** — and that ADR's own Consequences named the trigger: *"a module that later needs structured context will find `System.Logger` thin, and that is the moment to revisit, not now."* FR-09 is that module, so the revisit is executed rather than discovered, and the answer is FR-15's shape one milestone later — the module owns a **context SPI** (`ContextPropagator` / `ContextSnapshot` / `ContextSnapshot.Scope`) and ships no implementation that reads a logging framework. **The reflective `org.slf4j.MDC` lookup is the option that keeps the requirement's sentence literally true and it is rejected on its failure mode**, not on taste: it works when SLF4J happens to be on the classpath and silently propagates nothing otherwise — implicit configuration in the one type whose value is explicitness, which is item 4.1's argument against `findAndRegisterModules()` arriving in a second module. **Shipping the binding in `d4np-spring-adapter` is legal and is refused on the project's own thesis:** that module's allowlist bans only `org.redisson:*`, so SLF4J would be permitted — but MDC is SLF4J and not Spring, so a Jakarta EE host, which spec §1 names as a first-class target, would take a Spring dependency to get context propagation. **The contract is pinned under [ADR-0010](docs/adr/0010-single-specification-authority.md) rung 1 — *an RFC outranks the spec for every section it pins* — so FR-09's spec sentence is superseded rather than edited, and no manifest amendment or spec re-render is performed.** **The second finding is a lifecycle method that is silently replaced at run time, and it is measured rather than reasoned about.** `ExecutorService` became `AutoCloseable` in **Java 19** with a default `close()` that calls `shutdown()` then `awaitTermination(1, DAYS)` in a loop; this project compiles at `--release 17`, where that method does not exist, and ships to consumers on 17 **and** 21. A wrapper compiled at `--release 17` by JDK 21's javac — exactly how this project builds — and driven from a consumer compiled on 21 gives five results, none of which is visible in the source: without a declared `close()` the consumer's try-with-resources runs the **interface default** (`awaitTermination(1, DAYS)`, **3017 ms** against a 3 s task, the configured budget ignored entirely); with `close()` declared it runs ours (**510 ms** for a 500 ms budget) and **still runs ours through the `ExecutorService` static type** (**513 ms**), so it is a genuine override; **`@Override` on that method is a compile error at `--release 17`** — *"method does not override or implement a method from a supertype"* — so the one marker that tells a reader it is an override cannot be written and the method reads as removable convenience; and a **JDK 17 consumer cannot use try-with-resources at all** unless the type declares `AutoCloseable` itself (*"NoClose cannot be converted to AutoCloseable"*). Hence `ManagedThreadPool` declares both explicitly, and **item 5.1 owes a test that drives `close()` through the `ExecutorService` static type on a 21 runtime**, because a test through the concrete type keeps passing after the regression. **The third probe expected item 4.4's ambiguity and found something worse.** A `submit(Supplier<T>)` / `submit(Runnable)` pair is **not** ambiguous — all four call shapes compile — but the overload chosen depends on the body's *syntax*: `submit(() -> returnsInt())` binds to `Supplier` and returns `CompletableFuture<Integer>`, while **`submit(() -> { returnsInt(); })` — the same call with braces added — binds to `Runnable`** and returns `CompletableFuture<Void>`, discarding the result with no warning. ADR-001's naming-consequence rule decides it in the stronger direction than item 4.4 needed: an ambiguity is a compile error the author must fix, this compiles and diverges, so the methods are **`supply` and `run`** — which is the decision the JDK already made in the very class FR-09 wraps, where `supplyAsync` and `runAsync` have never been an overloaded pair. **FR-08's rejection handler is unreachable in the shape a reader will have in mind, which is why the queue bound becomes mandatory:** `Executors.newFixedThreadPool` hands `ThreadPoolExecutor` an **unbounded** queue, so the explicit `RejectedExecutionHandler` the requirement mandates can never fire and the threat model's *rejection storm* row is mitigated by decoration. `ThreadPoolSpec` therefore has no overload that omits the capacity and no default — FR-05's structural move applied to a second requirement — and `DiscardPolicy`/`DiscardOldestPolicy` stay legal but are **named in the Javadoc as the two that lose work silently**, ADR-0028's shape where refusing is unavailable. **What the factory returns is ADR-0022's rule again:** a `ThreadPoolExecutor` publishes `setCorePoolSize` and hands out the live queue via `getQueue()`, so every guarantee the factory just established becomes switchable; a bare `ExecutorService` is correctly narrow but carries no configured timeout, leaving NFR-05 advisory. `ManagedThreadPool` is a **final class** — not an interface, because an interface anyone can implement makes the drain guarantee per-implementation. **Three of FR-08's four thread properties are decisions the requirement does not state:** non-daemon by default (a daemon pool lets the JVM exit with work in flight, the direct contradiction of NFR-05's drain, and a forgotten `close()` becomes a diagnosable hang instead of silent loss), a mandatory `UncaughtExceptionHandler` because a pool thread dying silently is the classic defect, and **thread priority offered but deliberately untested** — `Thread.setPriority` is advisory and has no effect at all on common Linux configurations, so spec §6's rule cuts both ways and the claim is simply not made. **FR-10 is the milestone's most expensive page and the decision is one the requirement never raises:** lease time bounds *starvation*, which the threat model records, and says nothing about the converse that corrupts data — **a lease can expire while the holder is still running**, so two holders write. No lease-based lock can prevent it; the only structural mitigation is a fencing token, and without one in the *interface* no implementation can add it later without a MAJOR break. `LockHandle.fencingToken()` returns **`OptionalLong`**: mandatory would make the interface unimplementable by `d4np-lock-redisson` on day one, absent would foreclose it forever, and empty is a documented statement with teeth — the same refusal to fabricate a backend's field that kept `JdbcAccessException` from inventing SQLState 21000. **Two more interface constraints are stated because every implementation inherits them:** `close()` releases **this acquisition** and never a key, which is the classic defect of deleting a lock another holder has since taken; and an implementation without reentrancy must **refuse** a nested acquisition rather than block on it — [ADR-0031](docs/adr/0031-one-nesting-detector-for-the-whole-jvm.md)'s argument at longer range, since the permissive reading waits on a lock the caller already owns with nothing in a log to read. A `boolean isReentrant()` capability method was considered and rejected: its false arm is untestable against a real backend. **The threat model gains a row rather than only moving one, which no previous RFC here has done** — RFC-0003 added none: the *"information disclosure"* section has eight rows and none about **a pooled worker thread carrying one task's context into the next**, which is FR-09's real defect and the reason `install()` returns a `Scope` that restores rather than clears. Under the enterprise posture that makes it security-relevant, so **item 5.2 carries an ADR**; **no new trust boundary is needed** — B1 covers the API and B3 already names `lock-redisson → Redis` — stated because RFC-0002 routed a missing boundary to item 8.6 and a reader will look for the same. **NFR-02 is separated from item 8.8 and routed to 8.3, and the reason is not the one that applied to NFR-03:** RFC-0003 could recommend NFR-03 as a real gate because it is a *relative* ratio inside one JMH invocation, where a slow runner slows both arms; NFR-02 is phrased relatively but **bounded absolutely** at 5 µs, so it belongs with NFR-01's 2 ns/op in the stable-runner problem. **A second observation is recorded so item 5.2 need not rediscover it: the budget is loose by about two orders of magnitude** against `supplyAsync`'s ~100 ns submission, so a 4 µs regression would pass — 5.2 reports the measured number rather than only a verdict, and the audit phase decides whether to tighten. **The surface is stated as two numbers rather than one, because RFC-0003 stated one and was wrong by exactly the nested types it named in prose and did not count (item 4.5 found it at the end of the milestone): eleven new public types, nine top-level and two nested.** `d4np-core` gains no new type and no new method. **No `CHANGELOG.md` entry, deliberately** — nothing user-visible changed, and item 3.0 added one only because it shipped `Unit`. **`rfc_check.py` FAILs by design** ([ADR-0023](docs/adr/0023-the-owner-approves-this-projects-rfcs.md)): it expects `approved-by: tech-lead`, which is the *authoring* role, so satisfying it literally would be self-approval. **`authority_check.py` per role:** of the **6** paths this item touches, `tech-lead` may write exactly **one** — `docs/rfc/0004-concurrency-contracts.md`, the RFC itself — and is DENIED the other five, including the ROADMAP, the manifest, the threat model and both journal files, while **`enterprise-architect` covers all 6**; one human holds both, so it ships as one PR with the split recorded. **ROUTE-MISMATCH recorded, plus a smaller discrepancy in the route itself.** Re-resolving this item's signal set today — `route_advice.py --labels adr --check` — returns **`frontier-reasoning / extra`**, one effort notch above the **`frontier-reasoning / high`** the Routing basis table records for the `adr` signal. The tier is unchanged and so is the verdict against this host (Opus 5 is the `standard` tier, so the mismatch stands either way), but the table's own claim is that routes "were resolved with `route_advice.py`, not asserted" — and the resolver and the table now disagree by one notch, which is either a policy change since the legend's 2026-07-27 date or a different signal set on the original run. Recorded rather than reconciled: editing another item's recorded route on the strength of a re-run is the kind of quiet correction item 3.1 declined to make, and the routing legend is the artifact that owns it. **The spec re-render was verified rather than asserted:** a fresh `render.py --out` of the amended manifest reproduces `docs/specs/01_spec_utils.md` **byte-identically**, ADR-0010's property, which also confirms that neither the `manifest_rev` bump nor the `refs.rfcs` entry reaches the published spec. **`.github/workflows/ci.yml` does *not* round-trip**, which is how item 8.9 above came to be filed. Verified on Temurin **17.0.20.1+1** and **21.0.12.1+1**: `clean verify` on both (**333** in `d4np-core`, **51** in `d4np-jdbc`, **52** in `d4np-json` — all unchanged, this item ships no production code), `spotless:check -pl '!d4np-bom'`, `checkstyle:check`, `validate`, `consistency_lint.py`, and `traceability.py ROADMAP.md RFC-0001 RFC-0002 RFC-0003 RFC-0004` → OK, with the new **`RFC-0004 → M5`** edge derived rather than declared. **Those three test counts are `main`'s and are deliberately *lower* than the ones item 4.5 records, which is a fact about the branch rather than a regression:** this item was branched from `main` while **item 4.5 is still unmerged** (`feat/page-request-response` at `9df7559`, no PR opened), so 4.5's `+7` in core and `+72` in `d4np-jdbc` are not present here — 333/51/52 are exactly item 4.4's numbers, unchanged. The same absence is why this item's RFC **cites no ADR above 0032**: ADR-0033 and ADR-0034 arrive with 4.5, and linking them from a branch that does not contain them would ship two dead links. **Both PRs edit the *Latest checkpoint* pointer, so whichever merges second needs a rebase** — stated here because a squash-merge would otherwise silently drop one of them. The two CI commands that still cannot be reproduced locally are unchanged from item 3.1 — `japicmp:cmp` resolves no plugin (item 8.1) and `-Pcoverage` matches no profile (item 8.2) — size: **M** · route: **frontier-reasoning / high**
- [x] 5.1 `CustomThreadPoolFactory` plus the rejection/shutdown jcstress harness; graceful shutdown drains within the configured timeout (RFC-0004; FR-08, NFR-05) — **done: `CustomThreadPoolFactory`, `ThreadPoolSpec` (with `ThreadPoolSpec.Builder`) and `ManagedThreadPool` open `d4np-concurrent`, which becomes the first module here to publish an API and add no `requires` edge at all** — everything FR-08 needs is in `java.base`, where `d4np-jdbc` needed `requires transitive java.sql`. **The item's headline is a correction to the RFC it implements, and it arrived from ErrorProne rather than from re-reading the document.** RFC-0004 §FR-08 concluded that `@Override` on `close()` is *"a compile error at `--release 17`"*, so the method would carry no marker and read as removable convenience. The build disagreed on the first compile — `[MissingOverride] close implements method in AutoCloseable; expected @Override` — and probing it settled why: **the RFC's own probe measured a class implementing `ExecutorService` alone, which is not the shape the RFC specifies.** Once `AutoCloseable` is declared too (the RFC's own second conclusion), the supertype method exists at every release level. Measured both ways at `--release 17`: with `ExecutorService` only, `@Override` fails with *"method does not override or implement a method from a supertype"*; with `ExecutorService, AutoCloseable` it **compiles clean**, and `failOnWarning` then makes it mandatory. **[ADR-0035](docs/adr/0035-declare-autocloseable-so-the-override-is-legal.md) records that the interface declaration does three jobs where the RFC credited it with one** — it enables try-with-resources for a JDK 17 consumer, it makes the `@Override` legal, and it makes `close()` an abstract method the class must implement, so **deleting the method fails the build** instead of silently reverting to the JDK's one-day drain. That last one removes the hazard the RFC was warning about. **The guard has an expiry date and that is the part worth carrying:** at `--release 17` deleting `close()` is *"is not abstract and does not override abstract method close() in AutoCloseable"*, and compiled **natively on 21 the same deletion compiles**, because `ExecutorService` supplies the default that satisfies `AutoCloseable`. Both guards lapse together the moment the baseline moves past 18 — already a MAJOR bump under RFC-0001 §Versioning, now with one more thing to re-check. **The RFC is not amended**, on item 2.5's precedent that a document which changes to match the code it produced stops being a check on that code. **The test RFC-0004 asked for by name could not be written in the form it described, which is the same skew biting a second time:** it asks for try-with-resources over an `ExecutorService` variable, and **this project's test sources also compile at `--release 17`**, so that is *"incompatible types: try-with-resources not applicable to variable type (ExecutorService cannot be converted to AutoCloseable)"*. The test dispatches through `ExecutorService.class.getMethod("close").invoke(pool)` instead — not a workaround but the faithful version, since what regresses is `invokeinterface` on `ExecutorService.close()` and that is exactly the dispatch `Method.invoke` performs; on a 17 runtime the lookup throws `NoSuchMethodException` and the test asserts the runtime is below 19 rather than skipping silently. A companion test covers try-with-resources over `AutoCloseable`, which does compile at the baseline. **FR-08's mandated rejection handler cannot fire in the pool shape every reader has in mind, and that is why the queue bound became mandatory:** `Executors.newFixedThreadPool` hands `ThreadPoolExecutor` an **unbounded** queue, which never refuses a task, so the *explicit* `RejectedExecutionHandler` the requirement demands is decoration and the threat model's *rejection storm* row is mitigated by nothing. `ThreadPoolSpec` therefore has no overload that omits the capacity and no default — FR-05's structural move applied to a second requirement — and `firesWhenTheQueueIsFull` is an assertion that could never pass over an unbounded queue. `Integer.MAX_VALUE` is accepted, so unbounded buffering stays available as a decision in the caller's source rather than a default they inherited. `DiscardPolicy` and `DiscardOldestPolicy` remain legal, because refusing them is unenforceable — a discarding handler is four lines of a caller's own code — but are **named in the Javadoc as the two that lose work silently**, which is ADR-0028's shape where refusing is unavailable. **What the factory returns was the RFC's decision and it survived contact:** a `ThreadPoolExecutor` publishes `setCorePoolSize`, `setRejectedExecutionHandler` and hands out the live queue via `getQueue()`, so every guarantee just configured becomes switchable (ADR-0022); a bare `ExecutorService` is correctly narrow but carries no configured timeout, leaving NFR-05 advisory. `ManagedThreadPool` is a **final class** — not an interface, since an interface anyone can implement makes the drain guarantee per-implementation — and `publishesNoHandleToTheConfiguredExecutor` walks the reflected surface so a convenience accessor added later fails wherever it is written, the structural assertion FR-20 and FR-07 each used. **Three thread properties the requirement never named are decided rather than defaulted:** non-daemon by default, because a daemon pool lets the JVM exit with work in flight — silent loss, and the direct contradiction of NFR-05's drain — where a forgotten `close()` becomes a hang a thread dump diagnoses in seconds; a mandatory `UncaughtExceptionHandler`, defaulting to a `System.Logger` line, because a pool thread dying silently is the classic defect; and **thread priority offered but deliberately unasserted** — `Thread.setPriority` is advisory and has no effect at all on common Linux configurations, so the test asserts only that the value reached the thread and spec §6's rule is honoured in the other direction by simply not making the claim. **One asymmetry is documented and tested because a reader would otherwise stop checking futures:** a task given to `execute` that throws reaches the uncaught handler, and one given to any `submit` overload **does not** — `FutureTask` captures the throwable and returns it through `get()`. That is `ThreadPoolExecutor`'s contract rather than a choice here, and `doesNotReachTheHandlerThroughSubmit` pins both halves. **NFR-05 is the first requirement in this project to name a jcstress harness rather than leave one to judgement**, which is what makes items 3.1, 4.1, 4.3 and 4.5's opposite conclusions legible as decisions: `ThreadPoolRejectionShutdownStress` races a submit against a `close()` on one pool and reports `Interesting tests: No matches`. **It was shown to be falsifiable before it was trusted**, per item 4.4's rule — making `close()` throw turns it red with the named forbidden outcome **`close threw` at 100%** on every fork, and the sabotage was then removed and the green run repeated. The forbidden outcomes are named individually rather than left to a catch-all, so a red run says which promise broke. **Two smaller findings, both from running rather than reading:** `FluentBuilder`'s *validate-then-construct* ordering lives in another class, so NullAway cannot see it and every subclass with a mandatory **reference** field pays an `Objects.requireNonNull` in `construct()` — an assertion of ADR-0017's contract, not defensive padding; and the default uncaught handler logs during the worker thread's **death** sequence, after `awaitTermination` can already have returned, so the test that reads that line polls rather than racing — a property of `Thread.dispatchUncaughtException`, and the reason the first run of that assertion saw an empty recorder. **C-01 gains two call sites on outbound log lines**, the weaker of the control's two boundaries: the drain warning carries the **count** of tasks that never started and never the tasks, whose `toString()` this library does not control, and the thread-failure line carries the failure's **type** and never its message. Both are asserted against a task whose message carries a credential, and the same assertion catches an unrendered `MessageFormat` placeholder — ADR-0014's trap that item 3.2 measured. The pool **name** is bounded at construction — 48 characters, no ISO control characters — because a name holding `\r\n` folds one log line into two for the life of the pool; that is the fourth appearance of truncate-and-strip in this repository and it is **not** a fourth C-01 call site, because a pool name is a developer-supplied constant rather than client input, which is the distinction item 4.5's deferral did not draw. Verified on Temurin **17.0.20.1+1** and **21.0.12.1+1**: `clean verify` (**340** in `d4np-core`, **123** in `d4np-jdbc`, **52** in `d4np-json`, all unchanged, plus **39 new** in `d4np-concurrent`), `-Pjmh,jcstress verify` on both, `spotless:check -pl '!d4np-bom'`, `checkstyle:check`, `validate`, `consistency_lint.py` — size: **M** · route: **standard / high**
- [x] 5.2 `AsyncExecutor` with MDC propagation and the submission-overhead benchmark (RFC-0004; FR-09, NFR-02) — **done: `AsyncExecutor`, `ContextPropagator` and `ContextSnapshot` (with nested `ContextSnapshot.Scope`), plus [ADR-0036](docs/adr/0036-carry-context-through-an-spi-that-restores.md), which the enterprise posture made mandatory rather than optional because a cross-task context leak is an information-disclosure decision (AGENTS.md §7).** **The requirement's stated mechanism is unreachable from this module and ADR-0014 predicted the moment:** FR-09 names SLF4J's `MDC`, `d4np-concurrent`'s allowlist has no third-party entry at any scope, and this library logs through `System.Logger`, which has no MDC at all — so the module publishes a **context SPI**, ships no implementation that reads a logging framework, and the host binds MDC in four lines carried verbatim in the Javadoc. The contract is pinned under [ADR-0010](docs/adr/0010-single-specification-authority.md) rung 1, so FR-09's spec sentence is superseded rather than edited. A reflective `org.slf4j.MDC` lookup is refused on its failure mode — it would propagate when SLF4J happened to be present and silently not otherwise, which is item 4.1's argument against `findAndRegisterModules()` in a second module — and shipping the binding in `d4np-spring-adapter` is refused on the project's thesis, since MDC is SLF4J and not Spring and a Jakarta EE host would take a Spring dependency to get it. **The item's headline finding is that the leak test everyone would write — the one RFC-0004 and the threat model's own row both describe — passes against the defect it names.** *"A single-threaded pool, two tasks, assert the second sees nothing of the first"* was written first and **passed even with a `Scope` that restores nothing**, because the second submission installs its own capture over the residue and hides it. The leak is not visible to the next task through the same executor; it is visible to **anyone else sharing the pool**, which is the realistic case since a pool is shared. Both demonstrations now observe by submitting **directly to the pool**, as a matched pair over an identical sequence: `leavesTheWorkerAsItFoundIt` sees `<none>` and `theSamePoolLeaksWhenTheScopeRestoresNothing` sees `tenant-A` surviving on the worker. The threat-model row's own test description was corrected in the same edit, because it prescribed the version that cannot fail — a safety test that cannot see the defect it names is worse than none. **`install()` returns a `Scope` that restores rather than clears, and that is the design's whole point:** clearing destroys context the thread already had (the nested-submission case, which has its own test), doing nothing leaks, and only restoring is correct in both directions. `AsyncExecutor` closes it in a try-with-resources so the restore also runs when the body throws — a leak that only happens on the error path being the hardest kind to notice — and `Scope.close()` narrows `AutoCloseable`'s `throws Exception` away, because a restore that threw would run inside that same `finally` and replace whatever the body was reporting. **One Javadoc claim was written, measured and corrected in the opposite direction to the usual:** the draft said `CompletableFuture.supplyAsync` handles a rejecting executor differently across JDK versions. It does not — it lets `RejectedExecutionException` escape **synchronously on the submitting thread on Temurin 17.0.20.1+1 and 21.0.12.1+1 alike**, which is *consistent* JDK behaviour and a *stronger* justification for completing the future by hand than the version-quirk story would have been. `AsyncExecutor` delivers rejection through the future so one operation has one failure path, and `divergesFromSupplyAsyncWhichThrowsSynchronously` pins both halves so the divergence stays a decision rather than becoming an accident. An `Error` from a body is delivered through the future too, on the ground that a future nobody completes is a caller waiting forever — worse than an `Error` the caller can see. **The methods are `supply` and `run` rather than an overloaded `submit`**, which RFC-0004 settled by measurement: the pair is *not* ambiguous, it silently binds to `Runnable` and returns `CompletableFuture<Void>` when a body gains braces, and `publishesTwoNamesAndNoOverloadedSubmit` asserts the surface structurally. **The benchmark had to be rewritten before it measured the requirement.** The obvious harness — real pool, `join()` — put **every arm at ~12 µs including the raw one**, which is a thread handoff and not a submission; at CI's single iteration the arms were not even correctly ordered, with the context-carrying executor measuring *faster* than raw. So NFR-02 is judged on an **inline** pair sharing `Runnable::run`, where nothing but the wrapper differs, and the pooled pair is kept and reported so no reader assumes the budget includes the handoff. **Report grade, 5 forks × 10 iterations** ([report](docs/benchmarks/2026-08-30-async-submission.md)): on JDK 21 raw `supplyAsync` is **0.012 µs** and `AsyncExecutor` **0.013 µs**, and on JDK 17 both are **0.014 µs** — so the overhead is **one least-significant digit on 21 and exactly zero on 17, against a 5 µs ceiling**. Stated precisely because the honest number is smaller than the interesting one: the 21 difference *equals* the ±1 ns error on each arm, so the claim is *indistinguishable from the JDK's own submission at this granularity* rather than *exactly 1 ns*; the margin against the whole measured submission path is about **350×**. **Propagating an eight-entry context is free at this resolution on both toolchains** (the same 0.013/0.014 as the plain arm), which retires RFC-0004's worry that a large-map capture might consume the headroom. Through a real pool the wrapper adds nothing measurable — 11.160 ± 0.032 against raw's 11.865 ± 0.511 — and the report deliberately does **not** claim ours is faster, since the error bars overlap. **NFR-02's gate is item 8.3's and not item 8.8's**, and the distinction is RFC-0004's: NFR-03 is a *relative* ratio where a slow runner slows both arms, while NFR-02 is bounded *absolutely*, so a loaded runner moves the difference itself. **Two friction points recorded because they cost time:** NullAway rejects a literal `null` even in a test asserting the rejection, so the house `@SuppressWarnings("NullAway")` with a stated reason applies; and JMH's `@Setup` fields need `@SuppressWarnings("NullAway.Init")`, which item 4.3 reached for first. Verified on Temurin **17.0.20.1+1** and **21.0.12.1+1** — size: **M** · route: **standard / high**
- [x] 5.3 `DistributedLock` — interface only in this module, lease time mandatory, no reentrancy promise (RFC-0004; FR-10, ADR-001). The interface *is* the deliverable: it constrains every future implementation — **done, and Milestone 5 closes: `DistributedLock`, `LockHandle` and `DistributedLockException`, with [ADR-0037](docs/adr/0037-a-fencing-token-that-restarts-is-worse-than-none.md) and [ADR-0038](docs/adr/0038-refuse-the-convenience-form-the-rfc-sanctioned.md).** **An interface-only item cannot demonstrate anything by shipping code, so the deliverable beside the three types is a contract test run against a reference `InMemoryDistributedLock` in test scope** — FR-10's *interface only* rule is about `src/main`, and item 4.3 used a hand-written `DataSource` the same way. The assertions are written as statements about the *contract* rather than about that class, because each is a requirement `d4np-lock-redisson` inherits; the clock is injected so a lease can be expired without sleeping, which is what makes the load-bearing test deterministic. **That test is `aStaleHolderDoesNotReleaseTheCurrentHolder`, and it was shown to fail before it was trusted:** replacing the owner-compared release with a plain `remove(key)` — the classic distributed-lock defect, where a holder whose lease expired deletes the *next* holder's lock — turns **that one test red and no other**, which is item 4.4's rule applied to a contract test rather than to a jcstress harness. **ADR-0037 pins the two clauses RFC-0004 left as prose, and the first is the item's headline.** The RFC says an empty fencing token means *"this implementation cannot keep mutual exclusion across a lease expiry"*; it does not say what an implementation should do when its counter is monotonic **while it runs** and resets when it does not. It must return **empty**, because *a restarted counter is more dangerous than an absent one*: absent makes the risk visible, monotonic protects, and restarted **accepts the stale writer** while looking like a guarantee. That is ADR-0028's rule applied to a second field — `JdbcAccessException` refuses to fabricate a SQLState no driver raised, because a field means what the backend said. The second clause is that **the mitigation is the resource's, not the library's**: a token protects nothing unless the thing being written to records the highest it has accepted and refuses anything lower, which this library cannot enforce and therefore states in the imperative — item 4.4's `aRetainedConnectionIsDead` treatment of an obligation the library cannot check. `leaseExpiry()` is documented as an estimate on the **local** clock and `isHeld()` as best-effort and local, so `true` means *not known to be over* and never *safe to write*; clock skew between holder and backend is exactly what makes leases unsafe. **[ADR-0038](docs/adr/0038-refuse-the-convenience-form-the-rfc-sanctioned.md) declines a method the RFC explicitly permitted, on two measurements neither of which was visible when the RFC was written.** Its §Alternatives 6 sketches a callback form; the natural signature returns `Optional<T>`, and `Optional.ofNullable(null)` **is** `Optional.empty` — so *"someone else holds the lock"* and *"I held it, ran, and my body returned null"* are the same value, two outcomes demanding opposite reactions. That is the collision C-02 exists to prevent and the one `tryAcquire` does not have. **The value/void pair then completes a three-item pattern, and the arity is what decides it:** item 4.4's `TxCallback`/`TxVoidCallback` (one parameter) was ambiguous, item 5.2's `Supplier`/`Runnable` (nullary) resolved but diverged silently on body syntax, and this item measured `Function<H,T>`/`Consumer<H>` (one parameter) — *"both method `<T>run(Function<Handle,T>)` and method `run(Consumer<Handle>)` match"*. Three measurements, one conclusion: **a value/void pair over two functional interfaces is never the right published shape here.** Deferring costs nothing and adding later is MINOR while removing is MAJOR (RFC-0001 §Versioning), so the form is **filed as an obligation on item 7.3**, which will have a call site to shape it. **The leaked-handle hazard the convenience form would remove is also genuinely smaller than item 4.4's, and that is recorded rather than assumed:** a leaked `Connection` is held until the pool starves, whereas a leaked `LockHandle` is released by the backend at the lease — **because FR-10 makes the lease mandatory** — so the hazard is bounded by a guarantee the interface already enforces and self-heals. **`DistributedLockException` carries the key beside the message rather than inside it**, which goes past what the RFC asked: the RFC says *never the key in full*, and truncating an identifier like `order:tenant-42:user-7` protects a log line's shape without protecting the tenant, so `getMessage()` names only the **operation** — drawn from a closed set this library controls, never caller text — and the **failure's type**, while `key()` carries the bounded value for a handler that deliberately wants it. That is RFC-0001's `ErrorDetail.message`-versus-`.cause` split: correlating a log line becomes a decision rather than an accident. **The bounding lives inside the type for a reason stronger than style: every thrower is in another module**, so a rule each of them must remember is advisory by construction (ADR-0022), and `theBoundingIsInTheTypeNotInTheThrower` asserts both factories produce the same stripped value. **The reentrancy clause is tested in the direction that matters:** FR-10 permits either answer, so the contract test asserts a reentrant implementation *may* admit its holder **and** that a non-reentrant one refuses **promptly** — a 30-second `wait` returning in milliseconds — because the harmful behaviour is neither answer but the third one, blocking on a lock this thread already owns (ADR-0031 at longer range). **`DistributedLock` publishes exactly one abstract method**, asserted structurally so a convenience form added later must be a `default` costing implementers nothing rather than a new obligation, and `LockHandle.close()` narrows `AutoCloseable`'s `throws Exception` away — asserted by reflection, since it is a contract rather than an oversight. **No jcstress harness, and for the first time the reason is not "the type is stateless":** items 3.1, 4.1, 4.3 and 4.5 each recorded that, whereas here **the module ships no implementation**, so a harness would measure a test fixture. Mutual exclusion is real and is asserted under contention — sixteen racing threads, exactly one winner — and proving it against a *backend* is an obligation item 7.3 inherits. **The threat model gains a *Tampering* row and closes a *Denial of service* one**, which is the first time an item has done both: the mandatory lease closes *"distributed lock held forever"*, and *creates* the converse — *"a lease expiring mid-critical-section, so two holders write"* — which had no row and is 🚧 rather than ✅, because two of its residuals are real. **The truncate-and-strip extraction item 4.5 deferred to a fourth call site has now reached it**, and the answer is filed as item **8.10** rather than taken here: item 5.1 argued a pool name was a fourth appearance of the *shape* and not of the *control*, and a lock key **is** client-derived, so this is genuinely the fourth C-01 site — but a shared helper must be exported from `d4np-core` and is MAJOR-locked at 1.0, which outlives an interface item. Verified on Temurin **17.0.20.1+1** and **21.0.12.1+1** — size: **S** · route: **frontier-reasoning / high**

## Milestone 6 — security

**Goal:** JWT and cryptography under the hardened profiles the ADRs pin, with negative tests as
first-class deliverables.

- [x] 6.0 **RFC-0005 — security module contracts.** Closes the FR-12 gaps (no AAD support; no per-key message cap driving the rotation trigger) and the NFR-11 gap (no CVSS failing threshold and no suppression-file policy, so the scan is a report rather than a gate). Also decides whether `OutputEncoder` may take a compile dependency on OWASP Java Encoder — a new third-party surface in `d4np-security`, legal under ADR-001 but a decision, not a default. Blocks 6.2–6.4. — **done: [RFC-0005](docs/rfc/0005-security-contracts.md) is drafted `Proposed` with an empty `approved-by:` — no RFC self-approves (AGENTS.md §6) — and awaits the owner's word, to be flipped in a separate commit.** **The item's first finding is in the line above it: the mandate list is incomplete, and so is the blocks clause.** The risk register's **R-06** — *JWKS trust posture unspecified*, medium — carries the remediation *Specify in RFC-0005: JWKS origin allowlist, explicit TLS posture, and whether the URL may come from caller config at all*, owned **6.0 → 6.1**. So there are **four** mandates rather than three, and **item 6.1 is blocked on this RFC too** — not for its algorithm profile, which ADR-003 owns, but for where its key document may come from. An item starting before this landed would have built the one part of FR-11 with no contract behind it. **FR-12's AAD gap is closed in two halves, and the split is the decision.** The envelope header — the literal `v1:{keyId}:` — becomes **mandatory GCM AAD**, because the threat model's reasoning that a `keyId` swap fails authentication holds only *accidentally* (the attacker picked a key that does not work) and does not cover `v1` **at all**: the first `v2` format creates a downgrade, strip `v2:` and write `v1:`. A **caller-supplied** AAD is then *offered rather than mandated*, and the rule separating this from FR-05's SQL and FR-08's queue bound is stated because it is the first time the answer is not force-it: **force the safe parameter when omitting it makes a mandated control unreachable, offer it when omitting it merely declines an optional one** — an unbounded queue makes FR-08's rejection handler unfirable, whereas no caller AAD disables nothing the spec mandates and forcing `new byte[0]` teaches people to defeat the parameter. **The per-key cap is the hardest decision in the document, and the answer is an asymmetry rather than a number.** NIST SP 800-38D §8.3 gives the number — **2³² invocations** under one key with random 96-bit IVs — and the arithmetic makes it operational rather than academic: at 10 000 encryptions per second one key exhausts it in **under five days**. But `AesEncryptor` is stateless and concurrent and the same key runs on every replica, so a process-local counter observes one pod in fifty. **A local count reaching 2³² is proof the global limit is breached; a local count below it proves nothing** — so the encryptor refuses at the local cap (sound, never a false refusal), exposes its count for host aggregation, routes the rotation trigger to the `KeyProvider` SPI because the cap is a property of the *key*, and documents the insufficiency in the imperative. That is [ADR-0037](docs/adr/0037-a-fencing-token-that-restarts-is-worse-than-none.md)'s shape on a second subject: enforce exactly the half you can prove and say so about the other. **Two bounds FR-12 never mentions are pinned, and one of them is currently enforced by accident:** GCM's per-invocation plaintext limit of 2³⁹−256 bits (~64 GiB) is unreachable today only because a Java `byte[]` caps near 2 GiB — **the JVM's array limit is doing the work, not this library** — which stops being true the moment a stream overload is added, so that overload must bound the plaintext explicitly; and the envelope's encoding was unspecified, which with `:` as delimiter is a *parsing* question, so `keyId` is constrained to `[A-Za-z0-9_-]{1,64}` and the IV and ciphertext are base64url without padding. **`CryptoException` gets a uniform message across wrong key, tampered ciphertext, AAD mismatch and malformed envelope, and that is C-01's next call site with a new justification:** every previous one was about *disclosure*, and this one denies a **decryption oracle** — an exception that says *bad tag* for one and *unknown key* for another hands an attacker a bit at a time. **R-06 closes in four parts, and the third is the one an implementer who satisfied the first two would omit:** the JWKS URL is fixed at construction from an origin allowlist and the attacker-controlled `jku`/`x5u` header claims are never consulted; HTTPS only, refused structurally rather than silently upgraded; **redirects are not followed**, because a permitted origin answering 302 to an attacker origin defeats the allowlist entirely; and the fetch is bounded in time and response size. **Certificate pinning is refused rather than silently omitted** — it is the strongest control available and it converts an identity provider's routine rotation into an outage in a system the host does not operate; a host wanting it supplies its own `SSLContext`. **The `OutputEncoder` question is answered yes, and item 6.0's own framing of it as “legal under ADR-001 but a decision” understates it: two gates say no today.** `d4np-security`'s enforcer allowlist permits exactly `it.d4np:*`, test scope and `com.nimbusds:*`, so the dependency fails `mvn validate` rather than review; and spec §3's row reads `security -> core [compile: nimbus-jose-jwt]`. Reimplementing loses on the only ground that matters — escaping correctness is measured in CVEs and is not a place to be original — and three conditions make taking it acceptable, each of which item 6.3 must **verify rather than assume**: zero transitive dependencies proven by `mvn dependency:tree`, no OWASP type in a published signature (ADR-0024's measured lesson), and a non-transitive `requires`. **The spec change is an amendment rather than a supersession, and the rule deciding that is new: supersede a contract, amend a fact.** RFC-0004 superseded FR-09's sentence under [ADR-0010](docs/adr/0010-single-specification-authority.md) rung 1 because it was *prose about behaviour*; spec §3's dependency row is a **fact about what the built artifact depends on**, mirrored by a build gate, and a superseded-but-wrong fact tells a reader something false about the artifact in their hands. **It lands with item 6.3 and not here**, because amending §3 now would make the specification describe a dependency the build does not yet have — the same falsehood in the other direction. **NFR-11's threshold is scope-dependent: fail at CVSS ≥ 7.0 for `compile` and `runtime`, report only for `test`.** 9.0 is defensible for an application that can compensate and not for a library whose production dependencies are a JOSE implementation and an encoder; 4.0 produces a steady stream of unactionable findings whose observable result is a growing suppression file, and **a gate that trains its owner to suppress is worse than no gate** — item 4.3's reasoning for refusing to ship a flaky NFR-03 gate. **The suppression policy is the half that decides whether the gate survives**: a CVE id, a stated reason, an **expiry date** and a review each release, with the expiry load-bearing because a suppression without one is a permanent exception created under time pressure. **Verifying NFR-11's posture answered two open register rows, and reporting them corrected a claim this project made two items ago.** A fresh `render.py` and a full recursive diff of `.github/workflows` now differs in **exactly one hunk — five comment lines**, so **R-07**'s twelve reverting references and its SHA-pinned `action-gh-release` regression are no longer reproducible, and **R-02**'s tag-pinned actions are all full SHAs. Both are *reported, not closed* — the register is `security-auditor`-owned and item 8.6 runs the next pass. **The provenance of item 8.9 is corrected in the same change:** it credited item 5.0 with finding the `ci.yml` drift, and the audit found it first as R-07; 5.0 found the residue left after R-07's main body was remediated. **No public type count is stated, deliberately** — RFC-0003 stated one and was wrong by exactly the nested types it named in prose, RFC-0004 stated two numbers to avoid that, and here the surface depends on the `KeyProvider` SPI shape items 6.1–6.3 still own, so a number now would be a guess dressed as a commitment; item 8.1 gets it from japicmp. **The review absence is recorded as the material fact about this document rather than as boilerplate:** every section is a security decision — an AEAD binding, a key-lifetime bound from NIST, a trust posture whose failure mode is universal token forgery, a gate threshold — and the `security-auditor` role owns the threat model and register this RFC reads from and writes back to. It has not reviewed the answers, so a reader should treat this as owner-approved and un-reviewed, with item 8.6 the first independent look. **The spec re-render was verified rather than asserted:** a fresh `render.py --out` of the amended manifest is **byte-identical** to the committed `docs/specs/01_spec_utils.md`, confirming that neither the `manifest_rev` bump (8 → 9) nor the `refs.rfcs` entry reaches the published spec. **`rfc_check.py` FAILs by design** ([ADR-0023](docs/adr/0023-the-owner-approves-this-projects-rfcs.md)). Verified on Temurin **17.0.20.1+1** and **21.0.12.1+1** — size: **M** · route: **frontier-reasoning / high**
- [ ] 6.1 `JwtTokenProvider` on Nimbus with the ADR-003 hardened profile — per-key algorithm allowlist, `alg=none` structurally impossible, mandatory `exp` with configurable skew, JWKS caching — plus negative tests (alg-confusion, alg=none, expired, wrong-audience) and RFC 7515/7519 vectors (ADR-003; FR-11) — size: **L** · route: **frontier-reasoning / high**
- [ ] 6.2 `AesEncryptor` AES-256-GCM with the rotation envelope, AAD, and a per-key message cap; IV-uniqueness property test over 10⁷ operations (RFC-0005; FR-12, NFR-06) — size: **L** · route: **frontier-reasoning / high**
- [ ] 6.3 `OutputEncoder` delegating to OWASP Java Encoder rather than reimplementing context-aware escaping (RFC-0005; FR-13) — size: **S** · route: **frontier-reasoning / high**
- [ ] 6.4 OWASP Dependency-Check CVSS threshold and suppression policy turned into a real gate (RFC-0005; NFR-11). **Note the plugin is not declared at all yet** — the `security / owasp dependency-check` CI job fails with `No plugin found for prefix 'dependency-check'`, so this item must first *introduce* the plugin and only then tighten it into a gate — size: **S** · route: **frontier-reasoning / high**

## Milestone 7 — adapters and test support

**Goal:** confine host coupling to the edge; ship the test-scoped helpers without letting production
code reach them.

- [ ] 7.0 **RFC-0006 — adapter boundary contracts.** The normative RFC 7807 exception→status mapping, the AOP/DI binding surface, the Redisson lock's reentrancy and lease behaviour against the FR-10 interface, and the test module's `--add-opens` contract. Blocks 7.1–7.4. — size: **M** · route: **frontier-reasoning / high**
- [ ] 7.1 `spring-adapter`: `GlobalExceptionHandler` with the RFC 7807 mapping table — `BusinessException`→422, validation→400, `StrategyNotFoundException`→500 + alert, fallback→500 (RFC-0006; FR-19) — size: **M** · route: **standard / high**
- [ ] 7.2 `spring-adapter`: AOP/DI binding for the metrics aspect (RFC-0006; FR-15 binding) — size: **S** · route: **standard / medium**
- [ ] 7.3 `lock-redisson`: Redisson `DistributedLock` implementation with container-based integration tests (RFC-0006; FR-10 implementation). **Carries three obligations filed by item 5.3.** **(a) Re-decide the convenience form with a call site in hand** ([ADR-0038](docs/adr/0038-refuse-the-convenience-form-the-rfc-sanctioned.md)): RFC-0004 permitted a `default` callback method and 5.3 declined it on two measurements — an `Optional<T>` return cannot tell *not acquired* from *the body returned null*, and a `Function`/`Consumer` pair is ambiguous. The shape to start from is the **void-only** form, `boolean tryRunExclusively(key, lease, wait, Consumer<LockHandle>)`, which has neither problem; adding it is MINOR. **(b) Return an empty fencing token unless the sequence is monotonic across failover** ([ADR-0037](docs/adr/0037-a-fencing-token-that-restarts-is-worse-than-none.md)) — Redisson cannot supply one that survives a failover, and a best-effort counter is *worse* than none because a resource cannot tell it from a sound one. Do not invent one. **(c) Prove the lease and the release-this-acquisition rules against a real backend**, which item 5.3's contract test asserts only against an in-memory reference; the container tests are where `DEL key` versus an owner-compared release becomes observable — size: **M** · route: **standard / high**
- [ ] 7.4 `test` module: `ReflectionUtils` with the `--add-opens` contract and the CI check that fails production code importing the test module (RFC-0006; FR-25) — size: **M** · route: **standard / high**

## Milestone 8 — release engineering

**Goal:** make the compatibility and supply-chain contracts enforceable, then publish **1.0.0**.
This is the milestone where `0.x` becomes `1.0.0` and binary compatibility starts being promised.

- [ ] 8.1 `japicmp` per-module binary-compatibility gate against the previous release (NFR-09) — size: **M** · route: **frontier-reasoning / high**
- [ ] 8.2 JaCoCo line coverage ≥ 85% and PIT mutation ≥ 60% enforced on `core` and `security` (NFR-10) — **and declare the `coverage` and `mutation` Maven profiles.** The quality job already runs `mvn -B verify -Pcoverage` and `mvn -B -Pmutation test`; neither profile exists, and Maven only warns on an unmatched profile, so those two steps pass having measured nothing — the same vacuous-green class recorded against 1.8. After item 1.4 the job is green on `spotless:check` + `checkstyle:check` for real, and on the other two steps only nominally — size: **S** · route: **standard / medium**
- [ ] 8.3 Perf gates pinned to a stable runner **or** compared against a stored per-runner baseline, replacing the absolute NFR-01/NFR-06 numbers in CI. This changes how a stated NFR is enforced, so it is a design decision, not plumbing: NFR-01's 2 ns/op sits near JMH's noise floor and hosted runners vary by more than 10%. Until this lands, the Milestone 2 perf numbers are tracked on the reference machine and **advisory in CI** — size: **M** · route: **frontier-reasoning / max**
- [ ] 8.4 GPG-signed Maven Central publication via Sonatype, sources + javadoc JARs, reproducible-build plugin (NFR-12) — size: **L** · route: **frontier-reasoning / high**
- [ ] 8.5 Build provenance/attestation (SLSA or Sigstore) and pinned CI action SHAs (NFR-12 gap) — size: **M** · route: **frontier-reasoning / high**
- [ ] 8.6 Threat model under `docs/security/` via `/eados audit` (STRIDE) — size: **M** · route: **frontier-reasoning / high**
- [ ] 8.7 Wire the Javadoc build into CI, and fix the `@value` reference that breaks it today — **filed by item 3.1, which found it rather than fixed it.** AGENTS.md §10 has required *"`Javadoc` builds without warnings"* since the scaffold and no CI job runs `javadoc`, so the gate has never executed. It does not currently pass: `mvn -B javadoc:javadoc -pl d4np-core` fails with **2 errors and 1 warning**, all three from `StrategyNotFoundException.java` (item 2.3), where a class-level `{@value #MAX_KEYS_IN_MESSAGE}` points at a `private` constant the Javadoc tool will not resolve at the default `protected` visibility. Item 3.1 left it alone deliberately — a one-line edit to another item's shipped file, bundled into an unrelated PR, is how a green gate gets a quiet exception — and the fix and the job belong together, because either alone is half a control. Scope: repair the reference (or widen the doclet's visibility, with the trade recorded), add the `javadoc` run to the quality job, and check every module rather than only `core`. **A second blocker was measured by item 4.5 and is scoped in here rather than fixed there:** `-Xdoclint:all -Xwerror` over `d4np-core,d4np-json,d4np-jdbc` is clean on JDK 21 and fails on JDK **17** with eight `warning: no comment` findings on the record components of `AuditEvent` and `ErrorDetail` — item 3.3's noise, which items 4.1–4.4 recorded as no longer recurring. Item 4.5 verified it against `origin/main` in a separate worktree and got the **identical eight**, so it is inherited rather than introduced; what it could not settle is *why the earlier claim held*, since this host runs Temurin **17.0.20.1+1** against those items' **17.0.20+8**. So this item also owns deciding whether the components get `@param` comments or the doclint level differs per toolchain — and note that a gate turned on without settling it is red on half the matrix on its first run — size: **S** · route: **standard / medium**
- [ ] 8.8 **Make NFR-03 a real CI gate** — read the two arms out of `target/jmh-result.json` and fail the build when `executorQuery / handWrittenLoop` exceeds **1.10**. **Filed by item 4.3, which measured why it could not be done there.** RFC-0003 called NFR-03 *"the one performance gate in this project that can be a real CI gate today"*, and the reasoning holds: it is a **relative** comparison between two arms in one JMH invocation, so a slow runner slows both and the machine cancels — which is exactly what item 8.3 exists to fix for NFR-01 and NFR-06, and exactly what this budget does not need. **What it does need is a sample size, and that is the measurement item 4.3 ran rather than assumed:** at CI's defaults (1 fork, 1 warmup, 1 iteration) three consecutive repetitions on one machine put the ratio at **0.56, 0.66 and 1.27** — a 2.3x spread across a 1.10 threshold, so the gate would fail roughly a third of green builds and teach the team that red CI is normal. At 5 forks x 10 iterations the comparison is usable but **not automatically stable, and the report says why**: one iteration out of fifty measured **3.767 ms** against a ~0.70 ms median — a GC pause inside a 1 s iteration — which on its own moved the JDK-21 ratio from **0.95** (median of fork means) to **1.07** (raw mean). Both are inside the 1.10 budget, so the *finding* does not depend on which is used; a *gate* would. **So this item owns a statistic, not only a sample size:** either enough forks that a single pause cannot move the mean by 12 points of a 10-point budget, or an outlier-resistant statistic such as the median of per-fork means. JMH writes both into `jmh-result.json`, so the choice is a decision rather than a plumbing constraint. So the work is: raise the fork and iteration counts for this benchmark only — the `benchmark` job already runs `-Pjmh verify`, and the counts are `-Djmh.forks` / `-Djmh.iterations` properties — measure the resulting wall-clock, and add the ratio check. Scope also covers the `matrix.toolchain` reference in the `benchmark` job, which resolves against no matrix today. Not item 8.3: that one is about absolute numbers needing a stable runner, and merging the two would hide the distinction RFC-0003 drew — size: **S** · route: **standard / medium**
- [ ] 8.9 **Gate the rendered CI workflow against its template, and reconcile the hand-edit that is already there.** **Filed by item 5.0 and re-attributed by item 6.0.** The provenance first recorded here was wrong: the audit found this drift before item 5.0 did, as risk-register entry **R-07** (*high*), which described **twelve** reverting references including a SHA-pinned `action-gh-release` regressing to an older SHA. Item 5.0 found the **residue** left after R-07's main body had been remediated — item 6.0 re-rendered and diffed the whole of `.github/workflows` and found **exactly one hunk, five comment lines**, with every action now SHA-pinned. The finding stands and the gate is still missing; only the credit moves. Repairing another item's shipped file inside an unrelated PR is the quiet exception item 3.1 declined to make. `.github/workflows/ci.yml` is **generated** by `.eados-core/tools/render.py` from `orchestrator/project.yaml`, and `consistency_lint.py` does **not** compare the two — so a hand-edit to the rendered file leaves no trace and the next render silently reverts it. There is one today, on `origin/main` and predating this milestone: the committed file carries **five comment lines** the render does not produce, explaining that the `dependency-check` step is still red and routing it to items 6.4 and 6.0. Measured, not inferred: `render.py orchestrator/project.yaml --out <tmp>` then `diff` reports exactly that hunk, while the same run reproduces `docs/specs/01_spec_utils.md` **byte-identically** — so the mechanism works and only this file has drifted. Two halves, and shipping either alone leaves the hole open: **move the five lines into `ci.extra_jobs`/`commands.*` in the manifest** so the render emits them, and **add a `ci-render` check to `consistency_lint.py`** so the next hand-edit fails rather than evaporates. Note that the divergence is *benign today* — the lines are comments — which is exactly why it survived, and why the gate is worth more than the reconciliation — size: **S** · route: **standard / medium**
- [ ] 8.10 **Settle the shared truncate-and-strip helper before 1.0 freezes the surface.** **Filed by item 5.3, which is the fourth C-01 call site and therefore the one item 4.5's deferral named.** Four types now strip ISO control characters and truncate a caller-supplied string before it reaches a log or an FR-19 body: `KeyDiagnostics` (core, a *set* of keys), `JsonDiagnostics` (64 chars of a JSON property name), `PageDiagnostics` (64 chars of a sort property) and `DistributedLockException` (64 chars of a lock key). **Item 4.5 deferred the extraction with the rule that a fourth call site should reopen it on its own terms, and item 5.1 correctly declined to count itself** — a pool name is a developer-supplied constant, so it was a fourth appearance of the *shape* and not of the *control*. A lock key is client-derived, so this one counts. **Re-examined on its own terms, the original objection is weaker than it was:** item 4.1 rejected sharing because *"the caps and the destinations differ, so a shared helper would have to be parameterised on all of it"* — but only the **cap** differs, and a cap is an argument; the destination is the caller's business, and the set-rendering case (`KeyDiagnostics`) is a genuinely different operation that would stay put. **What has not changed is the cost:** the helper must be **exported from `d4np-core`** and is then MAJOR-locked by japicmp at 1.0, which is why this is scheduled here rather than taken inside a feature item — before the freeze, and beside item 8.1 which owns the surface count. Decide between one public `bound(String, int)` in core and four private copies, and record whichever wins; **shipping 1.0 without deciding is the outcome to avoid**, because after it both directions are MAJOR — size: **S** · route: **standard / medium**
